# Master Todo — Personal AI Stock Advisor

This file is the canonical, ordered, end-to-end task list to reach product completion. It is the source of truth for what to do next. Every new chat reads it after `Project_State.md`.

**Created:** 2026-05-29 (Chat 5.8 — review + planning)
**Last updated:** 2026-08-01 (#72–#79 round-2 fix bundles ALL SHIPPED + #44 TD3 valuation_verdict split SHIPPED + #51 dividend_yield ×100 unified formatter SHIPPED. Backend HEAD `e79f2309` / frontend HEAD `ce7960fa`. Round-3 sanity pass: all eight bundle fixes verified on EC2 (123 pytest passed, hygiene PASSED, `/health` ok, deploy-ui clean, live corporate-action idempotency proven `already_recorded` on duplicate POST, fundamentals heartbeat confirmed populated-with-real-work metadata, valuation_verdict+valuation_rationale live in the new buy run). GO/NO-GO on #42: **GO** — the sole provisional NO-GO blocker (#72 U1-a/b corp-action idempotency) is CLEARED; all MEDIUM findings shipped or explicitly documented; #60/#61 remain doc-prep-only non-blocking; #43/#45/#46 deferred to post-launch per their standing Phase-12/Scheduler-chat ownership. #69 is now CLOSED. #42 GO-LIVE is UNBLOCKED — do not start #42 in this chat; run it as its own dedicated Chat 10 operation. **Prior update:** 2026-07-28 (#69 round-2 user-acceptance review — DEEP-DIVE FILING. Investigation-first code review of the ENTIRE codebase at backend `38f43c1` / frontend `4031a80` (6 parallel reviewers + self-verification of every BLOCKING/HIGH item at HEAD; two false positives killed — the dossier "naive/aware datetime TypeError" does not exist because `utcnow()` is naive, and `suggestion_run_locks` DOES have a 900s TTL so no permanent deadlock). Findings filed as EIGHT new bundle rows #72–#79 (Source `User review`, no renumbering), each carrying full per-finding detail: **#72 U1 corporate-action (#68) data-integrity** (HIGH — idempotency effectively off since `source_ref` defaults to `""` + NO unique index on `transactions.source_ref` → a duplicate SPLIT double-scales lots; demerger swallows `recorded_with_warning`; `compute_demerger_cost_split(parent_quantity=)` fed `child_quantity`; dialog NaN validation gaps + fan-out misses cost-basis/reconciliation), **#73 U2 alert reliability** (MEDIUM — audit row written AFTER push → re-fire on write failure; Tavily first-call-of-day race → false quota-exceeded aborts news run; Guard-B unbounded arm scan; dividend-drift marker write unguarded; intraday insert re-raise suppresses eval), **#74 U3 dividend-drift (#65) correctness** (MEDIUM — `refresh_dividends_for` delete-before-check wipes the window on a transient yfinance `[]`; `_held_names` ignores `quantity>0` → spurious nudge; expected_amount uses current qty; same-ex-date collide; N+1), **#75 U4 cron observability** (MEDIUM — `refresh_fundamentals.py` heartbeat `with`-block closes BEFORE all real work → Sunday refresh failures invisible to F4; `ctx.meta` never persists; documented crontab `--notify --run-type` flags don't exist), **#76 U5 suggestions robustness** (MEDIUM — `SuggestionTrackingStatus` Literal missing `"rejected"` → latent hydration crash; `_parse_dossier` brace/truncation; concentration denominator inflated; news windowed on `fetched_at`; tiny-universe all-50; manual-vs-cron lock gap), **#77 U6 txn edit/delete & FIFO edges** (MEDIUM — edit/delete recompute uncaught → 500 after ledger mutated; `preview_sell` remaining-invested omits fees; same-day FIFO tie-break diverges between `validate_replay` and the real replay/tax sort; search future-bound UTC not IST; dup import), **#78 U7 frontend correctness & fan-out** (MEDIUM — sell preview hardcodes `total_fees:"0"`; reconciliation lazy-invalidate + sign-blind drift badge; `trade_value` ignored; notes-panel stale-edit; edit-sheet corp-action guard), **#79 U8 docs & hygiene** (LOW/DOC — crontab-line drift, stale 2026-05-24 doc stamps, #51 `dividend_yield` ×100 confirmed backend-only, stale data_flow Q3; several FIXED in this same doc pass across both READMEs + data_flow + Project_State so there is ZERO remaining doc-drift). REVISED BLOCKING SET: after resolving item A via `crontab -l` (live line is `--direction=both` only → DOC-DRIFT not a live break) and killing the two false positives, **nothing is BLOCKING**; the highest are HIGH (#72 U1-a/b corp-action idempotency, #75 U4-a fundamentals-heartbeat, #76 U5-a tracking_status Literal). GO/NO-GO on #42: **provisional NO-GO until #72 U1-a/b (corp-action idempotency) is cleared or explicitly deferred** — it is a money-math data-integrity risk the moment real corporate actions are recorded post-import; everything else is MEDIUM/LOW and can be fixed in a bundle or deferred with a documented decision. #69 stays OPEN until the fix bundles land + a final round-3 sanity pass. **Prior update:** 2026-07-27 (#68 corporate-action processing automation SHIPPED — backend `f8778c8` (endpoint + service + tests, ONE code commit) + frontend `4031a80` (Record-corporate-action dialog + `lib/api.ts` binding, frontend-only). #68 is the DATA-ENTRY / automation front-end for the #53 cost-basis math (already correct at the FIFO source), turning the hand-edited `scripts/add_manual_transactions.py` workflow into a runtime flow. NEW `app/services/corporate_action_service.py` — pure, DB-free builders: `build_split_row` (a `type="SPLIT"` row whose meaning lives in `corporate_action.ratio_from/ratio_to`), `build_bonus_row` (a zero-cost `type="BUY" price=0` row — the REAL-ledger pattern the actual data uses, NOT the dead `type="BONUS"` branch), `build_demerger_child_row` (`source="manual_demerger"` child BUY carrying the apportioned §49(2C) cost as `price` + the inherited `acquired_date`, #53), `compute_bonus_quantity`, `compute_demerger_cost_split` (child per-share cost + retained parent factor + signed §49(2C) adjustment), `compute_parent_reprice` (instructions only). NEW `POST /transactions/corporate-action` (`RecordCorporateActionRequest`, discriminated on `action_type` split/bonus/demerger; idempotent on `source_ref`; `validate_replay` guard on split; bonus qty computed from the live holding or an explicit override for odd broker allotments like CONCOR; demerger inserts the child BUY, auto-creates the §49(2C) `cost_basis_adjustments` doc, and RETURNS the parent-reprice as an AUDITED follow-up — apply via `PATCH /transactions/{id}` — rather than silently mutating immutable BUY rows; recompute mirrors the add_buy/sell `recorded_with_warning` contract). NO FIFO change, NO new collection/index/dependency, `/tax` + `tax_service` untouched (compliance). NEW `tests/test_corporate_action.py` (18 hermetic tests: pure builders + endpoint wiring through the FakeCollection harness); `tests/conftest.py` `fake_db` gains `cost_basis_adjustments`. Frontend NEW `components/corporate-action-dialog.tsx` (a "Record corporate action" dialog mounted in the transactions header next to "View audit log"; per-type fields, client validation, synchronous `refetchQueries` fan-out; a demerger surfaces the §49(2C) adjustment + parent-reprice follow-up toast) + `lib/api.ts` `CorporateAction*` types + `recordCorporateAction()` binding + `app/transactions/page.tsx` mount. Verified on EC2: 105 pytest (87 + 18), hygiene PASSED, `/transactions/corporate-action` on the OpenAPI surface, bad-ISIN → 422, split-with-no-ratios → 400; `~/deploy-ui.sh` build+lint clean, `/transactions` → 200; `tsc --noEmit` clean. GO-LIVE (#42) blocking set drops #68 → remaining: **#69 (round-2 review, LAST)**.) **Prior update:** 2026-07-27 (#65 dividend-announcement capture + realised-P&L drift matrix SHIPPED — backend `1f390c0` (FOUR code commits: `2df217d` Unit 1 capture + `d8b9d35` Unit 2 matrix/nudge + `1f390c0` Unit 3 tests; `2e771c4` was the pre-flood-control matrix superseded by `d8b9d35`), frontend `92a8974` (Unit 4: `f5d8e2b` drift section + `92a8974` tweaks). Design-first item deferred from Bundle B to its own chat. Dividends COUNT toward actual gains (feed `total_dividends_*` via #63/#64) though not taxable capital gains, so an announced-and-ex-but-never-recorded payout silently understates realised gain — #65 automates catching it using yfinance corp-actions (structured "announced" truth into NEW `dividend_announcements`, captured by evolving the weekly `refresh_fundamentals` earnings pattern) + the #50/#57 news pipeline (`corporate_action` theme corroboration). NEW `reconciliation.compute_dividend_drift()` (PURE read: announced × recorded `DIVIDEND` txns × booked `total_dividends_received` per held name → `matched`/`missing_receipt`/`pending`; ±21d match window, 21d settle margin, `first_purchased_at<=ex_date` held-heuristic) + `evaluate_dividend_drift_alerts()` (ntfy-only rising-edge nudge via `dividend_drift_marker` docs in `reconciliation_snapshots`, success-gated, FLOOD-CAPPED at 3 individual + 1 summary — 40 missing → 4 pushes not 40) + `GET /reconciliation/dividend-drift` (read-only, NOT a tax view) + daily-cron guarded caller in `take_reconciliation_snapshot.py`. NEW `tests/test_dividend_drift.py` (11 hermetic); `FakeCollection` +`$gte/$gt/$lte/$lt`+multikey-equality; `fake_db` +`dividend_announcements`/`news_articles`. Frontend `lib/api.ts` types+`getDividendDrift()` + `app/reconciliation/page.tsx` read-only Dividend-drift card (announced vs est-received per stock, status badges, missing-count banner, news-corroboration icon w/ tooltip+legend). Verified on EC2: 87 pytest, hygiene PASSED, endpoint returns 44 announcements / 40 missing across 29 held names, nudge 4-then-0, `/health` ok, deploy-ui build+lint + browser smoke. No new dep, no `/tax` change (compliance). GO-LIVE (#42) blocking set drops #65 → remaining: **#68, #69 (round-2 review LAST)**.) **Prior update:** 2026-07-26 (#50 entity mis-tagging fix + #57 news alerts SHIPPED — backend `4a9851c` (TWO code commits: `28e57f6` #50 + `4a9851c` #57+LLM), frontend unchanged `a2ffef1`. **#50 SHIPPED** — `news_fetcher._persist_results` now gates `entities_isins`/`entities_symbols` on a deterministic relevance matcher (`_article_mentions_company`: NSE symbol whole-token OR distinctive company-name token; corporate suffixes + generic tokens stripped) before stamping the query ISIN; `$addToSet` branch likewise gated so cross-company accumulation stops; `fetched_for_*` provenance stays unconditional. NEW `scripts/retag_news_entities.py` backfill re-ran the same matcher over 1540 existing docs: 187 changed, 873 wrong ISIN tags stripped, 115 emptied. NEW `tests/test_news_fetcher.py` 10 hermetic tests. NEW `news_classifier.confirm_entities_llm()` Phase 2b post-classify Haiku pass: finds articles where `fetched_for_isins ⊃ entities_isins` (rule-gate rejected them), asks Haiku per (article, company) pair ("is this actually about `<company>`?"), `$addToSet` confirmed ISINs back; additive. EC2 result: 65 pairs checked, 2 confirmed (LLM rescued what rule-gate conservatively blocked), 63 rejected, 0 failed batches. **#57 SHIPPED** — NEW `app/services/news_alerts.evaluate_news_alerts(window_days=3)`: eligible ISINs = held (`"news"` in `alert_on`) UNION watchlist; fires on classified `severity=="high"` non-noise articles fetched within `window_days`; dedup per (isin, article) via `cited_news_ids` membership on `alerts_log` (success-gated, mirrors #41/#56 pattern — a failed ntfy never silences a real story); ntfy-only `push_public("news")`; persists `Alert(alert_type="news_event", channel="ntfy_public_news")`. Wired into `scripts/fetch_news_for_universe.py` as Phase 2b (`confirm_entities_llm`, guarded) + Phase 3 (`evaluate_news_alerts`, guarded) after classify; heartbeat keys `entity_confirm_*` + `news_alerts_fired/error`. NO model/index/collection/dep change. EC2 result: 29 eligible names, 0 high-sev fresh articles this week (correct — news runs Sunday; dedup second-run = 0). 76 pytest, hygiene PASSED, `/health` ok. GO-LIVE (#42) blocking set drops #50 + #57 → remaining: **#65, #68, #69** (round-2 review LAST).) **Prior update:** 2026-07-25 (#56 + #70 SHIPPED — backend `6f1aa57` + frontend `a2ffef1`. **#56 target/sell-price breach alerts** — a DIRECT mirror of the shipped #41 stop-loss path on the intraday write path, evolving the existing evaluator (no parallel path). NEW `price_service.evaluate_target_price_alerts(rows)` sits beside `evaluate_stop_loss_alerts`; `scripts/refresh_prices_intraday.py` calls it guarded on the SAME rows right after the stop-loss block (no parallel price-fetch loop; new `target_alerts_fired` heartbeat key). Every axis is the stop-loss logic INVERTED: fires rising-edge when the latest intraday price crosses strictly ABOVE `target_price`; gated on `target_price` set AND `"target"` in `alert_on` — the token is **`"target"`** (the Holding model default `alert_on=["stop_loss","target","earnings","news","52w_high"]`), NOT `"target_price"` (read the model, not the row wording); success-gated dedup (only a DELIVERED `target_hit` suppresses; re-arm on a later at/below tick); **#67 Guard-B analog** — a first-ever fire requires a prior intraday tick at/BELOW `target_price` (proof the price genuinely crossed UP through the target), so a garbage target set below every observed price never fires; ntfy-only via `push_public("price")`; persists `Alert(alert_type="target_hit", channel="ntfy_public_price", severity="high")` to `alerts_log`. Message shape `"<SYMBOL> ₹<ltp> crossed above target ₹<tp>"`. NO `app/db/indexes.py` change — the existing `alerts_log isin_type_sent_desc` (isin, alert_type, sent_at) is generic across `alert_type`, so `target_hit` reuses it (the row listed indexes.py on the named surface, but reading the code showed it already sufficient). NO `lib/api.ts` change — `Holding.target_price`/`alert_on` already present. Frontend `components/holding-stats.tsx` REPLACED to add a read-only Target strip mirroring the stop-loss strip (target value + `"X% to target"` / `"target reached"` green when `current >= target`; dashed hint otherwise); editing stays in `components/notes-panel.tsx`. NO hermetic unit test — #41 had none either, and `FakeCollection` lacks `$gt/$lte` + Decimal128 range support (adding it just for this would over-build the harness, an explicit Chat B anti-pattern); verified live on EC2 instead, mirroring #41. Verified on EC2: hygiene PASSED, 66 pytest, `/health` ok/ok; the fire→suppress→re-arm functional drive returned `1/0/1` with 2 delivered `target_hit` rows + 2 ntfy pushes received on the phone, and the Guard-B garbage-target case returned 0 (no fire). **#70 (#53-follow-up DATA ACTION)** — replaced the two demerger `acquired_date` PLACEHOLDER dates (`2022-01-01`) with the real earliest parent-BUY dates read from the ICICI order books: TMCV → **18-Oct-2023** (earliest TMPV `INE155A01022` lot; the 100-sh TMPV parent block was bought across THREE lots 5/22/73 on 18-Oct-2023 / 05-Jun-2024 / 02-Dec-2024, so the single `acquired_date` carries the EARLIEST/LTCG-favourable date — an accepted approximation for the multi-lot block, exact per-lot inheritance being the deferred Option B), JIOFIN → **25-Jul-2022** (earliest RELIANCE `INE002A01018` buy; single-block parent — all 5 JIOFIN came from that one 5-sh buy; the 2024 1:1 bonus post-dates the 2023 demerger and is NOT the parent — so this inherited date is EXACT). PLACEHOLDER + `TODO(sahil)` markers removed on both rows; no schema/behaviour change, covered by the existing green pytest. These dates take effect through the #42 GO-LIVE staging→promote re-seed. GO-LIVE (#42) blocking set drops #56 + #70 → remaining: #57, #65, #68, #69 (#57 still gated on #50; #69 round-2 review LAST).)
**Prior update:** 2026-07-25 (#55 SHIPPED — LLM-authored hold-horizon on buy suggestions, backend `931943b` + frontend `7324573`. Four LLM-authored flat keys on the BUY dossier — `hold_horizon` (short|medium|long), `hold_horizon_expected_move` (the time-bound expected gain that justifies committing capital that long — encodes the user's "money with no time-bound expected gain is dead capital" principle), `hold_horizon_rationale`, `hold_horizon_review_trigger` — added to `dossier_service._SYSTEM_PROMPT` (buy only), the buy branch of `_parse_dossier`, and the buy branch of `_empty_dossier`. Coerce-and-default leniency: the four keys are deliberately absent from `_parse_dossier`'s `required` list so a garbled/absent horizon never fails the whole narrative; off-list bucket → `medium`, blank prose → `(insufficient data)`. NEW `_clamp_sentence(text, limit)` helper clamps horizon prose on a sentence/word boundary (400-char budget) instead of a hard mid-word `[:300]` slice (fixes a truncation bug seen in first testing). KEY INSIGHT from reading the actual code at HEAD `408e5b7d`: the dossier is a plain dict (not a Pydantic model) persisted as `SuggestionRun.notes` JSON `{dossiers:[...]}`, and `routers/suggestions._serialize_run` already passes any new dossier key straight through — so `models/suggestion.py`, `scoring_service.py`, `explainability.py`, and `routers/suggestions.py` needed NO change despite being on the row's named surface (the horizon is LLM-authored in the dossier from the CandidateScore already in the prompt; it is not a scored signal). Frontend `lib/api.ts` `SuggestionDossier` gains four optional fields; `components/suggestion-card.tsx` renders a Clock horizon badge visible WITHOUT expanding (buy-side only) + an "Expected hold horizon" section (Why the wait pays / Rationale / Re-check early if), reusing the shipped `!value.startsWith("(")` availability idiom; two small label/color helpers; sell cards + old runs unaffected. ALSO shipped as a #55 follow-up (user request): a manual "Run now" trigger — NEW `POST /suggestions/run?direction=buy|sell` (fire-and-forget 202 via FastAPI BackgroundTasks calling the SAME `suggestion_engine.run_suggestions` the cron uses, `notify=False` so no digest email/ntfy and no outcome rows) + `GET /suggestions/run/status?direction=` + NEW `suggestion_run_locks` collection (`_id==direction`, 900s TTL) providing a fail-fast per-direction advisory lock (409 on an overlapping manual run or Sunday cron; mirrors the TD20 recompute_locks primitive, keyed by direction not ISIN) + `db/client.py` accessor + `db/indexes.py` TTL index; frontend `lib/api.ts` `triggerSuggestionRun`/`getSuggestionRunStatus` + a "Run now" button on `/suggestions` (header + empty-state) with 10s status polling that refetches + toasts when the run lands, and a clearer toast (no misleading "poll the endpoint" copy). NEW hermetic `tests/test_dossier_service.py` (9 tests: horizon parse, off-list/missing bucket → medium, blank prose → marker, sell path untouched, garbled horizon leaves narrative intact, `_clamp_sentence` boundary behavior, empty-dossier shapes). Verified on EC2: 62 pytest (53 + 9), hygiene PASSED, `/health` ok/ok, a fresh manual buy run authored a valid `medium` bucket + genuine time-bound expected-move for all 10 candidates end-to-end, sell dossiers carry no horizon keys, 202/409 concurrency guard, `~/deploy-ui.sh` build+lint clean + browser smoke. No `models/suggestion.py`/`scoring_service.py`/`explainability.py`/`run_weekly_suggestions.py` change; no new dep. GO-LIVE (#42) blocking set drops #55 → remaining: #56, #57, #65, #68, #70, #69.)
**Prior update:** 2026-07-21 (#53 Bundle D partial SHIPPED — demerger holding-period inheritance at the FIFO source, backend-only. Investigation-first re-read of the actual ledger encoding (`add_manual_transactions.py`) at HEAD `eb611a3f` corrected the broadened-#53 premise: SPLIT cost is already correct (`_fifo_replay` divides price / scales qty via ratios, total cost preserved) and BONUS cost is already correct (real bonuses are recorded as zero-cost `type="BUY" price=0` rows, NOT the dead `type="BONUS"` branch), so neither needed a change — touching correct data would be a regression. Also corrected a real doc-drift: there is NO `DEMERGER` transaction type; the type Literal is `BUY/SELL/DIVIDEND/BONUS/SPLIT` and demergers are `source="manual_demerger"` BUY rows. The one genuine defect was demerger HOLDING PERIOD: a receipt (TMCV, JIOFIN) is a BUY dated at the receipt date, so `tax_service._classify` measured the period from receipt, but the IT Act inherits the parent's original acquisition date — misclassifying a genuine LTCG disposal as STCG. FIX (Option A, surgical, single FIFO source of truth, no parallel path): NEW optional `Transaction.acquired_date` (holding-period date only — does NOT change cost basis or FIFO ordering; absent -> `trade_date`, so every existing row is byte-identical); `_fifo_replay._Lot` gains `acquired_date` (defaults to `trade_date` on a normal BUY via `tx.get("acquired_date") or trade_date`) and the SELL disposal emits `lot.acquired_date` as `_realized_lots.buy_trade_date` — `tax_service` needed NO change (already reads `buy_trade_date`); `scripts/add_manual_transactions.py` TMCV + JIOFIN rows carry `acquired_date` as a PLACEHOLDER date + `TODO(sahil)` to set the real earliest parent-buy date before the #42 import. 4 NEW tests (2 `test_fifo_replay` — inheritance + cost-basis invariance + no-override default; 2 `test_tax_service` — inherited-date LTCG reclassification + no-`acquired_date` STCG regression). Verified on EC2: 53 pytest (49 + 4), hygiene PASSED, `/health` ok/ok, `/tax/capital-gains` 200. Backend-only; no frontend, no new collection, no new dep, no index change. KNOWN LIMITATION: a single `acquired_date` carries one inherited date onto the single demerger receipt row — exact for this portfolio's single-block demergers (TMCV from one TMPV block, JIOFIN from one RELIANCE block); multi-lot pro-rata inheritance (Option B, full FIFO rewrite) is deliberately out of scope. Bundle D's #68 (corporate-action data-entry/automation front-end) remains OPEN. Filed #70 as an explicit #53-follow-up DATA ACTION: replace the two demerger `acquired_date` PLACEHOLDER dates with the real earliest TMPV / RELIANCE BUY dates before the #42 import (exact steps in the #70 row). GO-LIVE (#42) blocking set drops #53 -> remaining: #54, #55, #56, #57, #65, #68, #70, #69.)
**Prior update:** 2026-07-21 (#63 + #64 Bundle B partial SHIPPED: backend `2792cefa` + frontend `3cde0d39`. #63: `portfolio_service.compute_summary` now also sums `total_dividends_received` across all holdings in its existing all-holdings scan, exposing two NEW totals keys — `totals.total_dividends_lifetime` (dividends only) + `totals.total_realized_with_dividends` (realized_pnl_lifetime + dividends); `realized_pnl_lifetime` unchanged (capital-only). `routers/portfolio.py` empty-portfolio zero-block extended with the two keys. COMPLIANCE: dividends are income from other sources, NOT capital gains — they feed realised P&L / total-return ONLY; `tax_service` + `/tax` capital-gains view intentionally untouched. `lib/api.ts` `PortfolioTotals` extended with the two new string fields. `_fifo_replay` already computed `total_dividends_received` per holding and stored it to the holdings doc; NO holdings model or FIFO change needed. #64: `components/totals-row.tsx` REPLACED — re-added the "Realized P&L" StatCard (reverses #40); grid 3→4; re-imported `TrendingUp`; headline = `total_realized_with_dividends` (total money made), subtext = `Capital: inrSigned(realized_pnl_lifetime) / Dividends: inr(total_dividends_lifetime) · N closed` — compliance split visible; tooltip explains dividends-are-NOT-capital-gains and links to /tax. `app/page.tsx` DashboardSkeleton grid 3→4, 3→4 skeleton cards. Verified on EC2: 49 pytest, hygiene PASSED, `/portfolio/summary` returns the two new keys, math sum holds (capital + dividends == combined), frontend `~/deploy-ui.sh` build+lint clean, 4-card totals row renders. #65 (dividend-announcement capture + drift matrix) deferred to its own design+chat. GO-LIVE (#42) blocking set drops #63/#64 -> remaining: #53, #54, #55, #56, #57, #65, #68, #69.)
**Updated:** 2026-07-08 (Phase 10.5 round-1 fixes — #66 + #67 SHIPPED, backend-only, code HEAD beaadf70f8ca8bc6acf7c4edf5154e387819b484, frontend unchanged a4b27bd7. #66 (9pm heartbeat silence): cron_health_check is anomaly-only by design — the pre-2026-06-14 daily mail was the #47/TD22 track_suggestion_outcomes false-positive (fixed 2026-06-14, dates match), so the silence was CORRECT. Fix = a POSITIVE daily heartbeat in the healthy branch of scripts/cron_health_check.py (unconditional, no crontab/argparse change): low-priority ntfy on the errors channel + a Resend email listing the N expected crons healthy today; anomaly path + self-failure path + the run's own heartbeat write byte-unchanged; the heartbeat leg is best-effort and never raises. Verified live on EC2 (email + ntfy received). #67 (spurious stop-loss fires): #41's evaluator fired CORRECTLY on garbage stop_loss data (GAEL stop 1000 vs ~161, OLAELEC stop 212312 vs ~42) — not a regression; no RELIANCE holding/alert exists so no price-feed bug. Fix = Guard B in price_service.evaluate_stop_loss_alerts: a first-ever fire (no prior delivered alert) now REQUIRES proof of arming (some intraday tick at/above stop_loss), reusing the existing intraday >= stop_loss lookup; garbage stops never armed -> suppressed, TATASTEEL (stop 123 < price 189) unaffected. Verified on EC2: 49 pytest, arm-proof GAEL/OLAELEC=False, stop_loss_hit rows before==after==2 (zero spurious). Garbage stops also cleared by the #42 wipe. Bundle A (Phase 10.5 "blocking-set chat bundles", this chat) — #58 dashboard nav declutter + #59 audit back-nav + #62 collapsible dashboard sections — SHIPPED 2026-07-08, frontend-only, frontend HEAD e3a9a44b3aaf1597edeeca7500779c0eb2de3361 (backend unchanged beaadf70); reused the existing dropdown-menu primitive, no new dep; verified via deploy-ui build+lint. GO-LIVE (#42) blocking set now drops #58/#59/#62 (and earlier #66/#67) -> #53, #54, #55, #56, #57, #63, #64, #65, #68, #69 (#69 round-2 review still gates #42).)
**Updated:** 2026-07-03 (Chat C — #41 Wire holdings.stop_loss intraday breach alert SHIPPED: backend + frontend, single logical unit, closes TD6. Backend: NEW price_service.evaluate_stop_loss_alerts(rows) on the EXISTING intraday write path — scripts/refresh_prices_intraday.py calls it (guarded) right after insert_intraday_quotes on the SAME fetched rows (no parallel price-fetch loop; cron stays a thin caller). Rising-edge: fires once when the latest intraday price crosses BELOW a holding's stop_loss, stays quiet while it remains below, re-arms when a later tick shows price back at/above stop_loss. Only holdings with stop_loss set AND "stop_loss" in alert_on are evaluated. Dedup is SUCCESS-GATED: only a previously DELIVERED stop_loss_hit Alert (delivery_status=="sent") suppresses re-fires, so a transient ntfy outage can't silently swallow a real breach — the next 15-min tick retries. Transport ntfy-only via push_public("price", ...) (a daily email would be noise; mirrors #25 auto-drift); every attempt persists an Alert(alert_type="stop_loss_hit", channel="ntfy_public_price", severity="high") to alerts_log as the durable audit row — #41 is the FIRST writer of alerts_log. Message shape "<SYMBOL> ₹<ltp> crossed below stop-loss ₹<stop_loss>". NEW dedup index alerts_log isin_type_sent_desc (isin, alert_type, sent_at desc). No routers/holdings.py change — the PATCH whitelist already carries stop_loss/target_price/alert_on and get_holding already returns the full doc, so reader + writer were already sufficient. Frontend: components/holding-stats.tsx REPLACED to add a read-only stop-loss strip (stop value + cushion % vs current price, red when breached; dashed "No stop-loss set" hint otherwise) — editing stays solely in components/notes-panel.tsx (no duplicate editor; no lib/api.ts change — updateHolding + Holding.stop_loss already existed). Verified on EC2: uv run python -m pytest => 49 passed, check_datetime_hygiene PASSED, init_db shows alerts_log 5 indexes incl. isin_type_sent_desc, functional fire->suppress->re-arm returned 1/0/1 with two delivery_status=="sent" audit rows, ~/deploy-ui.sh build+lint clean. Backend code HEAD now eb964cc8d416d8e817f326501319fa19218b0d59, frontend a4b27bd7cb39efdca8c1b097cb7ba9c779bd4208; Phase-10 #41 COMPLETE — closes TD6; pointer advanced to #52 (Phase 10.5 User Acceptance Review).)
**Updated:** 2026-07-03 (Chat C — #40 Realized-P&L UI hide SHIPPED: FRONTEND-ONLY. The only passive realized-P&L render site in the UI was the "Realized P&L" StatCard in `components/totals-row.tsx` (`totals.realized_pnl_lifetime` + `${fully_exited_lifetime} closed positions`); removed it, dropped the now-unused `TrendingUp` import, and set the totals grid 4->3 cols; shrank the `app/page.tsx` DashboardSkeleton from 4 to 3 cards to match. `grep -rn realized` at frontend HEAD verified no other passive realized figure renders — `app/tax/page.tsx` (#39 capital-gains) KEPT, sell-sheet realized preview/toast KEPT (core sell decision-support), transactions delete-dialog + audit-intro copy KEPT (explanatory, no figure). Backend `holdings.realized_pnl` field intentionally unchanged (FIFO writes it AND #39 capital-gains reads it). Verified on EC2 via `~/deploy-ui.sh` (npm build + lint clean, no unused-import warning). Frontend code HEAD now `16fab5aef77c2b1884b7ec70667032f39876c265`; backend HEAD unchanged at `17f7cfc434505d7b82305f006b023b1f765cd70a`; pointer advanced to #41 (Chat C — stop_loss wiring).)
**Updated:** 2026-07-01 (Chat 9 — #39 F11 capital-gains pack SHIPPED: NEW `GET /tax/capital-gains?fy=YYYY-YY` returning an STCG/LTCG per-lot breakdown for the Indian financial year (1 Apr → 31 Mar, IST). Backend Unit 1 (`32088c938ecdfb9f58d40783f9ecf6da45819fe2`): NEW `app/services/tax_service.py` (read-only over the transaction ledger — replays via `holdings_service._fifo_replay`, the single FIFO source of truth, now emitting per-disposal `_realized_lots`; classifies STCG/LTCG at a strict >12-calendar-month boundary; §49(2C) demerger cost honored via the `manual_demerger` BUY rows already in the ledger — `cost_basis_adjustments` NOT re-applied, no double-count) + NEW `app/routers/tax.py` (`GET /tax/capital-gains`, `fy` optional → current IST FY, 422 on malformed/non-consecutive fy, router-local `_jsonable`) + `_fifo_replay` now emits `_realized_lots` (popped in `_recompute_holding_impl` so it never lands on the `extra=forbid` holdings doc) + `main.py` includes `tax.router` + NEW `tests/test_tax_service.py`. Frontend Unit 2 (`747ae4f29e0e0074b21eb3f34a3114a769e32752`): NEW `app/tax/page.tsx` (FY selector + STCG/LTCG/Total summary cards + per-lot disposals table, printable) + `lib/api.ts` capital-gains types + `getCapitalGains(fy?)` + `app/page.tsx` Tax nav link. Verified on EC2: `uv run python -m pytest` => 49 passed (28 existing + 21 new tax tests), `check_datetime_hygiene` PASSED, `/tax/capital-gains` live (default fy resolves to current IST FY `2026-27`; non-consecutive `2025-27` + malformed `2025` → 422; route on OpenAPI), frontend `/tax` → 200 + renders "Capital Gains". Backend code HEAD now `32088c938ecdfb9f58d40783f9ecf6da45819fe2`, frontend `747ae4f29e0e0074b21eb3f34a3114a769e32752`; Chat 9 / Phase-10 #39 COMPLETE; filed the bonus/demerger holding-period nuance as NEW-ITEMS #53; pointer advanced to #40 (Chat C).)
**Updated:** 2026-07-01 (Chat B — #38 JSON-structured logging SHIPPED: `app/main.py` `logging.basicConfig` -> stdlib `JsonLogFormatter` (`logging.Formatter` subclass) + `_configure_logging()` covering root + `uvicorn`/`uvicorn.error`/`uvicorn.access` (`propagate=False`, no double-logging); one single-line JSON object per record to stdout/journald (timestamp UTC ISO-8601 ms+Z from `record.created`, level, logger, message, module, func, line; traceback on exc_info; `extra={...}` merged); no new dependency; verified on EC2 (hygiene guard PASSED, 28 pytest, JSON app + `uvicorn.access` logs valid, no double-logging); backend code HEAD now `8127c6f071a127e76a0c1cc6afd8a686bcf0cb7c`; #38 is the LAST Chat B row — Chat B / Phase 9 COMPLETE; pointer advanced to #39)
**Updated:** 2026-06-15 (Chat B — Phase 9 hygiene sweep: #30 P2-1, #31 P2-8, #32 P3-2 all SHIPPED; 11-site `datetime.utcnow()`->`utcnow()` sweep, tree-wide tz-aware Mongo-write sweep + NEW tokenize-based `scripts/check_datetime_hygiene.py` lint guard, Python ceiling pin; pointer advanced to #33; #33, #36, #37, #38 remain OPEN in Chat B)
**Updated:** 2026-06-26 (Chat B — #33 Review-note pytest harness SHIPPED: hermetic, zero-dependency `tests/*` harness — 28 tests across the 6 review-mandated targets (`_fifo_replay`, `validate_replay`, `preview_sell`, `recompute_holding` idempotency, `submit_feedback` write-before-apply ordering, `take_auto_snapshot` drift math); new `tests/_fakes.py` (in-memory `FakeCollection` + `tx()`/`oid()` factories) + `tests/conftest.py` (`fake_db` fixture patching the `Collections.*` accessors); runs via `uv run python -m pytest`; verified on EC2 (28 passed) + hygiene guard still PASSED. Backend code HEAD now `04fd97090cd189b7f9a62fbc6fe3564c9cccb55d`; pointer advanced to #36; #36, #37, #38 remain OPEN in Chat B)
**Updated:** 2026-07-01 (Chat B — #36 Ops-gap `POST /admin/recompute/{isin}` SHIPPED: NEW `app/routers/admin.py`, Tailscale-only, delegates to the existing `holdings_service.recompute_holding` (TD20 per-ISIN advisory lock; NO parallel recompute path); ISIN pattern-validated; registered in `app/main.py`; returns `{status: recomputed/no_active_holding, isin, holding}`, 409 on lock contention; verified on EC2 (active recompute idempotent re-run, no-txn ISIN -> no_active_holding/null 200, bad ISIN -> 422, route on OpenAPI surface, 28 pytest + hygiene guard still green); backend code HEAD now `1ef0eadf4e95349fdce12156fc2c37d90bc3719d`; pointer advanced to #37; #37, #38 remain OPEN in Chat B. Frontend HEAD reconciled to `c5bb1a34d09dfc6d6879a3b2264310e4a9771c24` — one docs-only commit `c5bb1a3` (frontend README per-page reference + nav cover /watchlist, #29) since `58bf6369`, NOT a code change.)
**Updated:** 2026-07-01 (Chat B — #37 Ops-gap Atlas backup → fresh-DB restore rehearsal SHIPPED: rehearsed on EC2 against prod Atlas the surgical per-collection backup→restore path for `monitored_stocks` + `suggestion_outcomes` + `digest_deliveries` — per-collection `mongodump --db=portfolio --collection=<name>` (this `mongodump` 100.17.0 rejects `--nsInclude`) → `mongorestore --nsFrom 'portfolio.*' --nsTo 'portfolio_restore_test.*' --drop` into a FRESH scratch DB (Atlas user `portfolio_app` can create a second DB — no Unauthorized); counts matched prod (2 / 155 / 10), scratch DB dropped, prod `portfolio` untouched; exact dump/restore/verify/cleanup runbook documented in `Project_State.md` Section 4. DOC + OPS only, no `app/` code — backend code HEAD unchanged at `1ef0eadf4e95349fdce12156fc2c37d90bc3719d`; pointer advanced to #38; #38 remains OPEN in Chat B.)
**Updated:** 2026-07-01 (Chat B — #38 Ops-gap JSON-structured logging SHIPPED: `app/main.py` `logging.basicConfig` text formatter replaced by a stdlib `JsonLogFormatter` (`logging.Formatter` subclass, no new dependency — consistent with the #32 `>=3.12,<3.14` pin) + `_configure_logging()` that installs the JSON `StreamHandler` on the root logger AND `uvicorn`/`uvicorn.error`/`uvicorn.access` with `propagate=False` (request logs are JSON too, emitted exactly once). Each record -> one single-line JSON object to stdout/journald: timestamp (UTC ISO-8601 ms+Z derived from `record.created` via `datetime.fromtimestamp`, NOT `now()`/`utcnow()` — clean against `check_datetime_hygiene`), level, logger, message, module, func, line; `traceback` on exc_info; any caller `extra={...}` merged at top level. Verified on EC2: `check_datetime_hygiene` PASSED, `pytest` 28 passed, journald shows valid JSON for app (`app.main` "API ready"), `uvicorn.error`, and `uvicorn.access` records, no double-logging (3 `/health` calls -> 3 access lines). No new dependency, no `uv.lock` change. Backend code HEAD now `8127c6f071a127e76a0c1cc6afd8a686bcf0cb7c`; #38 is the LAST Chat B row — Chat B / Phase 9 COMPLETE; pointer advanced to #39 (Phase 10, Chat 9 / F11 capital-gains pack).)
**Current backend SHA (2026-07-26 #50/#57 close):** deployed code HEAD `4a9851c` (#50 entity relevance gate + retag + 10 tests + LLM confirm pass; #57 `news_alerts.evaluate_news_alerts` + `fetch_news_for_universe.py` Phase 2b+3; 76 pytest, hygiene PASSED; the doc commit advances HEAD further). **Current frontend SHA:** `a2ffef1f810422d452441744127f715bb1103eb4` (unchanged — #50/#57 are backend-only). **Prior current backend SHA (2026-07-25 #56/#70 close):** deployed code HEAD `6f1aa57d26c19b7ed110398a20fe8c8b9b1a20cd` (#56 target-price intraday breach alerts — NEW `price_service.evaluate_target_price_alerts`, `refresh_prices_intraday.py` guarded caller; + #70 real demerger `acquired_date` TMCV 2023-10-18 / JIOFIN 2022-07-25; no new collection/index/dep; verified on EC2 — hygiene PASSED, 66 pytest, functional fire→suppress→re-arm 1/0/1 + 2 ntfy, Guard-B=0; the doc commit advances HEAD further). **Current frontend SHA (2026-07-25 #56 close):** `a2ffef1f810422d452441744127f715bb1103eb4` (#56 read-only Target strip in `components/holding-stats.tsx`; tsc clean; no `lib/api.ts` change). **Prior backend SHA (2026-07-21 #53 close):** deployed code HEAD `5604840` (#53 Bundle D partial — NEW optional `Transaction.acquired_date`; `_fifo_replay._Lot.acquired_date` emitted as `_realized_lots.buy_trade_date`; `add_manual_transactions.py` TMCV + JIOFIN carry `acquired_date`; 4 new tests; backend-only, no frontend/collection/dep/index change; the doc commit advances HEAD further). Frontend unchanged at `3cde0d397bea87e0d875062d2075dcd6f665b111`. **Prior backend SHA (2026-07-21 #63/#64 close):** deployed code HEAD `2792cefa3e85f1e77d49806f2014289f75fadd39` (#63 + #64 Bundle B partial — `portfolio_service.compute_summary` extended with `total_dividends_lifetime` + `total_realized_with_dividends`; `routers/portfolio.py` zero-block extended; `lib/api.ts` `PortfolioTotals` extended; `components/totals-row.tsx` REPLACED to re-add Realized P&L card (reverses #40) with headline=total_return, subtext=Capital/Dividends/N-closed split; `app/page.tsx` DashboardSkeleton 3→4; no new dep, no new collection, no FIFO change; tax_service + /tax untouched; verified on EC2: 49 pytest, hygiene PASSED, sum assertion true, deploy-ui build+lint clean). **Current frontend SHA (2026-07-21 #63/#64 close):** `3cde0d397bea87e0d875062d2075dcd6f665b111`. The doc commit advances HEAD further.
**Audit baseline:** Backend SHA `c6b1437b90c9555ab9090657af74ab550cf6e1cd`, Frontend SHA `4f31b49b103f92ea5b4721f9728156041e908f49`
**Current backend SHA (Chat 9 close):** deployed code HEAD `32088c938ecdfb9f58d40783f9ecf6da45819fe2` (Chat 9 #39 F11 capital-gains — backend Unit 1: NEW `app/services/tax_service.py` + `app/routers/tax.py`, `_fifo_replay` emits `_realized_lots`, `main.py` includes `tax.router`, NEW `tests/test_tax_service.py`; the Chat 9 doc commit advances HEAD further). **Current frontend SHA (Chat 9 close):** `747ae4f29e0e0074b21eb3f34a3114a769e32752` (Chat 9 #39 Unit 2: NEW `app/tax/page.tsx` + `lib/api.ts` capital-gains types/binding + `app/page.tsx` Tax nav). Prior: **Current backend SHA (Chat B close):** deployed code HEAD `8127c6f071a127e76a0c1cc6afd8a686bcf0cb7c` (Chat B #38 — the JSON-structured logging commit: `app/main.py` `logging.basicConfig` -> stdlib `JsonLogFormatter` + `_configure_logging()` covering root + `uvicorn`/`uvicorn.error`/`uvicorn.access`; one single-line JSON object per record to journald; no new dependency; the Chat B `Project_State.md` + `master_todo.md` doc commit advances HEAD further). #38 is the LAST Chat B row — shipping it CLOSES Chat B / Phase 9; verified on EC2 (hygiene guard PASSED, 28 pytest, JSON app + `uvicorn.access` logs valid, no double-logging). Prior: Chat B #36 was the `POST /admin/recompute/{isin}` admin-recompute commit on `1ef0eadf4e95349fdce12156fc2c37d90bc3719d`. #36 (Ops gap): NEW `app/routers/admin.py` exposes `POST /admin/recompute/{isin}` (Tailscale-only) — the HTTP replacement for SSH-shell recovery of a stuck holding (the TD19/#7 fallback) — delegating to the existing `holdings_service.recompute_holding` (per-ISIN advisory-locked, TD20; no parallel recompute path); ISIN `pattern=r"^[A-Z0-9]{12}$"` (422 at the boundary); returns `{status: "recomputed", isin, holding}` on an active result, `{status: "no_active_holding", isin, holding: null, message}` when `recompute_holding` returns None (full exit / no transactions — 200, not 404), 409 on TD20 lock contention, 500 otherwise; registered in `app/main.py`. Verified on EC2 against localhost:8000: active recompute -> `recomputed` + idempotent re-run (byte-identical aggregates), no-transaction ISIN -> `no_active_holding`/`null`, bad-shape ISIN -> 422, route on the OpenAPI surface, `uv run python -m pytest` => 28 passed + `uv run python -m scripts.check_datetime_hygiene` still PASSED. Prior: Chat B #33 was the pytest-harness commit on `04fd97090cd189b7f9a62fbc6fe3564c9cccb55d`. Chat B shipped Phase 9 #30 + #31 + #32 (2026-06-15) on `025b8a0688a67d181f09ed800b6692f574fdb9bc`, then #33 (2026-06-26) on `04fd970`. #33 (Review note): hermetic, zero-dependency `tests/*` pytest harness — 28 tests across the 6 review-mandated targets; new `tests/_fakes.py` (in-memory `FakeCollection`: `find/find_one/insert_one/update_one` upsert/`delete_one`/`delete_many`/`count_documents` with `$ne`/`$in`/`$exists`/`$or`/`$and`; `tx()`/`oid()` factories) + `tests/conftest.py` (`fake_db` fixture monkeypatching the `Collections.*` accessors); `recompute_holding` gets full DB idempotency (run twice -> stable aggregates + preserved `created_at` + exactly one active doc); `fetch_metadata`/`_get_our_numbers`/`_send_auto_drift_alert`/`log_change` monkeypatched so nothing hits Atlas/yfinance/ntfy; verified on EC2 `uv run python -m pytest tests/ -v` => 28 passed + `uv run python -m scripts.check_datetime_hygiene` still PASSED. Backend + doc only; frontend unchanged `58bf6369`. #30 (P2-1): 11 `datetime.utcnow()`->`utcnow()` sites across 5 files (`routers/portfolio.py`, `services/scoring_service.py`, `services/dossier_service.py`, `services/fundamentals_service.py`, plus 2 stragglers in `services/conversation_service.py` the row's stale list missed). #31 (P2-8): tree-wide tz-aware `datetime.now(timezone.utc)` Mongo-write sweep->`utcnow()` (15 write/query sites, incl. the `reconciliation.take_manual_snapshot` + `price_service.bulk_get_latest_intraday` sites the hygiene guard caught after a `replace_all` partial-match miss) + 19 `# tz-ok:` annotations + NEW `scripts/check_datetime_hygiene.py` tokenize-based lint guard. #32 (P3-2): `requires-python = ">=3.12,<3.14"` pin + `uv lock` relock (no dep churn). Chat B opened at backend `0b6e1147` (the Chat 8 doc-commit HEAD). Prior: **Current backend SHA (Chat 8 close):** deployed code HEAD `67704025650bed4cbce9549ea45a064bae892c12` (#29 CORS fix — add PUT to allow_methods; the Chat 8 `Project_State.md` + `master_todo.md` doc commit advances it further). Chat 8 shipped #29 (F13 watchlist) across four backend/frontend code commits plus one CORS fix — Unit 1 write-model + universe set-math (`34ff906d7cf6b2c8d1e1ccc210810023f069f7aa`: `MonitoredStockWatchlistPatch` + `build_universe` = NIFTY 100 ∪ watchlist + `get_watchlist_isins`), Unit 2 `/watchlist` CRUD (`a250d00189c67ddc073715affc9175b9fc68383d`: new `routers/watchlist.py` + widened audit-action Literal + `main.py` include), Unit 3 cron coverage (`9857570b8e1799c274d2ce422b44bef419f17d11`: `refresh_fundamentals.py` + `fetch_news_for_universe.py` fold in watchlist ISINs), and the CORS fix (`67704025`: `main.py` `allow_methods` += `PUT`). **Current frontend SHA (Chat 8 close):** `58bf6369e73916c26e534fc517ac92f2f3dfedb5` (Unit 4 — `lib/api.ts` watchlist types + bindings, new `app/watchlist/page.tsx`, `app/page.tsx` Watchlist nav link). Chat 8 opened at backend `803e6610` (Chat 7 code HEAD; the Chat 7 doc commit `c162d9c2` was the actual open base for the file re-reads) and frontend `e14d6a75`. Prior: **Current backend SHA (Chat 7 close):** deployed code HEAD `803e6610ec21a8bad9a56840abc059cf92db5890` (#28 F15 by-tag — Unit 2; the Chat 7 `Project_State.md` + `master_todo.md` doc commit advances it further). Chat 7 shipped #28 (F12 + F15) across two backend code commits — Unit 1 risk-summary (`97041621eedd94947a2ce2c1843d23f317b1b31b`: extract `_annotate_holdings` from `compute_summary` behaviour-preserving + `compute_risk_summary` + `GET /portfolio/risk-summary`) and Unit 2 by-tag (`803e6610`: `GET /portfolio/by-tag`) — plus two frontend code commits. **Current frontend SHA (Chat 7 close):** `e14d6a750f802dae941d512837ff1788a7a3a0f0` (Unit 3 risk-summary card + api bindings + Tags nav, then Unit 4 `/tags` page). Chat 7 opened at backend `5e787c9842496a720768f867560d18c31220ea81` (Chat 6 code HEAD; the Chat 6 doc commit `a104993` was the actual open base) and frontend `6093f6342e6a6ddb1ecf0c8a1b7fa2239d825c7d`. Prior: **Current backend SHA (Chat 6 close):** deployed code HEAD `5e787c9842496a720768f867560d18c31220ea81` (#27 F1+F3 ad-hoc chat shipped across five backend commits — Unit 1 data layer, Unit 2 enrichment orchestrator, Unit 3 chat service + endpoints, then the `/instruments/search` route-shadow fix; the Chat 6 doc commit advanced it further). **Current frontend SHA (Chat 6 close):** `6093f6342e6a6ddb1ecf0c8a1b7fa2239d825c7d` (Unit 4 chat UI — ChatPanel + StockResearchPanel + lib/api.ts; no new npm dependency). Chat 6 opened at backend `4403bb5348c37e323824efc03777050e3631ce15` and frontend `f59958015b8b07b6e84e3add7b4a302d32b43490`. Prior: **Current backend SHA (Chat A close):** deployed code HEAD `fae6edf446dab815982b767b8f9a15c2fe36e6b5` (the #48 doc-only commit; the Chat A `Project_State.md` + `master_todo.md` doc commit advanced it further). Chat A shipped seven items across five code/doc commits: #34 + #35 (Ops gaps) on `bd52c6baf9a37325cc91d50f495bee8d4be70421`; #25 (P2-7) on `1340396eb1d614a1d171c5c5c3a97f35330bf64e`; #49 (TD40) + #26 (P2-6) on `6032b644c43132aeda96807448836573cadc154a`; #47 (TD22) on `4b638e6f5282883c094f4233e288b24294157565`; #48 (TD36, doc-only) on `fae6edf446dab815982b767b8f9a15c2fe36e6b5`. Chat A was backend + doc only (frontend unchanged). Chat A opened at backend `c4b50364eb5dd12bca46649c702afcd00677eb5d` (the Chat 5.19 doc commit). Prior: **Current backend SHA (Chat 5.19 close):** deployed code HEAD `7fcda9eb1b0e7a5cee7aba684ef7129d77b9b977` (Phase 6 TD39 / #24 self-failure dual-transport alert wrapping `cron_health_check.main`'s Mongo reads shipped on ONE backend code commit; HEAD shown is after that code commit — the Chat 5.19 doc commit advances it further). Chat 5.19 opened at backend `247510f105969f991cf661eabada2b25d47ce799` (the Chat 5.18 doc commit). Prior: **Current backend SHA (Chat 5.18 close):** deployed code HEAD `0515fef4c7384710124dbc2f73ff726662d61137` (Phase 6 TD38 / #23 fallback heartbeat log + dual-source health check shipped on ONE backend code commit; HEAD shown is after that code commit — the Chat 5.18 doc commit advances it further). Chat 5.18 opened at backend `98ead2a2e499f09bdade95b3201b7d76d7531ee6` (the Chat 5.17 doc commit). Prior: **Current backend SHA (Chat 5.17 close):** deployed code HEAD `1d627d7861ead18b05df0158fe41c06c42de89d2` (Phase 6 TD37 / #22 reject NaN in `_to_decimal` shipped on ONE backend code commit; HEAD shown is after that code commit — the Chat 5.17 doc commit advances it further). Chat 5.17 opened at backend `f4168b3343725c0f873d5fd99d1c68bc5d6c88cc` (the Chat 5.16 doc commit). Prior: **Current backend SHA (Chat 5.16 close):** deployed code HEAD `f4168b3343725c0f873d5fd99d1c68bc5d6c88cc` (Phase 6 TD35 / #21 explicit `inserted_id` flow into digest delivery shipped on ONE backend code commit — the Chat 5.16 doc commit advances it further). Chat 5.16 opened at backend `67fd6803d5ffd609e378b4219c5a4710046247aa` (the Chat 5.15 doc commit). Prior: Chat 5.15 closed at deployed code HEAD `7d77b9cbee9f3155f22c86057b20640f21599ee9` (Phase 6 TD34 / #20 `notify.email()` transient-5xx/429 retry on ONE backend code commit); it opened at backend `582cd18d5d50d90b1ae4d1174a22a59799d69ca0` (the Chat 5.14 doc commit). Prior: Chat 5.14 closed at deployed code HEAD `4ac2c955782490818eefa6024c9daead92b0b0eb` (Phase 6 TD33 / #19 atomic Tavily quota claim on ONE backend code commit) + frontend `f59958015b8b07b6e84e3add7b4a302d32b43490` (unchanged — backend-only); it opened at backend `5ab01ef0df2ebb3c3d1d0aba26cdce9be17c17fe` (the Chat 5.13 doc commit). Prior: Chat 5.13 closed at deployed code HEAD `090d96c0042e7d5ccd154dcaf6329a0bba57ebb7` (TD29 dead-import, TD31 ISIN pattern, TD32 regex `$options` drop on three backend code commits) + frontend `f59958015b8b07b6e84e3add7b4a302d32b43490` (TD28 refetchQueries swap on one frontend commit); it opened at backend `07d9a413b39d330e3ea9047dec4e38917a446449` and frontend `4f31b49b103f92ea5b4721f9728156041e908f49`.

> Note (Chat 5.9): the on-disk copy of this file had the "Ordering rationale" + "When you finish an item…" paragraph duplicated 8 times (a paste/commit artifact). The Chat 5.9 full-file replacement collapsed it back to a single copy. No item rows were affected.

---

## Current position

**Next item to start: Chat 10 GO LIVE (#42) — #69 is CLOSED, all eight #72–#79 fix bundles are SHIPPED (backend `e79f2309` / frontend `ce7960fa`), #44 + #51 shipped in the same session, round-3 sanity pass PASSED, GO/NO-GO = GO. Do NOT start #42 in the current chat; open a dedicated Chat 10 for the one-time real ICICI data import (wipe + re-import scope: ONLY `transactions`/`transactions_staging`/`holdings`; preserve `monitored_stocks`/`suggestion_outcomes`/`digest_deliveries`/instruments/prices/news). Remaining non-blocking open items: #46 (TD21 Scheduler — its own dedicated chat, explicitly after GO-LIVE); #60/#61 (doc/prep only, non-blocking); #43/#45 (Phase 12 deferred TDs, post-launch).

**Round-3 sanity pass result (2026-08-01, backend `e79f2309` / frontend `ce7960fa`):** All eight #72–#79 bundle fixes verified: (1) corp-action idempotency proven live — duplicate POST returns `already_recorded`, one SPLIT row, partial-unique index `source_ref_unique_nonempty` confirmed on Atlas; (2) alert reliability — evaluators no longer crash post-push, Tavily first-call retry prevents false quota-exceeded, Guard-B arm probe bounded to 30d; (3) dividend-drift — window preserved on transient yfinance failure, qty>0 filter confirmed, N+1 reduced to 1 batched news query; (4) fundamentals heartbeat shows real work (metadata.fundamentals/.earnings/.dividends populated, `finished_at` reflects real duration); (5) `SuggestionOutcome(tracking_status="rejected")` hydrates without error; (6) edit/delete recompute returns `recompute_warning` not 500; (7) sell-sheet preview passes real fees, reconciliation updates synchronously, SPLIT/BONUS/DIVIDEND edit shows guard notice; (8) #51 unified — `fmt_pct is _fmt_pct → True`, `0.0046 → "0.46%"`. 123 pytest passed, hygiene PASSED, `/health` ok, `~/deploy-ui.sh` build+lint clean, `/suggestions/latest` returns the new `valuation_verdict`+`valuation_rationale` keys (confirmed in Atlas `notes.dossiers[0]`). No regressions found. GO/NO-GO = **GO**.

**(Prior) Next item to start: work the #69 round-2 fix bundles #72–#79 (lowest-numbered blocking first: #72 U1 corporate-action data-integrity), then a final round-3 sanity pass, then the GO/NO-GO call on #42. #69 (round-2 user-acceptance review) was RUN 2026-07-28 as a full investigation-first codebase deep-dive at backend `38f43c1` / frontend `4031a80`; it filed EIGHT bundle rows #72–#79 (Source `User review`, no renumbering) — see the header "Last updated" entry and the NEW ITEMS rows for the full per-finding detail. #69 stays OPEN (gates #42) until the fix bundles are shipped-or-deferred. Provisional GO/NO-GO = NO-GO until #72 U1-a/b (corp-action `source_ref=""` idempotency hole + no unique index → duplicate SPLIT double-scales lots) is cleared or explicitly deferred; all other findings are MEDIUM/LOW. Two false positives were investigated and NOT filed (dossier datetime-TypeError — `utcnow()` is naive; suggestion_run_locks "permanent deadlock" — a 900s TTL exists). #68 SHIPPED 2026-07-27 (backend `f8778c8`, frontend `4031a80`) — the corporate-action data-entry/automation front-end for the #53 cost-basis math (Bundle D). NEW `app/services/corporate_action_service.py` (pure builders: split row / zero-cost bonus BUY / manual_demerger child BUY with inherited `acquired_date` / §49(2C) split math / parent-reprice instructions) + NEW `POST /transactions/corporate-action` (`RecordCorporateActionRequest`, idempotent on `source_ref`, `validate_replay` guard on split, bonus qty from live holding or explicit override, demerger inserts child BUY + auto-creates the §49(2C) `cost_basis_adjustments` doc + returns the parent-reprice as an AUDITED PATCH follow-up — never a silent ledger mutation) + NEW `tests/test_corporate_action.py` (18 hermetic) + `conftest` `fake_db` +`cost_basis_adjustments`. Frontend NEW `components/corporate-action-dialog.tsx` + `lib/api.ts` `CorporateAction*` types/binding + `app/transactions/page.tsx` mount. NO FIFO change, NO new collection/index/dep, `/tax` untouched. Verified on EC2: 105 pytest (87 + 18), hygiene PASSED, route on OpenAPI, 422/400 guards fire, deploy-ui clean, `/transactions` → 200. GO-LIVE (#42) blocking set drops #68 → remaining: **#69 (round-2 review, LAST)**. Bundle D is now fully SHIPPED (#53 + #68).**

**(Prior) Next item to start: #65 SHIPPED 2026-07-27 (backend `1f390c0`, frontend `92a8974`). GO-LIVE (#42) blocking set remaining: #68, #69 (round-2 review LAST). #68 (automate corporate-action processing — data-entry/automation front-end for the #53 cost-basis math, Bundle D) is the next blocking item; #69 round-2 review runs LAST. #65 (dividend-announcement capture + realised-P&L drift matrix) shipped across FOUR backend commits + TWO frontend commits, a design-first item deferred from Bundle B to its own chat. Directive: dividends COUNT toward actual gains (they feed `total_dividends_*` via #63/#64) even though they are not taxable capital gains, so a payout that was announced and went ex while held but never recorded silently understates realised gain — #65 automates catching that, using yfinance corp-actions for the structured "announced" truth AND the #50/#57 news pipeline (`corporate_action` theme) for corroboration. Unit 1 (backend capture, `2df217d`): NEW `app/models/dividend_announcement.py` (`DividendAnnouncement`, one doc per `(isin, ex_date)`, `amount_per_share: Money`; mirrors `earnings_event.py`) + NEW `Collections.dividend_announcements()` + NEW `dividend_announcements` indexes (`(isin, ex_date)` unique / `ex_date_desc` / `isin`, mirrors `earnings_calendar`) + `fundamentals_service.fetch_dividends_yfinance`/`refresh_dividends_for`/`refresh_dividends_universe`/`get_dividend_announcements_for_isin` (yfinance `Ticker.dividends`, replace-recent-window (`_DIVIDEND_REPLACE_WINDOW_DAYS=400`) upsert, exactly mirrors the earnings refresh — same provider/universe/idempotency) + `scripts/refresh_fundamentals.py` guarded dividend-refresh on the SAME universe after the earnings block (best-effort, never affects exit code). Unit 2 (backend matrix + nudge, `d8b9d35`): `reconciliation.compute_dividend_drift()` — a PURE read joining announcements × recorded `DIVIDEND` transactions × booked `holdings.total_dividends_received` per held name, classifying each ex-date `matched` / `missing_receipt` / `pending` (date-window match `_DIVIDEND_MATCH_WINDOW_DAYS=±21d`; `missing_receipt` only when ex-date passed by `_DIVIDEND_SETTLE_MARGIN_DAYS=21d` AND held across it via the `first_purchased_at <= ex_date` heuristic — precise point-in-time FIFO reconstruction deliberately out of scope for round 1); news `corporate_action` corroboration reuses the #50 entity-accurate query. `reconciliation.evaluate_dividend_drift_alerts()` — ntfy-only nudge on the `price` channel for NEW `missing_receipt`s, rising-edge deduped via a `type="dividend_drift_marker"` doc in `reconciliation_snapshots` (keyed `dividend_missing:<isin>:<ex_date>`), success-gated (marker written only after a delivered push -> a transient ntfy outage retries next run; mirrors `_send_auto_drift_alert` — NO new AlertType, NO `alerts_log` write, NO new collection). FLOOD CONTROL (`_DIVIDEND_NUDGE_INDIVIDUAL_CAP=3`): at most 3 individual nudges/run; the remainder roll into ONE summary push (a fresh install legitimately has a large backlog of never-recorded historical dividends — verified live: 40 newly-missing → 3 individual + 1 summary = 4 pushes, not 40; markers still written for all 40 so the count stays exact and never re-floods). NEW `GET /reconciliation/dividend-drift` (read-only, `_serialize` reused; NOT a tax view — dividends are income, not capital gains, `tax_service`/`/tax` untouched per compliance). `scripts/take_reconciliation_snapshot.py` gains a guarded `evaluate_dividend_drift_alerts()` call in the daily cron + `dividend_drift_nudges` heartbeat key (dividend drift is a reconciliation concern, so it rides the daily reconciliation cron, NOT the news cron). Unit 3 (tests, `1f390c0`): NEW `tests/test_dividend_drift.py` (11 hermetic tests of the classifier — matched / missing / pending future / pending within-settle-margin / pending not-held-across / ±21d window / recorded-outside-window-stays-missing / news corroboration true+false / mixed statuses / booked surfaced / no-announcements); `tests/_fakes.py` `FakeCollection` extended MINIMALLY with `$gte/$gt/$lte/$lt` + array-multikey equality (a query value matches if it is a MEMBER of a list field — for `{themes:"corporate_action"}` / `{entities_isins:isin}`), existing 76 tests unaffected; `tests/conftest.py` `fake_db` now patches `dividend_announcements` + `news_articles`. Unit 4 (frontend, `f5d8e2b` + `92a8974`): `lib/api.ts` `DividendDriftStatus`/`DividendAnnouncementDrift`/`DividendDriftRow` types + `getDividendDrift()` binding; `app/reconciliation/page.tsx` NEW read-only "Dividend drift" card after the status card (per-stock table: ex-date with-year, per-share, est. received, status badge recorded/not-recorded/pending; a missing-count summary banner; a `corporate_action`-news corroboration Newspaper icon with a shadcn `Tooltip` AND an always-visible legend line explaining it — reuses the existing `useQuery`+shadcn Card/Table/Badge+`lib/format` patterns, no new dep; copy states NOT a tax view). Verified on EC2: 87 pytest (76 + 11), hygiene PASSED, `/reconciliation/dividend-drift` returns the full matrix (44 announcements captured across 29 held names; 40 `missing_receipt`), first-run nudge = 4 pushes (3 individual + 1 summary) then 0 on re-run (rising-edge dedup holds), `/health` ok/ok, `~/deploy-ui.sh` build+lint clean + browser smoke. No new collection beyond `dividend_announcements`, no new dep, no `/tax` change. Prior position note: #50 + #57 SHIPPED 2026-07-26 (backend `4a9851c`); #57 was GATED on #50; both shipped. #56 (target / sell-price breach alerts) SHIPPED 2026-07-25 — backend `6f1aa57` + frontend `a2ffef1`, a DIRECT mirror of shipped #41 on the intraday write path (evolved the evaluator, no parallel path). NEW `price_service.evaluate_target_price_alerts(rows)` beside `evaluate_stop_loss_alerts`; `scripts/refresh_prices_intraday.py` calls it guarded on the SAME rows after the stop-loss block (new `target_alerts_fired` heartbeat key). Rising-edge fire when the latest intraday price crosses strictly ABOVE `target_price`; gated on `target_price` set AND `"target"` in `alert_on` (the Holding-model default token — NOT `"target_price"`); success-gated dedup; #67 Guard-B analog (first fire requires a prior tick at/BELOW `target_price`); ntfy-only `push_public("price")`; persists `Alert(alert_type="target_hit")` to `alerts_log`. NO `indexes.py` change (existing `isin_type_sent_desc` covers `target_hit`); NO `lib/api.ts` change (`Holding.target_price`/`alert_on` already present). Frontend `components/holding-stats.tsx` gains a read-only Target strip mirroring the stop-loss strip (editing stays in `notes-panel.tsx`). No hermetic unit test (mirrors #41 — `FakeCollection` lacks `$gt/$lte`; verified live on EC2 instead). #70 (#53-follow-up DATA ACTION) SHIPPED same commit — set the real demerger `acquired_date` from the ICICI order books: TMCV → 18-Oct-2023 (earliest TMPV parent lot; multi-lot 5/22/73 block → earliest/LTCG-favourable approximation), JIOFIN → 25-Jul-2022 (earliest RELIANCE buy; single-block, EXACT). Verified on EC2: hygiene PASSED, 66 pytest, functional fire→suppress→re-arm 1/0/1 + 2 ntfy pushes, Guard-B garbage-target = 0. GO-LIVE (#42) blocking set drops #56 + #70 → remaining: #57 (gated on #50), #65, #68, #69 (round-2 review LAST).**

**(Prior) Next item to start: the lowest-numbered OPEN blocking row was #56 (target / sell-price breach alerts — Bundle C, a clean mirror of shipped #41 on the intraday write path). #55 (LLM-authored hold-horizon on buy suggestions) SHIPPED 2026-07-25 — backend + frontend. Four LLM-authored flat keys added to the BUY dossier (`hold_horizon` short|medium|long, `hold_horizon_expected_move`, `hold_horizon_rationale`, `hold_horizon_review_trigger`) in `dossier_service._SYSTEM_PROMPT` + buy branch of `_parse_dossier` (coerce-and-default: off-list bucket → `medium`, blank prose → marker, keys deliberately NOT in `required` so a horizon glitch never nukes the narrative) + buy branch of `_empty_dossier`; `_clamp_sentence` helper added so horizon prose clamps on a sentence/word boundary (400-char budget) instead of a hard mid-word `[:300]` slice. NO change to `models/suggestion.py` / `scoring_service.py` / `explainability.py` / `routers/suggestions.py` — the dossier is a plain dict and `_serialize_run` already passes new keys through. Frontend `lib/api.ts` `SuggestionDossier` +4 optional fields; `components/suggestion-card.tsx` renders a Clock horizon badge (visible without expanding, buy-side only) + an "Expected hold horizon" section (Why the wait pays / Rationale / Re-check early if) using the shipped `!startsWith("(")` availability guard. ALSO shipped a #55-follow-up manual "Run now" trigger: NEW `POST /suggestions/run?direction=` (fire-and-forget 202 via BackgroundTasks, `notify=False`, no digest/outcomes) + `GET /suggestions/run/status?direction=` + NEW `suggestion_run_locks` collection (`_id==direction`, 900s TTL, fail-fast 409 on overlap — mirrors TD20 recompute_locks) delegating to the SAME `run_suggestions` entry point (no parallel pipeline); frontend "Run now" button on `/suggestions` with 10s status polling. New hermetic `tests/test_dossier_service.py`. Verified on EC2: 62 pytest, hygiene PASSED, live buy run authors valid horizons end-to-end (e.g. HINDZINC medium "~38% upside"), sell dossiers carry no horizon keys, 202/409 concurrency guard, `~/deploy-ui.sh` build+lint clean. GO-LIVE (#42) blocking set drops #55 → remaining: #56, #57, #65, #68, #70, #69.**

**(Prior) Next item to start: the lowest-numbered OPEN blocking row was #55 (LLM-authored hold-horizon on buy suggestions). #54 (add-a-holding entry point) SHIPPED 2026-07-25 — frontend-only; backend `POST /portfolio/holdings` confirmed already sufficient; `BuySheet` refactored to discriminated `holding`|`instrument` prop; NEW `add-holding-dialog.tsx`; `BuyPayload.isin?`; `app/page.tsx` mounts `<AddHoldingDialog />` in the Holdings section. GO-LIVE (#42) blocking set drops #54 → remaining: #55, #56, #57, #65, #68, #70, #69.**

**(Prior) Next item to start: the lowest-numbered OPEN blocking row was #54 (add-a-holding entry point). #53 (demerger holding-period inheritance, Bundle D partial) SHIPPED 2026-07-21 — backend-only; scoped to the one genuine defect (SPLIT/BONUS cost were already correct in the actual ledger, so untouched); Bundle D's #68 (corporate-action data-entry/automation front-end) remains OPEN. Phase 10.5 round-1 findings #54-#69 are being cleared per the "Phase 10.5 blocking-set chat bundles" map below: Bundle A (#58 + #59 + #62, frontend nav & shell) SHIPPED 2026-07-08 (frontend e3a9a44b3aaf1597edeeca7500779c0eb2de3361); #66 + #67 SHIPPED 2026-07-08 (backend beaadf70); #63 + #64 SHIPPED 2026-07-21 (backend `2792cefa`, frontend `3cde0d39`); #53 SHIPPED 2026-07-21 (backend, the FIFO acquired_date change). GO LIVE #42 stays gated on the remaining blocking set (#54, #55, #56, #57, #65, #68, #70) + the #69 round-2 review. (#70 is the #53-follow-up DATA ACTION: set the real demerger `acquired_date` before the #42 import — exact steps in the #70 row.)** Phase 10 is now fully SHIPPED (#39 + #40 + #41). #41 (wire `holdings.stop_loss` — intraday stop-loss breach alert + `alerts_log` first writer + read-only stop-loss strip on the holding drill-down) SHIPPED 2026-07-03 (Chat C, backend `eb964cc8` + frontend `a4b27bd7`, single logical unit) — closes TD6. #40 (realized-P&L UI hide) SHIPPED 2026-07-03 (Chat C, frontend-only, `16fab5ae`). Chat 9 (Phase 10 / #39 F11 capital-gains pack) SHIPPED 2026-07-01 (backend `32088c9`, frontend `747ae4f`) — Chat 9 COMPLETE. Chat B (Phase 9 pre-GO-LIVE hygiene sweep; bundle #30, #31, #32, #33, #36, #37, #38) shipped ALL its rows — so Chat B / Phase 9 is COMPLETE.

Phase 1 + Phase 2 + Phase 3 + Phase 4 + Phase 5 + Phase 6 + Phase 7 + Phase 8 + Phase 9 are now fully SHIPPED. Phase 8's last item #29 (Chat 8 / F13 watchlist) SHIPPED 2026-06-15. Chat B (Phase 9) has shipped ALL its rows — #30, #31, #32 (2026-06-15), #33 (2026-06-26), #36, #37, #38 (2026-07-01) — closing Phase 9. Phase 10 is now fully SHIPPED (#39 2026-07-01; #40 + #41 2026-07-03 Chat C). The remaining OPEN work is the NEW Phase 10.5 (USER ACCEPTANCE REVIEW, #52), Phase 11 (GO LIVE), Phase 12 (deferred TDs), and the NEW-ITEMS rows (#46 scheduler, #50 + #51 Chat-6 data bugs). #53 (demerger holding-period inheritance at the FIFO source) SHIPPED 2026-07-21 — scoped to the one genuine defect; SPLIT/BONUS cost were already correct.

**Chat 9 (#39 — F11 capital-gains pack) — closed 2026-07-01.** Standalone large feature (backend + frontend), kept one-per-chat by design. A design proposal was approved before any code (endpoint contract; lot math; service shape; frontend page). Built + verified in two units, each its own commit, then the doc commit:
- **Unit 1 — backend (`32088c938ecdfb9f58d40783f9ecf6da45819fe2`):** NEW `app/services/tax_service.py` — `compute_capital_gains(fy=None)` returns `{fy, fy_start, fy_end, summary{stcg,ltcg,total each realized_gain/proceeds/cost/lot_count}, lots[]}`; each lot row = `isin, symbol, name, buy_date, sell_date, quantity, buy_cost, sell_proceeds, gain, holding_period_days, gain_type`. READ-ONLY on Phase 1: it replays each ISIN's ledger through `holdings_service._fifo_replay` — the SINGLE FIFO source of truth (NO parallel FIFO path) — which was evolved to additionally emit a per-disposal `_realized_lots` list (buy/sell `trade_date` + fee-normalized per-share cost/proceeds captured in the SELL branch). `_recompute_holding_impl` pops `_realized_lots` off `computed` so it never lands on the `extra=forbid` holdings doc (the 7 `test_fifo_replay` tests assert per-key, not exact-dict, so the additive return key is safe). FY = Indian financial year 1 Apr → 31 Mar in IST; a disposal belongs to the FY of its SELL `trade_date` (IST). LTCG = listed equity held strictly >12 calendar months (via day-clamped `_add_months`), else STCG; `holding_period_days` still reported. §49(2C) demerger cost basis is honored automatically because the apportioned cost is already baked into the `manual_demerger` BUY rows in `transactions` — `cost_basis_adjustments` is a read-only audit surface and is deliberately NOT re-applied (would double-count). NEW `app/routers/tax.py` — `GET /tax/capital-gains`, `fy` optional (`Query` charset-guarded; omitted → `current_fy()` IST), 422 on malformed OR non-consecutive fy (`FyParseError`), router-local `_jsonable` (the watchlist/cost_basis precedent, money as strings). `main.py` includes `tax.router`. NEW `tests/test_tax_service.py` (FY parse, strict-12-month boundary, fee normalization, FY-window inclusive/exclusive edges, single-sell-spans-two-lots STCG+LTCG split, partial sell, demerger cost read-from-ledger, empty, default FY). Verified on EC2: `uv run python -m pytest` => 49 passed (28 existing + 21 new), `check_datetime_hygiene` PASSED, `/tax/capital-gains?fy=2025-26` shape correct, default fy → `2026-27`, `2025-27` + `2025` → 422, route on OpenAPI.
- **Unit 2 — frontend (`747ae4f29e0e0074b21eb3f34a3114a769e32752`):** NEW `app/tax/page.tsx` at `/tax` — client page, FY selector (recent FYs derived client-side in IST, default = current FY), STCG/LTCG/Total summary cards, and a per-lot disposals table with an STCG/LTCG badge; printable (`print:` variants) mirroring the `/cost-basis` shell; reuses `Card/Table/Select/Badge/Skeleton` + `inr/inrSigned/colorForChange/dateShort/dateTime` (no new npm/shadcn dep). `lib/api.ts`: `CapitalGainsLot/CapitalGainsBucket/CapitalGainsSummary/CapitalGainsResponse` types + `getCapitalGains(fy?)` (query key `["tax","capital-gains",fy]`). `app/page.tsx`: Tax nav link (Receipt icon) after Audit. Verified on EC2 via `~/deploy-ui.sh` (build + lint clean) + `/tax` → 200 rendering "Capital Gains".

Known limitation filed as NEW-ITEMS #53: because #39 is read-only on Phase-1 FIFO, buy_date/cost for BONUS and DEMERGER lots reflect the ledger encoding (bonus diluted in place keeping the original trade_date; demerger as manual BUY rows), not the strict IT-Act treatment (bonus = zero cost + holding period from allotment; demerger = inherit original holding period). Fixing it requires Phase-1 FIFO changes, out of #39 scope.

Per the standing rule, the next item (Chat C — #40 + #41) continues in a fresh chat to keep context clean.

**Chat B (#30 + #31 + #32 + #33 + #36 + #37 + #38 — Phase 9 pre-GO-LIVE hygiene sweep) — COMPLETE, opened 2026-06-15, closed 2026-07-01.** Bundle = #30, #31, #32, #33, #36, #37, #38; each row shipped individually with the pointer advanced per row — ALL SHIPPED. Backend code HEAD at Chat B close is `8127c6f` (the #38 JSON-logging commit; the Chat B doc commit advances it further). Every aware-now site was re-read at HEAD before patching; the master_todo line numbers in the Phase 9 rows are a stale map, not ground truth.
- **#30 (P2-1) — `datetime.utcnow()` sweep.** Replaced 11 call sites with `utcnow()` from `app.models._common` across 5 files: the row's listed 9 (`routers/portfolio.py`, `services/scoring_service.py` x3, `services/dossier_service.py` x2, `services/fundamentals_service.py` x3) PLUS 2 stragglers in `services/conversation_service._format_position_block` (`days_held` + `days_to_earn`) that postdate the row's hardcoded list (added Chat 6). `utcnow()` returns `datetime.now(timezone.utc).replace(tzinfo=None)` (naive UTC) so every swap is byte-identical. Added the `utcnow` import to portfolio/scoring/dossier; fundamentals + conversation_service already imported it. Verified on EC2: `grep -rn "datetime.utcnow(" app/ scripts/` -> zero; the F3 holding chat ran both swapped `conversation_service` calls and returned a real LTCG answer.
- **#31 (P2-8) — tz-aware Mongo-write sweep + CI lint guard (two legs, one chat).** Leg 1: a tree-wide audit reclassified every `datetime.now(timezone.utc)` as Mongo-WRITE (swap to `utcnow()`) vs in-memory compare (keep aware + annotate). 15 write/query sites swept across `reconciliation` (incl. the `take_manual_snapshot:213` site a first-pass `replace_all` missed because the two `taken_at` writes were not byte-identical), `news_classifier`, `transactions_audit_service`, `cron_heartbeat_service`, `instrument_service` x3, `price_service` (`_df_to_rows` fetched_at, `fetch_intraday_quotes` + `bulk_get_latest_intraday` now_utc — the latter a second `replace_all` miss caught by the guard), `seed_nifty100`, `import_orderbooks`, `add_manual_transactions`, `seed_cost_basis_adjustments`. In-memory aware sites (subtraction vs aware-coerced reads, `astimezone(IST)`, JSON `as_of` responses, date-string computes) were LEFT aware — swapping them would TypeError. Leg 2: NEW `scripts/check_datetime_hygiene.py` — a tokenize-based guard that bans stdlib `utcnow` outright and fails any aware-`now()` lacking a trailing `# tz-ok: <reason>` annotation; comment-aware, self-skipping, fragment-built needles (won't trip the existing greps). 19 in-memory sites annotated `# tz-ok:`. The guard was first written line-based and FAILED 5 sites where the formatter wrapped a long statement and parked the `# tz-ok` comment on the closing-bracket line; rewritten to group by logical line (tokenize, NEWLINE-delimited) so the annotation matches wherever the formatter parks it. Verified on EC2: guard PASSED (exit 0); negative controls (a planted real `datetime.utcnow()` call AND a planted unannotated aware-now) both FAILED (exit 1) then cleared on revert. Run: `uv run python -m scripts.check_datetime_hygiene`. The `app.models._common.utcnow()` storage-invariant convention is now machine-enforced.
- **#32 (P3-2) — Python ceiling.** Pinned `requires-python = ">=3.12,<3.14"` in `pyproject.toml` (lower bound unchanged) + `uv lock` relock (no dependency version churn). Verified on EC2: `uv sync` clean, interpreter `Python 3.12.3` in range, `/health` ok/ok, hygiene guard still PASSED.
- **#33 (Review note) — pytest harness.** Stood up a hermetic, zero-dependency `tests/*` harness — 28 tests across the 6 review-mandated targets. NEW `tests/_fakes.py`: an in-memory `FakeCollection` implementing only the operators the SUTs use (`find`/`find_one` with `sort`/`skip`/`limit`, `insert_one`, `update_one` with `$set`/`$setOnInsert`/upsert, `delete_one`/`delete_many`, `count_documents`; `$ne`/`$in`/`$nin`/`$exists`/`$or`/`$and`) + `tx()` (Mongo-shaped transaction dict) + `oid()` factories. NEW `tests/conftest.py`: a `fake_db` fixture that monkeypatches every `Collections.*` accessor the targets touch (transactions/holdings/instruments/recompute_locks/reconciliation_snapshots/user_profile/monitored_stocks/monitored_stocks_audit/suggestion_outcomes/suggestion_runs) to a `FakeCollection`. Six test files: `test_fifo_replay.py` (7 — single buy, partial sell realizes P&L, full exit, buy fees fold into invested, dividend accrues without touching position, SPLIT scales qty/price, BONUS dilutes cost), `test_validate_replay.py` (5 — valid buy-then-sell, oversell rejected, chronology-by-trade_date not input-order, split enables post-split sell, deleted txns skipped), `test_preview_sell.py` (6 — partial/full/oversell/non-positive/no-txn/split-aware), `test_recompute_holding.py` (2 — full DB idempotency: run twice -> stable aggregates + preserved `created_at` + exactly one active doc; full-exit soft-delete returns None), `test_submit_feedback.py` (3 — audit-written-before-apply ordering via call-order capture, previous_status captured, action->status mapping), `test_take_auto_snapshot.py` (5 — no-manual-baseline skips drift block, no-drift on match, drift>threshold fires alert, rising-edge dedupe suppresses repeat, exactly-at-threshold is strict-`>` not drift). DB-coupled targets monkeypatch `fetch_metadata`/`_get_our_numbers`/`_send_auto_drift_alert`/`log_change` so nothing hits Atlas, yfinance, or ntfy. Every fixture grounded against the real signatures re-read at HEAD before construction. No new deps (pytest>=8.3 already in the dev group; `[tool.pytest.ini_options] pythonpath=["."]` already present). Verified on EC2: `uv run python -m pytest tests/ -v` => 28 passed in ~1.1s (Python 3.12.3, pytest-9.0.3); `uv run python -m scripts.check_datetime_hygiene` still PASSED (the test files use only fixed datetimes — no `utcnow()`/aware-now). Backend + test only; frontend unchanged.

- **#36 (Ops gap) — `POST /admin/recompute/{isin}` (Tailscale-only).** NEW `app/routers/admin.py` registered in `app/main.py` — the HTTP replacement for SSH-shell recovery of a stuck holding (the TD19/#7 fallback). Delegates to the existing `holdings_service.recompute_holding` (per-ISIN advisory-locked, TD20) — NO parallel recompute path. ISIN `pattern=r"^[A-Z0-9]{12}$"` (422 at the boundary); returns `{status: "recomputed", isin, holding}` on an active result, `{status: "no_active_holding", isin, holding: null, message}` when `recompute_holding` returns None (full exit / no transactions — 200, not 404), 409 on TD20 lock contention (`RuntimeError`), 500 on any other failure. Router-local `_jsonable` serializer (the watchlist precedent — no reach into another router's helper). "Tailscale-only" = the existing app-wide perimeter (no in-app auth gate added). No new test file — the thin HTTP wiring delegates to `recompute_holding`, already regression-guarded by `tests/test_recompute_holding.py` (#33). Verified on EC2 against localhost:8000: active ISIN -> `recomputed` + idempotent re-run (byte-identical quantity/avg_cost/invested/realized_pnl), no-transaction ISIN -> `no_active_holding`/`null`, bad-shape ISIN -> 422, `/openapi.json` shows the route, `uv run python -m pytest` => 28 passed + `uv run python -m scripts.check_datetime_hygiene` still PASSED (admin.py constructs no datetimes). Commit on `1ef0ead`.
- **#37 (Ops gap) — Atlas backup → fresh-DB restore rehearsal.** OPS + DOC only, no `app/` code, no code commit beyond the doc commit (backend code HEAD stays `1ef0ead`). Rehearsed on EC2 against prod Atlas the surgical per-collection backup→restore path for the three GO-LIVE-preserved collections (`monitored_stocks`, `suggestion_outcomes`, `digest_deliveries`) — the set #42 must keep when it wipes `transactions`/`transactions_staging`/`holdings`. Lesson learned in the drill: this `mongodump` (100.17.0) REJECTS `--nsInclude` (that flag is `mongorestore`-side) — the working dump form is per-collection `mongodump --uri "$MONGODB_URI" --db=portfolio --collection=<name> --out <dir>` (×3). Restore into a FRESH scratch DB via `mongorestore --uri "$MONGODB_URI" --nsFrom 'portfolio.*' --nsTo 'portfolio_restore_test.*' --drop <dir>` — the Atlas user `portfolio_app` CAN create a second DB (no Unauthorized), so the cross-DB remap path stands and the fallback was not needed. Verified restored counts matched prod (`monitored_stocks` 2, `suggestion_outcomes` 155, `digest_deliveries` 10), then dropped the scratch DB + local dump so nothing lingers and prod `portfolio` was never written. Documented the exact dump/restore/verify/cleanup runbook (plus the real-recovery `--nsInclude` variant) in `Project_State.md` Section 4 ("Backup → fresh-DB restore rehearsal").

- **#38 (Ops gap) — JSON-structured logging.** Replaced the `logging.basicConfig` text formatter in `app/main.py` with a stdlib `JsonLogFormatter` (`logging.Formatter` subclass — no new dependency, consistent with the #32 `>=3.12,<3.14` pin) + `_configure_logging()` that installs one JSON `StreamHandler` on the root logger AND `uvicorn`/`uvicorn.error`/`uvicorn.access` (`handlers.clear()` for idempotency; `propagate=False` so uvicorn lines emit exactly once). Each record serializes to one single-line JSON object on stdout/journald: `timestamp` (UTC ISO-8601 ms+Z from `record.created` via `datetime.fromtimestamp(..., tz=timezone.utc)` — NOT `now()`/`utcnow()`, so the tokenize guard has nothing to flag), `level`, `logger`, `message`, `module`, `func`, `line`; `traceback` when `exc_info` is set; `stack` when `stack_info` is set; any caller-supplied `extra={...}` keys merged at top level (reserved `LogRecord` attrs filtered out). `_configure_logging()` runs at import time and wins because uvicorn configures its own logging before importing `app.main`. No new file, no dependency, no `uv.lock` change. Verified on EC2 against localhost:8000: `uv run python -m scripts.check_datetime_hygiene` PASSED, `uv run python -m pytest` => 28 passed, `journalctl` shows valid single-line JSON for an app record (`app.main` "API ready"), a `uvicorn.error` startup record, and a `uvicorn.access` request record, with no double-logging (3 `/health` calls -> exactly 3 `uvicorn.access` lines). Commit on `8127c6f`.

#38 was the LAST Chat B row — shipping it CLOSES Chat B / Phase 9. No new tech-debt or follow-up items were filed.

**Chat 8 (#29 — F13 watchlist) — closed 2026-06-15.** Standalone large feature, kept one-per-chat by design (it multiplies data volume so it was gated LAST among the Phase-8 features, after Phase 4 storage hygiene + Phase 6 Tavily race, both COMPLETE). A design proposal was approved before any code (the universe set-math change + how `get_excluded_isins` interacts with watchlist membership; the `/watchlist` CRUD contract + reuse of `monitored_stocks` + the partial-unique-index implications; how the two crons pick up watchlist ISINs and the Tavily-quota blast-radius; where the frontend surface lives). Built + verified in four units, each its own commit, plus one CORS fix surfaced during browser testing:
- **Unit 1 — write-model + universe set-math (`models/monitored_stock.py` + `services/suggestion_engine.py`, commit `34ff906d`):** added `MonitoredStockWatchlistPatch` (mirrors `MonitoredStockFeedbackPatch` — `ConfigDict(extra="forbid")`, `status` pinned to `"watchlist"`, carries only the mutable watchlist fields, `exclude_none` on dump so a re-PUT never wipes unspecified fields). Evolved `build_universe()` from `instruments.find({"in_nifty100": True})` to **NIFTY 100 ∪ watchlist**, deduped by ISIN, resolving watchlist ISINs outside NIFTY 100 from `instruments` by ISIN (warns on any not found); added `get_watchlist_isins()` as the single source of truth for membership (`monitored_stocks` `status=="watchlist"`). Held stays filtered DOWNSTREAM by `filter_universe` exactly as before (a held+watchlist ISIN is not a buy candidate), so net candidates = `(NIFTY 100 ∪ watchlist) − held − rejected − acted`. **No change to `get_excluded_isins`** — it scans only `rejected`/`tracking`, so a watchlist row is structurally never excluded; flipping `rejected→watchlist` auto-un-suppresses. **No change to `indexes.py`** — `monitored_stocks` is one-doc-per-ISIN (upsert-on-`{isin}`), so a watchlist state and a tracking state can't be separate rows; the `isin_unique_active` partial index (`partialFilterExpression status:"tracking"`) is untouched and only constrains tracking docs. Verified on EC2: write-model dumps clean (Decimals coerced, no nulls); universe 100→101 on a temp non-NIFTY100 watchlist insert, restores to 100 on cleanup.
- **Unit 2 — `/watchlist` CRUD (`routers/watchlist.py` NEW + `services/monitored_stocks_audit_service.py` + `main.py`, commit `a250d00189`):** `GET /watchlist` (list, price-enriched via `bulk_get_latest_prices`), `GET /watchlist/{isin}`, `PUT /watchlist/{isin}` (idempotent create-or-update), `DELETE /watchlist/{isin}` (hard-delete, watchlist-only). Reuses `monitored_stocks` with `status="watchlist"` (no new collection). PUT flips status to watchlist (un-excludes a previously rejected/acted ISIN), 404s if the ISIN isn't a known instrument, seeds identity (`symbol/name/exchange/sector/industry`) from the instrument doc on insert, and runs the `Money` patch through `_convert_decimals_to_decimal128` before Mongo. **Write-before-apply audit (F10)** via `log_change` on every mutation — the audit-action `Literal` was WIDENED (not forked) to `AuditAction` (adds `watchlist_add/watchlist_update/watchlist_remove`) so the watchlist path reuses the SAME `monitored_stocks_audit` collection. DELETE 404s on a non-watchlist doc so a feedback row is never nuked. Verified on EC2 (12 checks): `watchlist_add`→`watchlist_update` audit trail, `target_buy_price` survives a re-PUT, un-exclude wiring (`in_universe: True`), and the 404/422 guards all fire.
- **Unit 3 — cron data-volume coverage (`scripts/refresh_fundamentals.py` + `scripts/fetch_news_for_universe.py`, commit `9857570b`):** the default weekly fundamentals universe (`get_nifty100_union_holdings`) now folds in watchlist ISINs via new `get_watchlist_instruments()`; the news fetch universe (both `--include-held` and default branches) folds them in via new `get_watchlist_for_news()`, deduped by ISIN. Both reuse `suggestion_engine.get_watchlist_isins()` (no third copy of the `status=="watchlist"` filter). **This is the F13 data-volume multiplier:** each watchlist name now pulls weekly fundamentals + earnings + daily news + consumes one Tavily call/run against the daily quota (TD33). Tavily blast-radius is a DOCUMENTED soft guardrail (no hard cap on watchlist size — consistent with the calls-only TD33 ceiling, which degrades safely via the atomic quota guard if exceeded). Verified on EC2: union size 117 (NIFTY100 + held + 1), news fold-in present; end-to-end `uv run python -m scripts.refresh_fundamentals --symbols 20MICRONS` → `Succeeded: 1/1`, `18/20 fields present`.
- **Unit 4 — frontend watchlist surface (`lib/api.ts` + `app/watchlist/page.tsx` NEW + `app/page.tsx`, frontend commit `58bf6369`):** added `WatchlistEntry` + `WatchlistUpsertPayload` types + `getWatchlist`/`getWatchlistEntry`/`upsertWatchlist`/`deleteWatchlist` bindings; widened `MonitoredStocksAuditEntry.action` to `FeedbackAction | WatchlistAuditAction` and `new_status` to include `"removed"`. New `/watchlist` page reuses the `/tags` page shell + `components/ui/table` primitives (watchlist rows are `monitored_stocks`, not `Holding[]`, so a purpose table — not `HoldingsTable`); add-control reuses `api.searchInstruments` (symbol→ISIN) and PUTs; per-row Remove DELETEs; mutations use `refetchQueries` + toast + `ApiError.detail` (the buy-sheet convention); target-buy cell highlights when current price is at/below target. Added a Watchlist nav link (Eye icon) to the dashboard header. Per the §21 anti-standalone-sprawl stance, reused existing components where they fit.
- **CORS fix (surfaced during browser testing, `main.py`, commit `67704025`):** add-to-watchlist failed from the dashboard with a CORS preflight error — `PUT` was missing from `CORSMiddleware allow_methods` (`["GET","POST","PATCH","DELETE","OPTIONS"]`). GET/DELETE worked (so list/search/remove worked) but the `PUT /watchlist/{isin}` upsert preflight `OPTIONS` was rejected. curl from inside the box doesn't do CORS preflight, so Unit 2's curl tests passed — it only surfaced from the browser. Fixed by adding `"PUT"` to the explicit method list (no switch to `["*"]`). Verified on EC2 with a simulated preflight `OPTIONS` (now 200 + advertises PUT) + a real PUT (200).

End-to-end verified on EC2 against localhost:8000 and from the browser over Tailscale (add INFY → success toast, row appears with price; remove → disappears). No new tech-debt or follow-up items were filed.

Per the standing rule, the next item (#30, Chat B) continues in a fresh chat to keep context clean.

**Chat 7 (#28 — F12 risk-summary + F15 by-tag) — closed 2026-06-15.** Standalone large feature, built and verified in two backend units + two frontend units, each its own commit. A design proposal was approved before any code (concentration by holding + sector with two-tier alerts; by-tag contract reading `holdings.tags`; both read-only over the existing Phase-1 holdings/price surfaces; frontend risk card on the dashboard + a dedicated `/tags` page):
- **Unit 1 — F12 backend (`app/services/portfolio_service.py` + `app/routers/portfolio.py`, commit `97041621`):** extracted the per-holding annotation loop + running totals out of `compute_summary` into a shared private helper `_annotate_holdings(holdings, latest_prices) -> (annotated, accum)`, then refactored `compute_summary` to call it — behaviour-preserving, gated by a `/portfolio/summary` curl-diff (minus the volatile `as_of`) that printed `OK: /summary unchanged`. Added `compute_risk_summary(holdings, latest_prices)` on the SAME helper (no parallel aggregation), returning `concentration_by_holding` (EVERY priced holding, desc by % — not just top 5), `concentration_by_sector`, and an `alerts` array. Alerts are two-tier from four module constants (the `TOP_MOVERS_LIMIT` pattern): `SINGLE_HOLDING_CONCENTRATION_WARN_PCT=10.0` / `HIGH_PCT=20.0`, `SECTOR_CONCENTRATION_WARN_PCT=30.0` / `HIGH_PCT=50.0`, plus a low-severity `stale_price` data-quality note (reuses the existing `price_stale`/missing-price flag — holdings with no price are excluded from the % denominator, so their weight is understated). New `GET /portfolio/risk-summary` mirrors the `/summary` router skeleton (`find({"deleted_at": None})` → `bulk_get_latest_prices` → compute → `_serialize`). Verified on EC2: 2 warn alerts (GAEL 13.67%, LT 10.02%), 28 holdings, 7 sectors; the risk-summary top concentration is byte-identical to `/summary`'s `concentration[0]`.
- **Unit 2 — F15 backend (`app/routers/portfolio.py`, commit `803e6610`):** `GET /portfolio/by-tag?tag=X` — `tag` is a REQUIRED query param (`Query(..., min_length=1)`; missing/empty → 422). Filters `holdings().find({"deleted_at": None, "tags": tag})` (exact, case-sensitive Mongo array-membership), annotates each row via the SAME path as `holdings.list_holdings` (`bulk_get_latest_prices` + `bulk_get_previous_closes` + `annotate_with_current_price`) so rows render in the existing `HoldingsTable` unchanged, plus a tag-scoped `totals` block (count, invested, current_value, unrealized_pnl, unrealized_pnl_pct). Unknown tag → empty holdings + zeroed totals (200, not 404). Imports the existing `_to_dec` from `portfolio_service` for the totals math (no parallel converter). Verified on EC2: tags discovered (`long-term`, `retail`); `retail` → TATASTEEL, count 1, matching the cross-check against the full holdings list; by-tag holding shape == `/portfolio/holdings` shape; unknown tag → zeroed 200; missing + empty tag → 422.
- **Unit 3 — F12 frontend (`lib/api.ts` + new `components/risk-summary-card.tsx` + `app/page.tsx`, frontend commit):** added `RiskSummary` / `RiskConcentrationHolding` / `RiskConcentrationSector` / `RiskAlert` types + `HoldingsByTag` / `TagTotals` types + `getRiskSummary` and `getHoldingsByTag` wrappers to the hand-typed `lib/api.ts` (the existing `ConcentrationItem` / `SectorBucket` did NOT match the risk-summary shapes, so new types — not reuse). New `RiskSummaryCard` (shadcn Card/Badge/Skeleton, reuses `inr`/`pct` from `lib/format`, severity-colored alert rows, top-5 holdings by weight linking to `/holdings/{isin}`, sector weights). Mounted on the dashboard as its own full-width section below the Sector/Top-Movers grid via an independent `useQuery(["dashboard","risk"])` so a risk-endpoint failure doesn't block the main dashboard. Added a "Tags" nav link to the header.
- **Unit 4 — F15 frontend (new `app/tags/page.tsx`, frontend commit):** dedicated `/tags` route that derives the tag universe from `getHoldings().tags`, renders a tag-pill selector, and on selection calls `getHoldingsByTag(tag)` to show a tag-scoped totals row + the existing `<HoldingsTable>` (reused wholesale). `useQuery(["tags","by-tag",tag])`, `enabled` only when a tag is picked. Verified on EC2: `npm run build` + lint clean via `~/deploy-ui.sh`; the two consumed endpoints alive; dashboard risk card + `/tags` page render correctly.

Both endpoints are READ-ONLY over the existing Phase-1 holdings/price surfaces; no new collections, no new indexes, no new npm dependency. No new tech-debt or follow-up items were filed.

Per the standing rule, the next item (#29, Chat 8) continued in a fresh chat to keep context clean.

**Chat A (Ops & alerting quick-wins) — closed 2026-06-14.** Bundled seven small, independent, backend/doc-only rows and cleared all of them, each marked SHIPPED individually:
- **#34 (Ops gap, `app/main.py`):** `GET /health` was hardcoding `{"status":"ok"}` + HTTP 200 even when the Mongo `ping()` failed (it only flipped the `mongo` field). Now returns 503 + `{"status":"degraded","mongo":"fail"}` on ping failure, 200 + `{"status":"ok","mongo":"ok"}` on success — aligns code with the README §10 claim. yfinance deliberately NOT probed on the hot path (a Yahoo throttle would cause false 503s; price-source health stays in the `refresh_prices*` cron heartbeats). Commit `bd52c6b`.
- **#35 (Ops gap, `scripts/refresh_prices_intraday.py`):** `insert_intraday_quotes` wrapped in try/except — on failure (market is open by construction once `fetch_intraday_quotes` returns rows) it fires a GUARDED `push_public("errors", ...)` then re-raises so `cron_run` still records the failure heartbeat. Commit `bd52c6b`.
- **#25 (P2-7, `app/services/reconciliation.py`):** `take_auto_snapshot` was silent to Mongo only (notes string). Now fires `push_public("price", ...)` when invested drift vs the last manual snapshot exceeds `DRIFT_ALERT_THRESHOLD_INVESTED` (₹1,000). ntfy ONLY (the manual `_send_drift_alerts` keeps its dual ntfy+email transport; the daily auto cron would make email noise). Rising-edge dedupe: alert fires only when this snapshot has drift AND the most recent prior auto snapshot did not, so a standing divergence does not re-push every day; a fresh manual snapshot resets the baseline and re-arms. Current-value drift intentionally NOT alerted on the auto path (multi-day gap dominated by live-price movement). `drift_invested`/`has_drift`/`alerts_sent` recorded on the auto snapshot (the model already declares them optional; route returns `_serialize(dict)` with no `response_model`, so no shape break). New `_send_auto_drift_alert` mirrors only the ntfy half of the existing alerting — evolved in place, not a parallel alerter. Commit `1340396`.
- **#49 (TD40, `app/services/cron_heartbeat_service.py`):** the idle `weekly_suggestions_sell` CronSpec carried `expected_weekdays={6}`, so `cron_health_check` fired a false `MISSING: weekly_suggestions_sell` every Sunday even though the umbrella `weekly_suggestions --direction=both` cron covers both directions. Set `expected_weekdays=set()` so `is_expected_today()` is always False for the idle entry and the MISSING/FAILED checks skip it; the spec stays in the registry/dashboard as `expected=False`. NO change to the #23/TD38-hardened `_persist` / `_append_fallback` / `count_today_heartbeats_from_fallback` paths (re-read at HEAD; left untouched). Commit `6032b64`.
- **#26 (P2-6 / TD1, `app/routers/suggestions.py`):** the literal "add `payload.direction`" would have crashed — `SuggestionFeedback` had only `action` + `note` with `extra="forbid"`, so there was no `payload.direction`. Added `direction: Literal["buy","sell"] = "buy"` (default keeps the current frontend, which sends only `{action, note}`, working — a required field would 422 every existing submit). The relabel query filters by direction, using the repo's existing back-compat guard `{"$or":[{"direction":"buy"},{"direction":{"$exists":False}}]}` for the buy branch (matching `get_latest_run` / `compute_system_performance`) and a bare `{"direction":"sell"}` for sell. Does NOT close TD1/#43 — cross-direction relabel policy stays DEFERRED. Commit `6032b64`.
- **#47 (TD22, `scripts/track_suggestion_outcomes.py`):** investigation-first. The daily 19:45 IST cron failed every weekday (0 success / 1 failure) → fired the 21:00 IST health email daily. Root cause (reproduced live at `6032b64`): `snapshot_open_outcomes()` returns its count under `active_outcomes` (renamed in Commit A.5 when selection broadened from "open" to all non-expired), but the cron's `main()` still read `stats["open_outcomes"]` in both `hb.metadata.update(...)` and the result print → `KeyError` on every run, raised inside the `cron_run` context so the heartbeat recorded a failure at line 27 before any result line flushed (explaining the empty `cron-outcomes.log` — the traceback went to stderr, not the log redirect). Fix: read `stats["active_outcomes"]` and rename the heartbeat metadata key + print label to match. Safe rename — the heartbeat never persisted `open_outcomes` (it always KeyError'd first), so nothing downstream depends on the old key; no schema/migration. Verified on EC2: pre-fix run reproduced the KeyError + failure heartbeat; post-fix run exits 0, prints "Active outcomes: 115", persists a `success` heartbeat with `active_outcomes` in metadata. Root cause was a deterministic single-file key drift — well within a quick fix, no STOP needed. Commit `4b638e6`.
- **#48 (TD36, DOC-ONLY, `README.md` + `docs/data_flow.md`):** corrected the stale "monthly" Tavily-quota wording to "daily (resets 00:00 UTC)". The code check (per the read-the-code rule) also surfaced two further doc bugs fixed in the same commit: data_flow said "resets at 00:00 IST on the 1st" (the code keys the quota on `date_utc` and `TavilyQuotaExceeded` states "Resets at 00:00 UTC"), and README documented a non-existent env var `TAVILY_MONTHLY_QUOTA` (the real one is `TAVILY_DAILY_CALL_LIMIT`, enforced in `tavily_client`, not `news_fetcher`). No `app/` or `scripts/` code touched. Project_State Sections 5/7/10/12/14 were already correct. Commit `fae6edf`.

Per the standing rule, the next item (#27, Chat 6) continued in a fresh chat to keep context clean.

**Chat 6 (#27 — F1 + F3 ad-hoc chat) — closed 2026-06-14.** Standalone large feature, built and verified in four units plus one latent-bug fix:
- **Unit 1 (data layer):** `app/models/conversation.py` evolved from placeholder to the real `Conversation(BaseDoc)` write model + added a `scope: Literal["suggestions","holding"]` discriminator (kept distinct from `intent` — surface vs question classification); added `(scope, created_at desc)` index `scope_created_desc`; added `instrument_service.lookup_by_isin(isin)` reverse lookup (ISIN -> instrument, NSE-preferred) to support researching not-yet-owned stocks. `Collections.conversations()` accessor and the four base conversations indexes already existed at HEAD; `init_db.py` needed no edit (calls `ensure_all_indexes()` generically). Commit `4403bb5` open base → Unit 1 commit.
- **Unit 2 (enrichment orchestrator):** `app/services/conversation_service.ensure_stock_context(isin)` — resolves identity, then ensures fundamentals (14d freshness), upcoming earnings, and recent classified news exist, fetching on demand via the existing `fundamentals_service` / `news_fetcher` / `news_classifier` (the same cron-path services). Freshness gates keep a warm turn external-call-free; `TavilyQuotaExceeded` + fetch failures degrade gracefully to cached data. Writes ONLY Phase-2 reference collections (fundamentals/earnings/news), never Phase-1 portfolio data.
- **Unit 3 (chat service + API):** context builders — buy-research framing for not-yet-owned names, position/tax overlay for held names (LTCG>365d / near-LTCG / weight, reimplemented from `_build_position_context_block` semantics since the dossier helper is `CandidateScore`-coupled), latest buy+sell run summary for F1 — a single structured Sonnet call returning a `{answer, intent}` JSON envelope (mirrors `dossier_service._generate_one`: lazy Anthropic import, `ANTHROPIC_MODEL_PRIMARY`, collect `block.text`, parse + one retry + graceful fallback), token/cost capture (Sonnet $3/$15 per MTok module constants -> `Money` cost_usd), and Conversation persistence (insert then re-read the doc so the router serializes POST responses and history rows through one path). New `app/routers/conversations.py`: `POST /chat/suggestions` (F1), `POST /chat/holdings/{isin}` (F3, ISIN `pattern=r"^[A-Z0-9]{12}$"`, 404 on unknown instrument), `GET /chat/history` (scope/isin filtered); registered in `app/main.py`. Reuses `dossier_service._to_float`/`_format_news_summaries`, `suggestion_engine`, `price_service` verbatim.
- **Unit 4 (frontend):** `lib/api.ts` chat + instrument-search types and wrappers (`chatSuggestions`, `chatHolding`, `getChatHistory`, `searchInstruments`); new `components/chat-panel.tsx` (transcript + composer + sentiment toggle + pending state + a self-contained markdown renderer, since the repo has no markdown dependency — keeps deploy a plain `npm run build`); new `components/stock-research-panel.tsx` (symbol search via the existing `/instruments/search` -> ChatPanel bound to `/chat/holdings/{isin}` for a not-yet-owned stock). Embedded on `/suggestions` (F1 + research panel) and `/holdings/[isin]` (F3). TanStack `refetchQueries` convention. Frontend HEAD `6093f63`.
- **Route-shadow fix (surfaced during F3 testing, `app/routers/instruments.py`):** the dynamic `/{exchange}/{symbol}` route was declared before the static `/search/{symbol_prefix}`, so `GET /instruments/search/INFY` was captured as `exchange=search, symbol=INFY` and 404'd — the search endpoint was unreachable over HTTP (latent since it was added; scripts call the service layer directly, so the chat research panel was its first HTTP caller). Reordered search-before-dynamic + added a NOTE so the ordering isn't reintroduced. Filed under #27 (no separate row). Backend HEAD `5e787c9`.

End-to-end verified on EC2 against localhost:8000: F1 returns the real weekly run with cited ISINs (HINDZINC/JSWSTEEL/VBL); F3 held (Reliance) works the +6.3% gain / LTCG / 1.6% weight / July-17 earnings overlay; F3 not-held (HDFC Bank, INFY) gives buy-research analysis with fundamentals + classified news; unknown ISIN -> 404; bad ISIN shape -> 422; history newest-first + scope/isin filtered; `model_used=claude-sonnet-4-5`, token counts + `cost_usd` populated.

Two issues surfaced during #27 testing, filed as new rows (NOT fixed this chat): **#50** news entity mis-tagging (the HDFCBANK chat returned TCS/Kenya articles — wrong `entities_isins` in `news_articles`, upstream of chat in `news_fetcher`/`news_classifier`) and **#51** `dividend_yield` ×100 formatting shared by `dossier_service._fmt_pct` and the chat formatter (Reliance showed "46%"; verified the chat formatter is byte-consistent with the dossier `_fmt_pct`, so it is a pre-existing app-wide yfinance unit inconsistency, not new to #27).

Items completed since this file was created:
- #1 (TD14) — SHIPPED 2026-06-02 (Chat 5.9)
- #2 (TD10) — SHIPPED 2026-06-02 (Chat 5.9)
- #3 (TD15) — SHIPPED 2026-06-02 (Chat 5.9)
- #4 (TD16) — SHIPPED 2026-06-06 (Chat 5.10)
- #6 (TD18) — SHIPPED 2026-06-06 (Chat 5.10)
- #5 (TD17) — SHIPPED 2026-06-06 (Chat 5.10)
- #7 (TD19) — SHIPPED 2026-06-06 (Chat 5.10)
- #8 (TD20) — SHIPPED 2026-06-06 (Chat 5.10)
- #9 (TD23) — SHIPPED 2026-06-08 (Chat 5.11)
- #10 (TD24) — SHIPPED 2026-06-08 (Chat 5.11)
- #11 (TD25) — SHIPPED 2026-06-08 (Chat 5.11)
- #12 (TD26) — SHIPPED 2026-06-08 (Chat 5.12)
- #13 (TD27) — SHIPPED 2026-06-08 (Chat 5.12)
- #14 (TD28) — SHIPPED 2026-06-08 (Chat 5.13)
- #15 (TD29) — SHIPPED 2026-06-08 (Chat 5.13)
- #16 (TD30) — SHIPPED 2026-06-08 (Chat 5.13)
- #17 (TD31) — SHIPPED 2026-06-08 (Chat 5.13)
- #18 (TD32) — SHIPPED 2026-06-08 (Chat 5.13)
- #19 (TD33) — SHIPPED 2026-06-09 (Chat 5.14)
- #20 (TD34) — SHIPPED 2026-06-12 (Chat 5.15)
- #21 (TD35) — SHIPPED 2026-06-12 (Chat 5.16)
- #22 (TD37) — SHIPPED 2026-06-12 (Chat 5.17)
- #23 (TD38) — SHIPPED 2026-06-12 (Chat 5.18)
- #24 (TD39) — SHIPPED 2026-06-14 (Chat 5.19)
- #34 (Ops gap) — SHIPPED 2026-06-14 (Chat A) — commit `bd52c6b`
- #35 (Ops gap) — SHIPPED 2026-06-14 (Chat A) — commit `bd52c6b`
- #25 (P2-7) — SHIPPED 2026-06-14 (Chat A) — commit `1340396`
- #49 (TD40) — SHIPPED 2026-06-14 (Chat A) — commit `6032b64`
- #26 (P2-6 / TD1) — SHIPPED 2026-06-14 (Chat A) — commit `6032b64`
- #47 (TD22) — SHIPPED 2026-06-14 (Chat A) — commit `4b638e6`
- #48 (TD36) — SHIPPED 2026-06-14 (Chat A) — commit `fae6edf`
- #27 (Chat 6 / F1 + F3) — SHIPPED 2026-06-14 (Chat 6) — backend `5e787c9`, frontend `6093f63`
- #28 (Chat 7 / F12 + F15) — SHIPPED 2026-06-15 (Chat 7) — backend Unit 1 `97041621`, Unit 2 `803e6610`; frontend `e14d6a75`
- #29 (Chat 8 / F13 watchlist) — SHIPPED 2026-06-15 (Chat 8) — backend Unit 1 `34ff906d`, Unit 2 `a250d001`, Unit 3 `9857570b`, CORS fix `67704025`; frontend `58bf6369`
- #30 (P2-1) — SHIPPED 2026-06-15 (Chat B) — 11 `datetime.utcnow()`->`utcnow()` sites across 5 files (backend `025b8a0`)
- #31 (P2-8) — SHIPPED 2026-06-15 (Chat B) — tree-wide tz-aware Mongo-write sweep + NEW `scripts/check_datetime_hygiene.py` tokenize-based guard (backend `025b8a0`)
- #32 (P3-2) — SHIPPED 2026-06-15 (Chat B) — pin `requires-python = ">=3.12,<3.14"` + `uv lock` relock (backend `025b8a0`)
- #33 (Review note) — SHIPPED 2026-06-26 (Chat B) — hermetic zero-dependency pytest harness, 28 tests across 6 targets + `tests/_fakes.py` FakeCollection + `tests/conftest.py` fake_db fixture (backend `04fd970`)
- #36 (Ops gap) — SHIPPED 2026-07-01 (Chat B) — `POST /admin/recompute/{isin}` Tailscale-only admin recompute endpoint; NEW `app/routers/admin.py` delegating to `holdings_service.recompute_holding` (TD20), registered in `app/main.py` (backend `1ef0ead`)
- #37 (Ops gap) — SHIPPED 2026-07-01 (Chat B) — Atlas backup → fresh-DB restore rehearsal for `monitored_stocks` + `suggestion_outcomes` + `digest_deliveries`; per-collection `mongodump --db=portfolio --collection=<name>` → `mongorestore --nsFrom 'portfolio.*' --nsTo 'portfolio_restore_test.*' --drop` into a dropped scratch DB (counts matched prod 2/155/10, prod untouched); exact runbook documented in `Project_State.md` Section 4 (DOC + OPS only, no code change — code HEAD stays `1ef0ead`)
- #38 (Ops gap) — SHIPPED 2026-07-01 (Chat B) — JSON-structured logging: `app/main.py` `logging.basicConfig` -> stdlib `JsonLogFormatter` (`logging.Formatter` subclass, no new dependency) + `_configure_logging()` over root + `uvicorn`/`uvicorn.error`/`uvicorn.access` (`propagate=False`, no double-logging); one single-line JSON object/record to journald (timestamp UTC ms+Z from `record.created`, level, logger, message, module, func, line; traceback on exc_info; `extra` merged); verified on EC2 (hygiene PASSED, 28 pytest, JSON app+access logs valid). LAST Chat B row — Chat B / Phase 9 COMPLETE (backend `8127c6f071a127e76a0c1cc6afd8a686bcf0cb7c`)
- #40 (Realized-P&L UI hide) — SHIPPED 2026-07-03 (Chat C) — frontend-only; removed the "Realized P&L" StatCard from `components/totals-row.tsx` (grid 4->3 cols, dropped now-unused `TrendingUp` import) + shrank `app/page.tsx` DashboardSkeleton 4->3; backend `holdings.realized_pnl` field STAYS (FIFO + #39 capital-gains read it); `/tax`, sell-sheet realized preview/toast, and transactions/audit copy intentionally untouched; verified via `~/deploy-ui.sh` build+lint on EC2 (frontend `16fab5ae`)
- #63 (Dividends into realised P&L totals) — SHIPPED 2026-07-21 — backend `2792cefa`, frontend `3cde0d39`
- #64 (Re-show Realized P&L card, reverses #40) — SHIPPED 2026-07-21 — frontend `3cde0d39`
- #53 (Demerger holding-period inheritance at the FIFO source, Bundle D partial) — SHIPPED 2026-07-21 — backend-only. NEW optional `Transaction.acquired_date` (holding-period date only; absent -> `trade_date`, byte-identical for existing rows); `_fifo_replay._Lot.acquired_date` emitted as `_realized_lots.buy_trade_date` so `tax_service` classifies STCG/LTCG off the inherited parent acquisition date; `add_manual_transactions.py` TMCV + JIOFIN rows carry `acquired_date` (PLACEHOLDER + TODO for the real date before #42); 4 new tests. SPLIT/BONUS cost were already correct in the actual ledger (untouched); the `DEMERGER` transaction type does not exist (doc-drift corrected — demergers are `manual_demerger` BUY rows). Verified on EC2 (53 pytest, hygiene PASSED, `/tax/capital-gains` 200). Bundle D's #68 remains OPEN.
- #54 (Independent add-a-holding entry point) — SHIPPED 2026-07-25 — frontend-only. `BuySheet` REPLACED with discriminated `holding`|`instrument` prop (holding-header.tsx call site unchanged); NEW `components/add-holding-dialog.tsx` (search sheet → BuySheet in instrument mode); `lib/api.ts` `BuyPayload.isin?`; `app/page.tsx` mounts `<AddHoldingDialog />` in Holdings section. Backend `POST /portfolio/holdings` confirmed already sufficient (`add_buy` creates-or-adds via FIFO recompute, accepts `isin` override). No backend change, no new dep. `tsc --noEmit` clean; `~/deploy-ui.sh` build+lint clean on EC2.
- #55 (LLM-authored hold-horizon on buy suggestions) — SHIPPED 2026-07-25 — backend `931943b`, frontend `7324573`. Four LLM-authored flat keys on the BUY dossier (`hold_horizon` short|medium|long + `hold_horizon_expected_move` + `hold_horizon_rationale` + `hold_horizon_review_trigger`) in `dossier_service._SYSTEM_PROMPT` + buy branch of `_parse_dossier` (coerce-and-default; keys NOT in `required`) + buy branch of `_empty_dossier`; NEW `_clamp_sentence` (boundary clamp, 400 budget) fixes mid-word truncation. Dossier-dict passthrough via `_serialize_run` meant NO `models/suggestion.py` / `scoring_service.py` / `explainability.py` / `routers/suggestions.py` change. Frontend `lib/api.ts` `SuggestionDossier` +4 optional fields + `suggestion-card.tsx` Clock badge (no-expand, buy-only) + "Expected hold horizon" section. NEW `tests/test_dossier_service.py` (9 tests). Verified on EC2 (62 pytest, hygiene PASSED, live end-to-end buy horizons, sell carries none, deploy-ui clean).
- #71 (#55-follow-up: manual "Run now" trigger) — SHIPPED 2026-07-25 — backend `90c23f7`/`931943b`, frontend `b095522`/`7324573`. NEW `POST /suggestions/run?direction=buy|sell` (fire-and-forget 202 via BackgroundTasks → SAME `suggestion_engine.run_suggestions`, `notify=False`) + `GET /suggestions/run/status?direction=` + NEW `suggestion_run_locks` collection (`_id==direction`, 900s TTL, fail-fast 409, mirrors TD20 recompute_locks keyed by direction) + `db/client.py` accessor + `db/indexes.py` TTL index; frontend `triggerSuggestionRun`/`getSuggestionRunStatus` + "Run now" button with 10s polling. Verified on EC2 (202/409 guard, live run completes ~4.5 min, `suggestion_run_locks` index present).
- #41 (Wire `holdings.stop_loss` intraday breach alert) — SHIPPED 2026-07-03 (Chat C) — backend + frontend single unit, closes TD6. NEW `price_service.evaluate_stop_loss_alerts(rows)` on the existing intraday write path (guarded call from `scripts/refresh_prices_intraday.py` right after `insert_intraday_quotes`, same fetched rows, no parallel loop); rising-edge fire-once-on-cross-below, success-gated dedup (only a DELIVERED `stop_loss_hit` Alert suppresses re-fires; re-arm on a later at/above tick), gated on `stop_loss` set AND `"stop_loss" in alert_on`; ntfy-only via `push_public("price", ...)`; persists `Alert(alert_type="stop_loss_hit", channel="ntfy_public_price", severity="high")` to `alerts_log` (FIRST writer) + NEW dedup index `isin_type_sent_desc`. No `routers/holdings.py` change (PATCH whitelist + `get_holding` already sufficient). Frontend read-only stop-loss strip in `components/holding-stats.tsx`; editing stays in `components/notes-panel.tsx`. Verified on EC2 (49 pytest, hygiene PASSED, `alerts_log` 5 indexes, functional 1/0/1, deploy-ui clean) — backend `eb964cc8`, frontend `a4b27bd7`
- #68 (Automate corporate-action processing — demerger/split/bonus data-entry front-end, Bundle D) — SHIPPED 2026-07-27 — backend `f8778c8`, frontend `4031a80`. NEW `app/services/corporate_action_service.py` (pure DB-free builders: `build_split_row`, `build_bonus_row` zero-cost BUY, `build_demerger_child_row` manual_demerger + inherited `acquired_date`, `compute_bonus_quantity`, `compute_demerger_cost_split`, `compute_parent_reprice`) + NEW `POST /transactions/corporate-action` (`RecordCorporateActionRequest` discriminated on `action_type`; idempotent on `source_ref`; `validate_replay` guard on split; bonus qty from the live holding or explicit override; demerger inserts the child BUY + auto-creates the §49(2C) `cost_basis_adjustments` doc + returns the parent-reprice as an AUDITED `PATCH /transactions/{id}` follow-up, never a silent bulk mutation; recompute mirrors the add_buy/sell `recorded_with_warning` contract) + NEW `tests/test_corporate_action.py` (18 hermetic) + `tests/conftest.py` `fake_db` +`cost_basis_adjustments`. Frontend NEW `components/corporate-action-dialog.tsx` + `lib/api.ts` `CorporateAction*` types/binding + `app/transactions/page.tsx` mount. NO FIFO change, NO new collection/index/dep, `/tax` + `tax_service` untouched (compliance). Verified on EC2: 105 pytest (87 + 18), hygiene PASSED, route on OpenAPI, bad-ISIN → 422, split-no-ratios → 400, `~/deploy-ui.sh` build+lint clean, `/transactions` → 200, `tsc --noEmit` clean. Bundle D fully SHIPPED (#53 + #68). GO-LIVE (#42) blocking set drops #68 → remaining: #69 (round-2 review, LAST).

When you finish an item, change its row's Status column from `OPEN` to `SHIPPED <YYYY-MM-DD> (Chat <N>)` and advance the "Next item to start" pointer. Do not delete shipped rows — they are the audit trail.

---

## Chat bundles

Execution overlay (added Chat 5.19) to reduce the number of chats to open. This GROUPS the existing numbered rows into chats — it does NOT renumber, move, or merge rows. Item numbers, phase membership, Status, and the ordering rationale below are all unchanged and remain the source of truth. Bundle = "open one chat, clear these items together."

Bundling policy: bundle only small, independent, same-surface items; keep each large feature one chat (avoids the Section-16 context-loss risk). Conservative plan (chosen Chat 5.19).

**Next chat to open: User Review chat (#52 — Phase 10.5 User Acceptance Review / UAT)** — Chat A, Chat 6, Chat 7, Chat 8, Chat B, Chat 9, and Chat C are all COMPLETE. Chat C SHIPPED both its rows 2026-07-03 (#40 realized-P&L UI hide, frontend `16fab5ae`; #41 stop_loss wiring, backend `eb964cc8` + frontend `a4b27bd7`, closes TD6). Phase 8, Phase 9, and Phase 10 are now fully shipped. The lowest-numbered OPEN item is now #52 (Phase 10.5 — USER ACCEPTANCE REVIEW; GO LIVE #42 is gated behind it).

| Chat | master_todo items | Theme | Surface | Notes |
|---|---|---|---|---|
| **A — Ops & alerting quick-wins** | #25, #26, #34, #35, #47, #48, #49 | Reconciliation/feedback alerting + ops gaps + cron false-positives + doc | Backend + doc only | COMPLETE — all seven SHIPPED 2026-06-14 (Chat A). #34/#35 `bd52c6b`; #25 `1340396`; #49/#26 `6032b64`; #47 `4b638e6`; #48 `fae6edf`. |
| **B — Pre-GO-LIVE hygiene sweep** | #30, #31, #32, #33, #36, #37, #38 | Cross-cutting cleanup (whole Phase 9) | Backend + ops | COMPLETE (Chat B). #30 + #31 (same datetime sweep) + #32 SHIPPED 2026-06-15; #33 pytest harness SHIPPED 2026-06-26; #36 admin recompute + #37 restore rehearsal + #38 JSON logging SHIPPED 2026-07-01. Phase 9 COMPLETE. |
| **C — UI cleanup** | #40, #41 | Realized-P&L hide + stop_loss wiring | Frontend-led (#41 adds backend) | COMPLETE — both SHIPPED 2026-07-03 (Chat C). #40 realized-P&L UI hide (frontend `16fab5ae`); #41 stop_loss intraday alert + `alerts_log` first writer + read-only stop-loss strip (backend `eb964cc8`, frontend `a4b27bd7`, closes TD6). |
| **D — Deferred TDs** | #43, #44, #45 | Post-launch internal cleanup (TD1/TD3/TD7) | Backend + frontend | Run after GO LIVE is stable. |
| **Chat 6** (standalone) | #27 | F1+F3 ad-hoc chat feature | Backend + frontend | COMPLETE — SHIPPED 2026-06-14 (Chat 6). 4 units + 1 route-shadow fix. Filed #50 + #51. |
| **Chat 7** (standalone) | #28 | F12 risk-summary + F15 tag views | Backend + frontend | COMPLETE — SHIPPED 2026-06-15 (Chat 7). 2 backend units (`97041621`, `803e6610`) + 2 frontend units (`e14d6a75`). Read-only; no new collections/indexes/deps. |
| **Chat 8** (standalone) | #29 | F13 watchlist | Backend + frontend | COMPLETE — SHIPPED 2026-06-15 (Chat 8). 4 units (`34ff906d`, `a250d001`, `9857570b`, frontend `58bf6369`) + 1 CORS fix (`67704025`). Evolved build_universe + reused monitored_stocks; no new collection/index. |
| **Chat 9** (standalone) | #39 | F11 capital-gains pack | Backend + frontend | COMPLETE — SHIPPED 2026-07-01 (Chat 9). Backend Unit 1 (`32088c9`: NEW `tax_service.py` + `tax.py` + `_fifo_replay` `_realized_lots` + `tests/test_tax_service.py`) + frontend Unit 2 (`747ae4f`: NEW `app/tax/page.tsx` + `lib/api.ts` + nav). Read-only over the Phase-1 ledger; §49(2C) honored via the ledger, no parallel FIFO path. |
| **User Review chat** (standalone) | #52 | F-USERACCEPT complete user acceptance review / UAT | Cross-cutting (you drive) | NEW (Chat 8). Second-to-last stage. You walk every page/flow/cron and file findings as their own rows in NEW ITEMS (no renumbering). GO LIVE (#42) must not start until blocking findings are cleared or deferred. |
| **Chat 10 — GO LIVE** (standalone) | #42 | F7 real ICICI data import | Backend | Last, alone, so test pollution wipes in one operation. Gated behind the User Review chat (#52). |
| **Scheduler chat** (standalone) | #46 | TD21 registry-generated crontab migration | Backend + ops | Section 9 explicitly mandates its own dedicated chat. |

Net: remaining OPEN work is now 4 chats (Chat D + 3 standalone: User Review, GO LIVE, Scheduler; Chats A, 6, 7, 8, B, 9, and C done). Sequencing still follows the Ordering rationale + per-row gating; bundles never override a gating dependency. Recommended order: User Review (#52) → Chat 10 GO LIVE (#42) → Scheduler (#46) → Chat D, adjusting for whatever you want next. When you finish a bundle, mark each of its rows SHIPPED individually (the bundle is a chat-grouping, not a single work item) and advance the "Next item to start" pointer to the lowest-numbered OPEN row as usual.

---

### Phase 10.5 blocking-set chat bundles (added Chat #54)

Overlay for the remaining Phase 10.5 round-1 blocking findings (#53-#69, minus #60/#61 non-blocking and #66/#67 already SHIPPED). Same policy as above: bundle only small, independent, same-surface items; keep large/compliance-sensitive features one chat; bundles NEVER override a gating dependency. Groups rows for chat efficiency — does NOT renumber or merge them.

| Bundle | Items | Theme / surface | Coupling rationale | Status |
|---|---|---|---|---|
| **A — frontend nav & shell** | #58, #59, #62 | Dashboard nav declutter + audit back-nav + collapsible sections (`app/page.tsx`, `app/transactions/audit/page.tsx`, NEW `collapsible-section.tsx`) | All pure-frontend UX, same surface, mutually independent, zero backend/data risk | SHIPPED 2026-07-08 (Chat #54, frontend `e3a9a44b3aaf1597edeeca7500779c0eb2de3361`) |
| **B — dividends & realised-P&L** | #63, #64, #65 | Dividend income into realised P&L + re-show figure + announcement-capture/drift matrix (`holdings_service`, `reconciliation`, yfinance corp actions, `totals-row.tsx`, recon page) | One coherent feature: #65 captures announcements -> #63 folds dividends into realised P&L (NOT /tax capital-gains) -> #64 re-shows the figure (reverses #40). Compliance-sensitive. | #63 + #64 SHIPPED 2026-07-21 (backend `2792cefa`, frontend `3cde0d39`); **#65 SHIPPED 2026-07-27** (backend `1f390c0` + frontend `92a8974` — yfinance dividend-announcement capture into NEW `dividend_announcements` + `compute_dividend_drift` matrix + flood-capped ntfy nudge + `GET /reconciliation/dividend-drift` + read-only recon-page card; news `corporate_action` corroboration; NOT a tax view). **Bundle B fully SHIPPED.** |
| **C — alert evaluators** | #56 SHIPPED 2026-07-25; #50 + #57 SHIPPED 2026-07-26 | Target/sell-price breach + news alerts (`price_service`, `refresh_prices_intraday`, `news_fetcher`/`news_classifier`, `news_alerts`, `alerts_log`, `notify`) | #56 SHIPPED (backend `6f1aa57` + frontend `a2ffef1`); #50 SHIPPED (backend `28e57f6` — entity relevance gate + LLM confirm + retag backfill); #57 SHIPPED (backend `4a9851c` — `news_alerts.evaluate_news_alerts`, wired into fetch cron Phase 2b+3). **Bundle C fully SHIPPED.** | ALL SHIPPED |
| **D — corporate-action cost-basis** | #53 (done), #68 | Full corp-action cost-basis at the FIFO source + data-entry/automation front-end (`_fifo_replay`, `tax_service`, `cost_basis_adjustments`, transactions UI/router) | #68 is the data-entry/automation front-end for the #53 math. **#53 SHIPPED 2026-07-21 (backend-only)** — scoped to demerger holding-period inheritance (`acquired_date` at the FIFO source); SPLIT/BONUS cost were already correct so untouched; no `DEMERGER` type exists (doc-drift fixed). Highest-risk row cleared without a full FIFO rewrite (Option A). **#68 SHIPPED 2026-07-27** — the data-entry/automation front-end: NEW `app/services/corporate_action_service.py` (pure builders) + `POST /transactions/corporate-action` (split/bonus/demerger, idempotent, auto-creates the §49(2C) `cost_basis_adjustments` doc, returns the parent-reprice as an audited PATCH follow-up) + `components/corporate-action-dialog.tsx`. NO FIFO change (the math it drives is already correct at the source). Bundle D fully SHIPPED. | #53 SHIPPED 2026-07-21; #68 SHIPPED 2026-07-27 — Bundle D COMPLETE |
| **#54** (standalone) | #54 | Add-a-holding entry point (frontend buy-sheet + existing create/transaction path) | Frontend-led, mostly independent; different surface from Bundle A. | SHIPPED 2026-07-25 — frontend-only. `BuySheet` REPLACED with discriminated `holding`\|`instrument` prop; NEW `add-holding-dialog.tsx`; `lib/api.ts` `BuyPayload.isin?`; `app/page.tsx` mounts `<AddHoldingDialog />` in Holdings section above `<HoldingsTable>`. Backend confirmed already sufficient. `tsc --noEmit` clean; `~/deploy-ui.sh` build+lint clean. |
| **#55** (standalone) | #55 | LLM-authored hold-horizon on buy suggestions (`dossier_service`, `scoring_service`, `models/suggestion.py`, `explainability`, `suggestion-card.tsx`) | Standalone LLM feature — one per chat. | SHIPPED 2026-07-25 — scoped to the two real seams (`dossier_service.py` + frontend `lib/api.ts`/`suggestion-card.tsx`); `scoring_service`/`models/suggestion.py`/`explainability` NOT needed (dossier dict passthrough). Backend `931943b`, frontend `7324573`. Also shipped the #71 manual-run trigger. |
| **#69 (LAST)** | #69 | Round-2 user-acceptance review | Must run after all round-1 blockers clear; gates #42. | CLOSED 2026-08-01 — all #72–#79 fix bundles + #44 + #51 SHIPPED; round-3 sanity PASSED; GO/NO-GO = GO; #42 UNBLOCKED. |

Recommended order: Bundle A (done) -> B (#63/#64 done; #65 done) -> #53 (done) -> #54 (done) -> #55 (done) -> C (#56 done; #50 -> #57 done) -> #65 (done) -> #68 (done) -> #69 (CLOSED) -> #42 GO LIVE. Non-blocking #60/#61 (doc-prep) can slot anywhere. #66 + #67 SHIPPED 2026-07-08; #53 SHIPPED 2026-07-21; #54 + #55 SHIPPED 2026-07-25; #56 + #70 SHIPPED 2026-07-25; #50 + #57 SHIPPED 2026-07-26; #65 + #68 SHIPPED 2026-07-27. **#72–#79 (round-2 fix bundles) ALL SHIPPED 2026-08-01. #44 + #51 SHIPPED 2026-08-01. Round-3 sanity PASSED. GO/NO-GO = GO. #42 GO LIVE IS UNBLOCKED.** (#71, the #55-follow-up manual "Run now" trigger, shipped alongside #55 — not a blocking row.)

---

## Ordering rationale

Ordered to minimize rework. Principle: **fix the code surface before adding features on top of it.**

1. **Phase 1** — Unblock ops first (no code; immediate value; restores weekly digest).
2. **Phase 1** — Reconcile documentation (TD15) before any chat that reads files with F-comments; otherwise future chats hallucinate against unmapped F-numbers.
3. **Phase 2** — Fix transactions/holdings/audit invariants BEFORE Chat 9 touches `holdings` (stop_loss + realized P&L hide). SHIPPED Chat 5.10.
4. **Phase 3** — Fix intraday correctness early; every dashboard load and every sell-side suggestion depends on it. SHIPPED Chat 5.11.
5. **Phase 4** — Storage hygiene (TTL + body purge) BEFORE Chat 10 GO LIVE — real ICICI data import is when collections start filling for keeps. SHIPPED Chat 5.12.
6. **Phases 5-7** — Frontend correctness + external-service hardening + reconciliation alerting; mostly independent of one another, can be batched. Phase 5 SHIPPED Chat 5.13; Phase 6 #19 SHIPPED Chat 5.14, #20 SHIPPED Chat 5.15, #21 SHIPPED Chat 5.16, #22 SHIPPED Chat 5.17, #23 SHIPPED Chat 5.18, #24 SHIPPED Chat 5.19 — Phase 6 COMPLETE. Phase 7 (#25, #26) SHIPPED Chat A — Phase 7 COMPLETE.
7. **Phase 8** — New features (Chats 6-8) AFTER underlying services are correct; Chat 8 (watchlist) must come after Phase 4 (storage) + Phase 6 (Tavily race) since it multiplies data volume. Chat 6 (#27 F1+F3 ad-hoc chat) SHIPPED Chat 6; Chat 7 (#28 F12 risk-summary + F15 by-tag) SHIPPED Chat 7 — both read-only over existing surfaces. Chat 8 (#29 F13 watchlist) SHIPPED Chat 8 — evolved build_universe + reused monitored_stocks; the two crons fold in watchlist ISINs (the data-volume multiplier, Tavily blast-radius documented as a soft guardrail). **Phase 8 COMPLETE.**
8. **Phase 9** — Cross-cutting cleanup (`datetime.utcnow()` sweep, Python ceiling, pytest harness, ops gaps) right before GO LIVE so launch lands on one clean state. (The two ops-gap quick-wins #34 + #35 were pulled forward into Chat A as small/independent alerting fixes; #30 + #31 (datetime sweep) + #32 (Python ceiling) SHIPPED in Chat B 2026-06-15; #33 (pytest harness) SHIPPED in Chat B 2026-06-26; #36 (admin recompute) + #37 (backup→restore rehearsal) + #38 (JSON-structured logging) SHIPPED in Chat B 2026-07-01; Phase 9 COMPLETE.)
9. **Phase 10** — Chat 9 pre-launch cleanup (F11 + realized P&L hide + stop_loss wiring).
10. **Phase 10.5** — USER ACCEPTANCE REVIEW (#52, NEW Chat 8). You personally walk the entire tool end-to-end and file every bug / issue / change request. Second-to-last stage; GO LIVE must not start until blocking findings are resolved or explicitly deferred. The system is "production-ready" only AFTER this review is cleared.
11. **Phase 11** — Chat 10 GO LIVE (F7 real data import) — last, so test pollution gets wiped in one operation. Gated behind Phase 10.5.
12. **Phase 12** — Deferred TDs (TD1, TD3, TD7) — after launch is stable.
13. **New items (Chat 5.9 / 5.16 / 5.19 / Chat 6)** — TD21 (registry-generated crontab migration) remains OPEN; TD22 (track_suggestion_outcomes daily failure) SHIPPED Chat A; TD36 (Tavily monthly→daily doc cleanup) SHIPPED Chat A; TD40 (weekly_suggestions_sell Sunday false MISSING) SHIPPED Chat A; #50 (news entity mis-tagging) + #51 (dividend_yield ×100 formatting) filed Chat 6 and OPEN; see the NEW ITEMS phase below.

---

## Item legend

- **Source column codes:**
 - `TD<N>` — Tech debt registry row in `Project_State.md` Section 18
 - `P0/P1/P2/P3-<N>` — Code review finding (see `code_review_findings_chat_6_audit.md` if archived, or the Chat 5.8 review)
 - `Chat <N>` — Pre-existing chat in the Section 13 chat split plan
 - `F<N>` — Feature ticket (mirrored from external registry; see TD15)
 - `Ops gap` — Operational gap called out in the review's ops-gaps section
 - `User request` — Stage/row added at the user's explicit direction in a chat
- **Status column codes:**
 - `OPEN` — Not started
 - `IN PROGRESS (Chat <N>)` — Being worked in the named chat
 - `SHIPPED <YYYY-MM-DD> (Chat <N>)` — Done; commit landed
 - `DEFERRED` — Acknowledged and intentionally pushed to a later phase
 - `DROPPED` — Explicitly de-scoped; note rationale in the row

---

## PHASE 1 — Unblock operations (no code; do this week) — SHIPPED Chat 5.9

| # | Source | Item | Files / surface | Status |
|---|---|---|---|---|
| 1 | TD14 / P1-5 | Fix Sunday 07:00 IST crontab line: drop bogus `--notify --run-type scheduled` flags. Optional: run `scripts/run_weekly_suggestions.py --direction=both` manually for immediate digest recovery. Also confirm whether nightly `cron_health_check` ntfy + email alerts have actually been arriving — if not, second silent failure in dual-transport path. **Chat 5.9 closed build-right: Part A flags removed from crontab (verified via `crontab -l`); Part B `CRON_REGISTRY` entry renamed `run_weekly_suggestions` → `weekly_suggestions` (commit `c097b473`) to match the heartbeat the script writes, killing the phantom Sunday MISSING. Dual-transport confirmed HEALTHY by inspection (email + ntfy both arrive daily). The daily 21:00 health alert is a SEPARATE failure → filed as #47 (TD22).** | EC2 `crontab -e` + `app/services/cron_heartbeat_service.py` | SHIPPED 2026-06-02 (Chat 5.9) |
| 2 | TD10 | Remove redundant `0 0 * * 0 find ... -size +10M ...` crontab line (logrotate replaces it). Verify first logrotate cycle completed (next: 2026-05-31 Sun 00:00 IST window) then remove. **Chat 5.9: GATE PASSED (rotation trail `cron-*.log.1` 2026-05-31 + `.2.gz` 2026-05-24 present for all 10 logs) AND the `find -size +10M` line was found ABSENT from the live crontab — already removed in a prior session or never deployed. End state satisfied; no edit needed.** | EC2 `crontab -e` | SHIPPED 2026-06-02 (Chat 5.9) |
| 3 | TD15 | Reconcile F-number fix registry: read every file at HEAD that carries an F-comment (F2/F3/F4/F5/F7/F8/F12/F14/F16-F21/F23/F27-F29/F79/F80/F82), map each F-number to its file + one-line description, add rows to `Project_State.md` Section 18. Do BEFORE any code chat. Also: this may surface that items #26 (P2-6) and #43 (TD1) are already partially addressed by an F-ticket. **Chat 5.9: grepped at HEAD `c097b473` — 25 unique in-code F-numbers (the "~20" estimate was low; fix-registry subset is 21) across TWO colliding namespaces (feature-F vs fix-Chat-5.5+-F). Authored the "F-number fix registry" subsection in Section 18 with a Kind column. Recovered the truncated Section 18 (and Sections 16-tail through 22) that the Chat 5.8 doc commit `8f74b50` had silently amputated. No overlap found that lets #26 or #43 drop — both stay OPEN/DEFERRED.** | `docs/Project_State.md` | SHIPPED 2026-06-02 (Chat 5.9) |

## PHASE 2 — Transactions / holdings / audit consistency — SHIPPED Chat 5.10

Fix this surface before Chat 9 touches it. All five items shipped + verified on EC2 against localhost:8000.

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 4 | P1-1 | Make `PATCH /transactions/{id}` and `DELETE /transactions/{id}` write `transactions_audit` BEFORE applying the change. Mirror the suggestions feedback handler pattern. **Chat 5.10 SHIPPED: both handlers now `log_change(...)` BEFORE `update_one(...)`; PATCH audits a computed `{**before, **update_fields}` after-state then applies then re-reads for the response; `validate_replay` still runs first so a rejected edit/delete writes no audit row.** | `app/routers/transactions.py` `edit_transaction` + `delete_transaction` | SHIPPED 2026-06-06 (Chat 5.10) — commit `17f9f94` |
| 5 | P1-3 | Add `validate_replay` to `/portfolio/holdings/{isin}/sell` and to the manual import path so backdated SELLs that produce negative quantity get 400'd, not silently logged. **Chat 5.10 SHIPPED: `/sell` replays `existing_txns + [proposed_sell]` and 400s before the ledger write; `add_manual_transactions.py` gates SELL inserts on `validate_replay` and aborts with `RuntimeError` instead of silently staging. Existing point-in-time `held_qty` check kept for the clearer common-case message.** | `app/routers/holdings.py` `sell`; `scripts/add_manual_transactions.py` insert loop | SHIPPED 2026-06-06 (Chat 5.10) — commit `5cf3087` |
| 6 | P1-2 | Delete duplicate route handler `list_transactions` (lines ~329-335); keep `get_holding_transactions` (~163-180). **Chat 5.10 SHIPPED: deleted the shadowed EOF `list_transactions` handler; `get_holding_transactions` is the sole handler for `GET /portfolio/holdings/{isin}/transactions`. Behaviour-neutral (first-registered handler already won every request).** | `app/routers/holdings.py` | SHIPPED 2026-06-06 (Chat 5.10) — committed after `17f9f94`, before `5cf3087` |
| 7 | P2-9 | Make `add_buy` / `sell` non-atomic path safer: wrap `recompute_holding` in try/except, return success with a warning flag if recompute fails. Or use Mongo M10 transactions for atomicity. **Chat 5.10 SHIPPED (warning-flag path, user-confirmed): both handlers wrap `recompute_holding` in try/except; on exception they `log.exception(...)` and return 2xx `{status:"recorded_with_warning", isin, warning}` so the persisted ledger write isn't masked. `recompute_holding` returning None stays a legitimate full-exit success outside the except. M10 multi-doc transactions explicitly REJECTED — per-step session latency on a single-user box not worth it.** | `app/routers/holdings.py` `add_buy` + `sell` (+ module logger) | SHIPPED 2026-06-06 (Chat 5.10) — commit `fb23307` |
| 8 | P2-10 | Serialize `recompute_holding` per-ISIN: per-ISIN advisory lock doc with TTL, OR API-layer mutex keyed by ISIN. **Chat 5.10 SHIPPED (advisory-lock doc, user-confirmed): `recompute_holding` wraps its body (renamed `_recompute_holding_impl`) in `_per_isin_recompute_lock`, a CM that inserts a doc keyed `_id==isin` into the new `recompute_locks` collection (atomic via unique `_id` index), releases in `finally`, and is TTL-reclaimed after 60s. `asyncio.Lock` REJECTED — confirmed at HEAD that every holdings handler is sync `def` under sync Uvicorn, so asyncio.Lock wouldn't apply; `threading.Lock` rejected because it's blind to the out-of-process scripts. Lock lives at the service layer so all callers (API + scripts) are covered.** | `app/services/holdings_service.py`; `app/db/client.py`; `app/db/indexes.py` | SHIPPED 2026-06-06 (Chat 5.10) — commit `b34721e` |

## PHASE 3 — Intraday & price correctness — SHIPPED Chat 5.11

All three items shipped in ONE backend code commit (`a2806cd`) + verified on EC2 against localhost:8000. All three touch only `app/services/price_service.py`.

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 9 | P1-4 / TD23 | Guard `_intraday_row_from_df` against yfinance returning yesterday's bar on market holidays: if bar timestamp's IST date != today, return None. **Chat 5.11 SHIPPED: the function now reads the latest 5m bar's index timestamp (`clean.index[-1]`) and returns None when its IST date != today's IST date — yfinance `period="1d"` returns the prior trading day's bars on an NSE holiday. Added module-level `IST = timezone(UTC+5:30)` (India has no DST) + `_to_ist()` helper (tz-aware → `astimezone`; tz-naive → treated as UTC first, matching the existing `_df_to_rows` / `annotate_with_current_price` naive→UTC convention). "today" is derived from the passed-in `captured_at`. Verified on EC2: a today-dated synthetic bar returns a dict; a yesterday-dated bar returns None.** | `app/services/price_service.py` `_intraday_row_from_df` (+ new `IST`/`_to_ist` helpers) | SHIPPED 2026-06-08 (Chat 5.11) — commit `a2806cd` |
| 10 | P2-14 / TD24 | Align `price_stale` docstring vs code: doc says "4 trading days", code says `timedelta(days=6)`. Pick one; pragmatic fix is update docstring. **Chat 5.11 SHIPPED (CODE is canonical — user-delegated decision): kept `timedelta(days=6)`; updated the docstring "more than 4 trading days old" → "more than 6 calendar days old" and added an inline comment noting 6 calendar days ≈ 4 NSE trading days across a weekend (the clear original intent). Doc-/comment-only; zero behaviour change. Verified: `/portfolio/holdings` still returns `price_stale` correctly (false on a fresh price).** | `app/services/price_service.py` `price_stale` docstring + inline comment | SHIPPED 2026-06-08 (Chat 5.11) — commit `a2806cd` |
| 11 | P2-13 / TD25 | Rewrite `bulk_get_previous_closes` to push the filter into the Mongo pipeline (or loop `find_one` per ISIN). Currently pulls ~34k price docs per dashboard request. **Chat 5.11 SHIPPED (per-ISIN `find_one` — user-delegated decision): rewrote the body to delegate to the existing single-ISIN `get_previous_close` (an indexed `find_one({"date": {"$lt": latest}}, sort=[("date",-1)])` point-query per ISIN) instead of `$push`-ing every `{date, close}` for every ISIN into an in-memory `all_dates` array and filtering in Python. Eliminates the ~34k-doc pull per dashboard request; keeps the Decimal128/Decimal normalization in exactly one place. Chosen over an aggregation-pipeline rewrite because it evolves existing code (no new query pattern) and the `(isin, date)` index makes each call a single-doc point-query. Verified on EC2: `bulk_get_previous_closes` result is byte-identical to per-ISIN `get_previous_close` for all held ISINs.** | `app/services/price_service.py` `bulk_get_previous_closes` | SHIPPED 2026-06-08 (Chat 5.11) — commit `a2806cd` |

## PHASE 4 — Storage hygiene (must land BEFORE Chat 10 GO LIVE) — SHIPPED Chat 5.12

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 12 | P2-3 | Add TTL index on `prices_intraday.captured_at` with `expireAfterSeconds = 90 * 86400`. **Chat 5.12 SHIPPED (TD26): confirmed at HEAD that `_intraday_row_from_df` writes `captured_at` as a BSON Date (`datetime.now(timezone.utc)`, serialized to UTC Date) so the TTL actually expires docs — a TTL silently no-ops on a string/Decimal field. Added `captured_at_ttl` (ASC, `expireAfterSeconds = 90*86400 = 7776000`) alongside the existing non-TTL `captured_at_desc` (ASC vs DESC are different key patterns, so both coexist — mirrors `cron_heartbeats` `started_at_ttl` + `started_at_desc`). `ensure_all_indexes` stays purely additive (no drop). Verified on EC2: `db.prices_intraday.getIndexes()` shows `captured_at_ttl` on `{captured_at:1}` with `expireAfterSeconds:7776000`; all four indexes intact.** | `app/db/indexes.py` | SHIPPED 2026-06-08 (Chat 5.12) |
| 13 | P2-4 | New `scripts/purge_news_bodies.py` daily cron: `$unset {body_text: ""}` on classified `news_articles` whose `fetched_at` is older than 30 days; stamp `body_purged_at`. Register in `CRON_REGISTRY`. **Chat 5.12 SHIPPED (TD27): field corrections vs the original spec — the bulky field is `body_text` (NOT `body`), and age keys on `fetched_at` (always present via `default_factory=utcnow`; `published_at` is nullable so it would strand undated docs). Script mirrors `refresh_prices_intraday.py` (`cron_run("purge_news_bodies")` heartbeat, `hb.metadata[...]`, `hb.mark_skipped("no_expired_bodies")` on nothing-to-do); adds `--dry-run` (count-only). Filter `{classified:True, fetched_at:{$lt:cutoff}, body_purged_at:None, body_text:{$nin:["",None]}}`; write `{$unset:{body_text:""}, $set:{body_purged_at: now}}` (idempotent). `CronSpec.cron_name == cron_run() name == "purge_news_bodies"` (TD14 contract). Crontab line at 02:30 IST (`30 2 * * *`) with `>> cron-news-purge.log 2>&1`. Verified on EC2 against the real `portfolio` DB: dry-run reported 1 candidate, live run purged 1, sentinel returned `body_text` absent + `body_purged_at` a Date, success heartbeat `metadata.purged:1`; `/cron/heartbeats` shows it `healthy:true, expected_today:true`.** | `scripts/purge_news_bodies.py` (NEW) + crontab + `CRON_REGISTRY` | SHIPPED 2026-06-08 (Chat 5.12) |

## PHASE 5 — Frontend correctness & quick wins — SHIPPED Chat 5.13

Spans BOTH repos: #14 is frontend (one commit, frontend HEAD `f59958`); #15/#17/#18 are backend (three commits, deployed code HEAD `090d96c`); #16 is a doc-drift confirmation (no code). All verified on EC2 — frontend via `~/deploy-ui.sh` + `npm run build`; backend via `ssh ubuntu@100.112.20.41` + `curl localhost:8000` with positive landed-assertions per item.

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 14 | P2-2 | Swap `invalidateQueries` → `refetchQueries` in `notes-panel.tsx` (lines 43, 46) and `refresh-button.tsx` (lines 17-19). **Chat 5.13 SHIPPED (TD28): minimal name-swap (no `async`/`await` reorder). At HEAD the two `notes-panel.tsx` calls were at lines 42 + 45 (`["holding", holding.isin]`, `["dashboard"]`); the three `refresh-button.tsx` calls were at lines 17-19 inside the existing `await Promise.all([...])` (`["dashboard"]`, `["reconciliation"]`, `["cost-basis"]`). Aligns these two outliers with the project-wide synchronous-refetch convention (Project_State Section 14). Verified on frontend HEAD `f59958`: `grep invalidateQueries` → 0; `refetchQueries` counts notes-panel:2 + refresh-button:3 = 5; `~/deploy-ui.sh` build clean.** | `components/notes-panel.tsx`, `components/refresh-button.tsx` | SHIPPED 2026-06-08 (Chat 5.13) — frontend HEAD `f59958` |
| 15 | P3-3 | Remove unused `from pydoc import doc` import. **Chat 5.13 SHIPPED (TD29): dead import at line 6, immediately shadowed by local `doc` variables in the serializer helpers. Behaviour-neutral. Verified on backend HEAD `090d96c`: `grep "from pydoc import doc"` → empty (literal string, true negative).** | `app/routers/holdings.py` line 6 | SHIPPED 2026-06-08 (Chat 5.13) — backend HEAD `090d96c` |
| 16 | P3-6 | Fix doc drift: `Project_State.md` Section 10 says `MONGODB_URL`, code uses `MONGODB_URI`. **Chat 5.13 SHIPPED (TD30): confirmed at HEAD that Project_State Section 10 already reads `MONGODB_URI` (with the explicit master_todo #16 note that earlier versions said `MONGODB_URL`) — the correction had landed in the Chat 5.12 Project_State. Row closed with no further code/doc edit beyond stamping it SHIPPED here. Doc-only confirmation; zero behaviour change.** | `docs/Project_State.md` | SHIPPED 2026-06-08 (Chat 5.13) |
| 17 | P3-7 | Add `pattern=r"^[A-Z0-9]{12}$"` to ISIN Path params on `/suggestions/{isin}/audit` and `/suggestions/{isin}/feedback`. **Chat 5.13 SHIPPED (TD31): added `pattern=r"^[A-Z0-9]{12}$"` alongside the existing `min_length=12, max_length=12` on the two ISIN `Path(...)` params — `get_feedback_audit_for_isin` (line 240, GET `/suggestions/{isin}/audit`) and `submit_feedback` (line 260, POST `/suggestions/{isin}/feedback`). `/runs/{run_id}` left untouched (it's an ObjectId, not an ISIN). Malformed ISINs now 422 at the boundary instead of reaching Mongo. Verified on backend HEAD `090d96c`: `grep -F 'pattern=r"^[A-Z0-9]{12}$"'` → 2 matches; a 12-char lowercase ISIN `INE002a01018` → 422 (rejected by pattern, not length); valid `INE002A01018` → 200.** | `app/routers/suggestions.py` ~245 | SHIPPED 2026-06-08 (Chat 5.13) — backend HEAD `090d96c` |
| 18 | P3-8 | Drop `"$options": "i"` from `transactions/search` regex — symbols already uppercased pre-query; "i" disables the index. **Chat 5.13 SHIPPED (TD32): the regex was at lines 91-92 in `search_transactions` (not ~102-115; that range is the date-bound block). Input is already `symbol.upper()` and symbols are stored uppercase, so the `"i"` flag was pure redundancy that disabled the `(symbol, trade_date)` index on the prefix regex. Dropped the flag (`query["symbol"] = {"$regex": f"^{escaped}"}`) and corrected the now-false "(case-insensitive)" inline comment. Behaviour-neutral for callers; restores index use. Verified on backend HEAD `090d96c`: `grep '$options'` → empty; clean regex present at line 113; `GET /transactions/search?symbol=tr` returns `total: 20` (parity with pre-change).** | `app/routers/transactions.py` ~102-115 | SHIPPED 2026-06-08 (Chat 5.13) — backend HEAD `090d96c` |

## PHASE 6 — External-service hardening — SHIPPED Chat 5.14–5.19 (COMPLETE)

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 19 | P2-5 | Replace Tavily quota check-then-act with atomic `find_one_and_update` against `tavily_quota`. **Chat 5.14 SHIPPED (TD33): collapsed the `get_today_quota()` pre-check + separate `_increment_quota()` `$inc` into ONE conditional `find_one_and_update` filtered on `{date_utc: today, calls_today: {$lt: TAVILY_DAILY_CALL_LIMIT}}` with `upsert=True, return_document=AFTER` and the existing `$inc`/`$setOnInsert`/`$set` blocks. Under the cap (or the day's first call) the filter matches/upserts and the `$inc` applies atomically; at/over the cap the existing same-day doc no longer matches, so the upsert attempts a second `date_utc==today` insert and the unique `date_unique` index raises `DuplicateKeyError`, caught and surfaced as `TavilyQuotaExceeded` (no credit consumed on refusal). Cap stays calls-only (`credits_today` tracked, not capped) — race fix, not a new ceiling (user-delegated). Added `from pymongo.errors import DuplicateKeyError`; removed the now-redundant pre-check in `search()`. Backend-only; callers (`news_fetcher.py`) untouched. Verified on EC2 at backend HEAD `4ac2c95`: `/health` ok/ok, `/suggestions/latest?direction=buy|sell` + `/cron/heartbeats` all 200 (import chain intact; the guard has no HTTP surface, so coverage is deploy + import-graph + boot regression).** | `app/services/tavily_client.py` ~110-145 | SHIPPED 2026-06-09 (Chat 5.14) — commit `4ac2c95` |
| 20 | P3-4 | Add 1-2 attempt retry with 30-60s backoff inside `notify.email()` on transient 5xx / 429. Don't retry 400s. **Chat 5.15 SHIPPED (TD34): added a 1-retry (2 attempts total) loop inside `email()` on transient HTTP 429/5xx with a blocking 30s backoff; 400s and any other client/no-status error return immediately (no retry). Added module-level `logging` + `time` imports + a module logger, two helpers — `_email_error_status()` (reads the Resend SDK exception's int status off `.code`/`.status_code`, falls back to `error_type=="rate_limit_exceeded"`→429) and `_is_transient_email_error()` (True only for 429 + 5xx) — plus three module constants (`_EMAIL_MAX_ATTEMPTS=2`, `_EMAIL_RETRY_BACKOFF_SECONDS=30`, `_EMAIL_TRANSIENT_STATUSES`). The `{ok, id, error}` return contract and the no-raise / swallow-exceptions guarantee are UNCHANGED, so the three callers that branch on `result["ok"]` (`digest_delivery`, `reconciliation._send_drift_alerts`, `cron_health_check` dual-transport) are untouched — verified by re-reading all three at HEAD before patching. Retry count / backoff seconds / blocking-`time.sleep` acceptability were user-delegated; chose 1 retry + 30s fixed blocking sleep (conservative end of the 30–60s window). Verified on EC2 at backend HEAD `7d77b9c` via a monkeypatched harness.** | `app/services/notify.py` | SHIPPED 2026-06-12 (Chat 5.15) — commit `7d77b9c` |
| 21 | P3-5 | Persist suggestion run BEFORE digest formatting; pass `inserted_id` explicitly to `send_combined_digest`. **Chat 5.16 SHIPPED (TD35): re-anchored at HEAD `67fd680` and found the run is ALREADY persisted before digest formatting (`_persist_run(run)` runs inside `_run_buy_pipeline`/`_run_sell_pipeline` and returns `result.inserted_id`); the real defect was that the id was discarded and then re-derived TWICE via `find_one(..., sort=[("run_date", -1)])`. Fix (model-carried id, NO signature change): `_persist_run` now sets `run.id = result.inserted_id` on the in-memory `SuggestionRun`, covering every persist path. `send_combined_digest` reads `buy_run_id = buy_run.id` and the internal `find_one` re-derivation is deleted (signature unchanged → sole caller `_do_both` untouched). `_do_both` attaches outcomes via `buy_run.id` / `sell_run.id`. Standalone `--direction=buy/sell` path passes `run_id=run.id` into the two `send_weekly_digest(run)` calls. Backend-only, ONE commit `f4168b3`. Verified on EC2 via grep + a monkeypatched harness with a `find_one` tripwire.** | `app/services/digest_delivery.py` ~470-490; `app/services/suggestion_engine.py`; `scripts/run_weekly_suggestions.py` | SHIPPED 2026-06-12 (Chat 5.16) — commit `f4168b3` |
| 22 | P3-1 | Reject NaN in `_to_decimal`: `if isinstance(v, float) and v != v: raise ValueError("NaN not allowed")`. **Chat 5.17 SHIPPED (TD37): nested the NaN check inside the existing `float` branch (`if v != v: raise ValueError("NaN not allowed")` before `return Decimal(str(v))`). `ValueError` (not `TypeError`) so the `Money = Annotated[Decimal, BeforeValidator(_to_decimal)]` validator surfaces a NaN as a Pydantic `ValidationError` → 422 at the API boundary; `TypeError` would have escaped as a 500. Scoped to the float ingress path only — `Decimal('NaN')` / Decimal128 NaN read paths are out of #22's scope. Verified on EC2 at backend HEAD `1d627d7` via landed-grep + an in-box harness.** | `app/models/_common.py` ~12 | SHIPPED 2026-06-12 (Chat 5.17) — commit `1d627d7` |
| 23 | P2-12 | Add fallback log file for `cron_run` heartbeat-insert failure (`/home/ubuntu/cron-heartbeat-fallback.log`); `cron_health_check` reads both sources. **Chat 5.18 SHIPPED (TD38): `_persist`'s `except Exception: pass` replaced with `except Exception: _append_fallback(doc)` — a new best-effort writer that appends the heartbeat as ONE JSON object per line to `_FALLBACK_LOG_PATH = "/home/ubuntu/cron-heartbeat-fallback.log"` and NEVER raises. New reader `count_today_heartbeats_from_fallback(...)` mirrors `count_today_heartbeats` exactly; `_parse_fallback_dt` normalizes tz-aware → naive UTC; a missing/unreadable file or malformed line contributes zero and never raises. `cron_health_check.main` merges its counts into the Mongo counts BEFORE the MISSING/FAILED evaluation, recording `metadata.fallback_heartbeats_merged`. Because a run's heartbeat lands in at most one source, the merge can never double-count. Verified on EC2 at backend HEAD `0515fef` via a temp-file + forced-Mongo-failure-tripwire harness + the live `cron_health_check.py` running end-to-end through the merge loop.** | `app/services/cron_heartbeat_service.py` + `scripts/cron_health_check.py` | SHIPPED 2026-06-12 (Chat 5.18) — commit `0515fef` |
| 24 | P3-9 | Wrap `cron_health_check.main` Mongo reads in try/except that fires an "anomaly: health-check itself failed" ntfy on errors channel even when Mongo is unreachable. **Chat 5.19 SHIPPED (TD39): wrapped `main`'s per-cron heartbeat-read loop — the ONLY place the script reads Mongo (the `count_today_heartbeats` calls) — in `try/except`. On a Mongo-read failure the `except` fires a dedicated "anomaly: health-check itself failed — could not read cron heartbeats from MongoDB" alert on BOTH transports: `push_public(channel="errors", priority="urgent", ...)` GUARDED in its own try/except + `notify.email(...)` (never raises). It then RE-RAISES so `cron_run` records the run as a `failure` (its heartbeat falls to the #23/TD38 disk fallback). The pure `ist_today_window_utc()`, disk-only `count_today_heartbeats_from_fallback()`, and in-memory `get_registry()` stay outside the wrap, and the #23/TD38 merge loop is preserved verbatim INSIDE the wrap. Backend-only, ONE commit `7fcda9e`. LAST Phase 6 item — Phase 6 COMPLETE.** | `scripts/cron_health_check.py` | SHIPPED 2026-06-14 (Chat 5.19) — commit `7fcda9e` |

## PHASE 7 — Reconciliation alerting & feedback correctness — SHIPPED Chat A (COMPLETE)

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 25 | P2-7 | Make `take_auto_snapshot` fire `push_public("price", ...)` when drift change exceeds threshold against last manual snapshot. ntfy only (email too noisy on daily cron). **Chat A SHIPPED: now fires `push_public("price", ...)` when invested drift vs the last manual snapshot exceeds the existing `DRIFT_ALERT_THRESHOLD_INVESTED` (₹1,000), via a new `_send_auto_drift_alert` that mirrors only the ntfy half of `_send_drift_alerts`. Rising-edge dedupe: fires only when this snapshot has drift AND the most recent prior auto snapshot did not. Current-value drift intentionally NOT alerted. `drift_invested`/`has_drift`/`alerts_sent` recorded on the auto snapshot. ntfy guarded. Verified on EC2 via a hermetic 4-case harness.** | `app/services/reconciliation.py` ~78-115 | SHIPPED 2026-06-14 (Chat A) — commit `1340396` |
| 26 | P2-6 / TD1 | Decide & implement: direction-aware feedback relabel. Add `"direction": payload.direction` to outcome filter in `submit_feedback`. Folds in TD1's lingering question; may overlap with an F-ticket — confirm via #3 (TD15) before patching. **Chat 5.9 note: TD15 reconciliation found no F-ticket already covering this; stays OPEN.** **Chat A SHIPPED: the literal "add `payload.direction`" would have crashed — `SuggestionFeedback` had only `action` + `note` with `extra="forbid"`. Added `direction: Literal["buy","sell"] = "buy"` (default keeps the current frontend working). The relabel query filters by direction, using the repo's existing back-compat guard `{"$or":[{"direction":"buy"},{"direction":{"$exists":False}}]}` for the buy branch and a bare `{"direction":"sell"}` for sell. Does NOT close TD1/#43 — cross-direction relabel policy stays DEFERRED. Verified on EC2 via a hermetic harness.** | `app/routers/suggestions.py` ~310-325 | SHIPPED 2026-06-14 (Chat A) — commit `6032b64` |

## PHASE 8 — New features per chat split plan — SHIPPED Chats 6–8 (COMPLETE)

Do AFTER Phases 2 + 6 so underlying surfaces are correct. Chats 6 and 7 are independent; Chat 8 (watchlist) came last among these because it multiplies data volume.

| # | Source | Item | Files / surface | Status |
|---|---|---|---|---|
| 27 | Chat 6 / F1 + F3 | Ad-hoc chat about suggestions (F1) + ad-hoc chat about a specific holding (F3). Share `conversations` collection scaffolding. New `POST /chat/suggestions` and `POST /chat/holdings/{isin}` endpoints. Frontend chat surface. **Chat 6 SHIPPED across 4 units + 1 route-shadow fix: Unit 1 `conversation.py` real model + `scope` field + `scope_created_desc` index + `instrument_service.lookup_by_isin`; Unit 2 `conversation_service.ensure_stock_context(isin)` on-demand enrichment (fundamentals/earnings/news via the existing cron-path services, freshness-gated, writes only Phase-2 reference collections); Unit 3 chat service (buy-research framing for not-yet-owned names, position/tax overlay for held names, latest buy+sell run summary for F1; single structured Sonnet `{answer,intent}` call mirroring `dossier_service._generate_one`; Sonnet $3/$15 MTok → Money cost_usd; persist) + `routers/conversations.py` (`POST /chat/suggestions`, `POST /chat/holdings/{isin}` ISIN-validated + 404 on unknown instrument, `GET /chat/history`) + `main.py` include; Unit 4 frontend `ChatPanel` + `StockResearchPanel` (symbol→ISIN via existing `/instruments/search`) + `lib/api.ts` + embeds on `/suggestions` and `/holdings/[isin]`, self-contained markdown renderer (no new npm dep). Path-2 enrichment makes F3 work for ANY known NSE instrument, held or not. Generalized the per-stock endpoint (kept the documented `/chat/holdings/{isin}` path). Surfaced + fixed a latent `/instruments/search` route-shadow (dynamic `/{exchange}/{symbol}` declared first → search unreachable; reordered). Filed #50 (news entity mis-tagging) + #51 (dividend_yield ×100 formatting). End-to-end verified on EC2.** | `routers/conversations.py` (NEW), `services/conversation_service.py` (NEW), `models/conversation.py`, `services/instrument_service.py`, `routers/instruments.py`, `app/main.py`, frontend `components/chat-panel.tsx` (NEW) + `components/stock-research-panel.tsx` (NEW) + `lib/api.ts` + `app/suggestions/page.tsx` + `app/holdings/[isin]/page.tsx` | SHIPPED 2026-06-14 (Chat 6) — backend `5e787c9`, frontend `6093f63` |
| 28 | Chat 7 / F12 + F15 | `/portfolio/risk-summary` (concentration & risk alerts) + `/portfolio/by-tag?tag=X` (tag-based portfolio views). **Chat 7 SHIPPED across 2 backend units + 2 frontend units, design-approved before code, both endpoints READ-ONLY over the existing Phase-1 holdings/price surfaces (no new collections/indexes/deps). Unit 1 (`97041621`): extracted `_annotate_holdings(holdings, latest_prices) -> (annotated, accum)` out of `compute_summary` (behaviour-preserving, gated by a `/portfolio/summary` curl-diff that printed `OK: /summary unchanged`), added `compute_risk_summary` on the SAME helper (no parallel aggregation) — `concentration_by_holding` (every priced holding, desc by %), `concentration_by_sector`, two-tier `alerts` from four module constants (SINGLE_HOLDING WARN 10/HIGH 20, SECTOR WARN 30/HIGH 50, the TOP_MOVERS_LIMIT pattern) + a low-severity `stale_price` data-quality note — and `GET /portfolio/risk-summary` mirroring the `/summary` skeleton. Verified: 2 warn alerts (GAEL 13.67%, LT 10.02%), 28 holdings, 7 sectors; risk top concentration == /summary concentration[0]. Unit 2 (`803e6610`): `GET /portfolio/by-tag?tag=X` — required `tag` (Query min_length=1; missing/empty → 422), `find({"deleted_at": None, "tags": tag})` (exact case-sensitive array membership), annotated via the SAME path as `holdings.list_holdings` so rows render in the existing HoldingsTable, plus tag-scoped totals; unknown tag → zeroed 200; reuses `_to_dec` (no parallel converter). Verified: retail → TATASTEEL count 1 matching cross-check, shape parity with /portfolio/holdings, missing+empty → 422. Unit 3 (frontend): RiskSummary/HoldingsByTag types + getRiskSummary/getHoldingsByTag in lib/api.ts (new types — ConcentrationItem/SectorBucket didn't match), new RiskSummaryCard mounted full-width on the dashboard via an independent useQuery, + Tags nav link. Unit 4 (frontend): dedicated `app/tags/page.tsx` — tag-pill selector from holdings.tags → getHoldingsByTag → tag-scoped totals + reused HoldingsTable. `npm run build`/lint clean via ~/deploy-ui.sh. No new TD/follow-ups.** | `routers/portfolio.py`, `services/portfolio_service.py`, frontend `lib/api.ts` + `components/risk-summary-card.tsx` (NEW) + `app/page.tsx` + `app/tags/page.tsx` (NEW) | SHIPPED 2026-06-15 (Chat 7) — backend Unit 1 `97041621`, Unit 2 `803e6610`; frontend `e14d6a75` |
| 29 | Chat 8 / F13 | Watchlist: `build_universe` becomes NIFTY 100 ∪ watchlist ∪ held − excluded. Extend `refresh_fundamentals.py` AND `fetch_news_for_universe.py` to cover watchlist ISINs. New `/watchlist` CRUD endpoints (reuse `monitored_stocks` with `status="watchlist"`). Frontend watchlist surface. **Chat 8 SHIPPED across 4 units + 1 CORS fix, design-approved before code; must come last among Phase-8 (multiplies data volume; gated after Phase 4 storage + Phase 6 Tavily race). Resolved set-math: `build_universe` = NIFTY 100 ∪ watchlist (deduped by ISIN, watchlist ISINs outside NIFTY 100 resolved from `instruments`); held stays filtered DOWNSTREAM by `filter_universe` exactly as before, so net buy candidates = (NIFTY 100 ∪ watchlist) − held − rejected − acted; the data-refresh universe additionally ∪ held. Unit 1 (`34ff906d`): `MonitoredStockWatchlistPatch` (mirrors `MonitoredStockFeedbackPatch`, extra=forbid, status pinned watchlist, exclude_none on dump) + `build_universe` evolved + `get_watchlist_isins()` single source of truth. NO change to `get_excluded_isins` (watchlist is structurally outside its rejected/tracking scan; flipping rejected→watchlist auto-un-excludes) and NO change to `indexes.py` (monitored_stocks is one-doc-per-ISIN via upsert-on-{isin}; the partial unique index stays scoped to status:tracking). Unit 2 (`a250d001`): new `routers/watchlist.py` — GET list (price-enriched) + GET one + PUT (idempotent create/update, un-excludes a previously rejected/acted ISIN, 404s unknown instrument, seeds identity from instrument, Money→Decimal128) + DELETE (hard, watchlist-only so feedback rows are never nuked); write-before-apply audit (F10) reusing the SAME audit collection via a WIDENED action Literal (`AuditAction` += watchlist_add/update/remove); `main.py` include. Unit 3 (`9857570b`): `refresh_fundamentals.py` + `fetch_news_for_universe.py` fold in watchlist ISINs (the data-volume multiplier — weekly fundamentals + earnings + daily news + 1 Tavily call/run each), reusing `get_watchlist_isins()`; Tavily blast-radius documented as a soft guardrail (no hard cap; degrades safely via the TD33 atomic quota guard). Unit 4 (frontend `58bf6369`): `lib/api.ts` watchlist types + bindings (+ widened MonitoredStocksAuditEntry action/new_status) + new `app/watchlist/page.tsx` (reuses the /tags shell + ui/table primitives; add via searchInstruments→PUT; per-row Remove→DELETE; refetchQueries) + Watchlist nav link on the dashboard. CORS fix (`67704025`): added `PUT` to `main.py` CORSMiddleware `allow_methods` — the browser preflight for the watchlist upsert was rejected (GET/DELETE worked, PUT didn't); curl-from-box doesn't preflight, so it only surfaced in the browser. End-to-end verified on EC2 + browser. No new TD/follow-ups.** | `services/suggestion_engine.py`, `models/monitored_stock.py`, `services/monitored_stocks_audit_service.py`, `app/main.py`, `routers/watchlist.py` (NEW), `scripts/refresh_fundamentals.py`, `scripts/fetch_news_for_universe.py`, frontend `lib/api.ts` + `app/watchlist/page.tsx` (NEW) + `app/page.tsx` | SHIPPED 2026-06-15 (Chat 8) — backend Unit 1 `34ff906d`, Unit 2 `a250d001`, Unit 3 `9857570b`, CORS `67704025`; frontend `58bf6369` |

## PHASE 9 — Cross-cutting cleanup before GO LIVE

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 30 | P2-1 | Replace 9 `datetime.utcnow()` call sites with project's `utcnow()` from `app.models._common`. | `routers/portfolio.py:43`, `services/scoring_service.py:116,813,890`, `services/dossier_service.py:166,192`, `services/fundamentals_service.py:370,485,505` | SHIPPED 2026-06-15 (Chat B) — 11 sites / 5 files (9 listed + 2 `conversation_service` stragglers); verified grep-clean + F3 runtime |
| 31 | P2-8 | Audit every `datetime.now(timezone.utc)` (tz-aware) Mongo write site; replace with `utcnow()`. Add CI lint rule (ruff custom or grep). | `routers/transactions.py:196,245,267`, `services/reconciliation.py:78,~138`, `services/tavily_client.py:50,~55`, `services/price_service.py:155` | SHIPPED 2026-06-15 (Chat B) — tree-wide: 15 write/query sites->`utcnow()` (incl. reconciliation:213 + price_service bulk_get_latest_intraday misses) + 19 `# tz-ok:` annotations + NEW `scripts/check_datetime_hygiene.py` tokenize-based guard; guard PASSED + negative controls fired |
| 32 | P3-2 | Pin `requires-python = ">=3.12,<3.14"` in `pyproject.toml`. | `pyproject.toml` | SHIPPED 2026-06-15 (Chat B) — pin + `uv lock` relock (no dep churn); `uv sync` clean, Python 3.12.3 in range |
| 33 | Review note | Stand up basic pytest harness. Minimum coverage: `_fifo_replay`, `preview_sell`, `validate_replay`, `recompute_holding` idempotency, `submit_feedback` write-before-apply ordering, `take_auto_snapshot` drift math. | `tests/*` (NEW) | SHIPPED 2026-06-26 (Chat B) — hermetic, zero-dependency harness; 28 tests across the 6 targets. NEW `tests/_fakes.py` (in-memory `FakeCollection`: find/find_one+sort, insert_one, update_one $set/$setOnInsert/upsert, delete_one/delete_many, count_documents; $ne/$in/$nin/$exists/$or/$and; `tx()`/`oid()` factories) + `tests/conftest.py` (`fake_db` fixture monkeypatching the `Collections.*` accessors). `recompute_holding` gets FULL DB idempotency (run twice -> stable aggregates + preserved created_at + exactly one active doc). DB-coupled targets monkeypatch `fetch_metadata`/`_get_our_numbers`/`_send_auto_drift_alert`/`log_change` (no Atlas/yfinance/ntfy). No new deps (pytest>=8.3 already in dev group; pythonpath=["."]). Verified on EC2: `uv run python -m pytest tests/ -v` => 28 passed; hygiene guard still PASSED. backend `04fd970` |
| 34 | Ops gap | Audit `/health` endpoint — does it actually ping Mongo or just return 200? Add Mongo + optionally yfinance heartbeat. **Chat A SHIPPED: `/health` already pinged Mongo but hardcoded `{"status":"ok"}` + HTTP 200 even when the ping failed. Now returns 503 + `{"status":"degraded","mongo":"fail"}` on ping failure and 200 + `{"status":"ok","mongo":"ok"}` on success. yfinance deliberately NOT probed on the hot path. Verified on EC2: live `/health` → 200 ok/ok; a forced-`ping()`-False TestClient probe → 503 + degraded.** | `app/main.py` | SHIPPED 2026-06-14 (Chat A) — commit `bd52c6b` |
| 35 | Ops gap | Add ntfy push on `insert_intraday_quotes` exception during market hours. **Chat A SHIPPED: `insert_intraday_quotes` wrapped in try/except — on failure it fires a GUARDED `push_public("errors", ...)` then re-raises so `cron_run` still records the failure heartbeat. Verified on EC2 via a hermetic harness.** | `scripts/refresh_prices_intraday.py` | SHIPPED 2026-06-14 (Chat A) — commit `bd52c6b` |
| 36 | Ops gap | Add `POST /admin/recompute/{isin}` (Tailscale-only). Replaces SSH-shell recovery for stuck holdings (#7 fallback). | `app/routers/admin.py` (NEW) | SHIPPED 2026-07-01 (Chat B) — NEW `app/routers/admin.py` delegates to `holdings_service.recompute_holding` (TD20 advisory lock; no parallel path), registered in `app/main.py`; ISIN pattern-validated (422); `{status: recomputed\|no_active_holding, isin, holding}`, 409 on lock contention; verified on EC2 (idempotent re-run, no-txn -> no_active_holding, OpenAPI surfaced, 28 pytest + hygiene guard green); backend `1ef0ead` |
| 37 | Ops gap | Rehearse Atlas backup → fresh-DB restore for `monitored_stocks` + `suggestion_outcomes` + `digest_deliveries`. Document exact `mongorestore` command in `Project_State.md`. | `docs/Project_State.md` | SHIPPED 2026-07-01 (Chat B) — rehearsed on EC2 against prod Atlas: per-collection `mongodump --db=portfolio --collection=<name>` (this `mongodump` 100.17.0 rejects `--nsInclude`) → `mongorestore --nsFrom 'portfolio.*' --nsTo 'portfolio_restore_test.*' --drop` into a FRESH scratch DB; counts matched prod (2 / 155 / 10), scratch DB dropped, prod `portfolio` untouched (Atlas user `portfolio_app` can create a second DB — no Unauthorized). Exact dump/restore/verify/cleanup runbook (+ real-recovery `--nsInclude` variant) documented in `Project_State.md` Section 4. DOC + OPS only, no `app/` code — code HEAD stays `1ef0ead`. |
| 38 | Ops gap | Switch backend logging to JSON-structured (basicConfig → custom formatter or `python-json-logger`). | `app/main.py` (logging config) | SHIPPED 2026-07-01 (Chat B) — stdlib path chosen (no new dependency): `app/main.py` `logging.basicConfig` -> `JsonLogFormatter` (`logging.Formatter` subclass) + `_configure_logging()` installing one JSON `StreamHandler` on root + `uvicorn`/`uvicorn.error`/`uvicorn.access` (`propagate=False`, no double-logging); one single-line JSON object/record to journald (timestamp UTC ms+Z from `record.created` via `datetime.fromtimestamp` — clean against `check_datetime_hygiene`; level, logger, message, module, func, line; traceback on exc_info; `extra` merged). Verified on EC2 (hygiene PASSED, 28 pytest, JSON app + `uvicorn.access` logs valid, 3 `/health` -> 3 access lines). LAST Chat B row — Phase 9 COMPLETE; backend `8127c6f` |

## PHASE 10 — Chat 9 pre-launch cleanup

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 39 | Chat 9 / F11 | Capital gains pack. New `GET /tax/capital-gains?fy=YYYY-YY` returning STCG/LTCG breakdown per lot for the financial year. Frontend tax view page. | `routers/tax.py` (NEW), `services/tax_service.py` (NEW), frontend tax page | SHIPPED 2026-07-01 (Chat 9) — `fy=YYYY-YY` (default → current IST FY), Indian FY 1 Apr→31 Mar IST (sell-date IST decides the FY); response = summary (STCG/LTCG/total: realized_gain/proceeds/cost/lot_count) + per-lot rows (isin, symbol, name, buy_date, sell_date, quantity, buy_cost, sell_proceeds, gain, holding_period_days, gain_type). NEW `app/services/tax_service.py` replays via `holdings_service._fifo_replay` (single FIFO source of truth — evolved to emit per-disposal `_realized_lots`, popped in `_recompute_holding_impl`), read-only on Phase 1; LTCG = listed equity held strictly >12 calendar months; §49(2C) honored via `manual_demerger` BUY rows in the ledger (`cost_basis_adjustments` NOT re-applied). NEW `app/routers/tax.py` (router-local `_jsonable`, 422 on malformed/non-consecutive fy), `main.py` includes it. Frontend NEW `app/tax/page.tsx` (`/tax` route, FY selector + STCG/LTCG/Total cards + disposals table, printable) + `lib/api.ts` types + `getCapitalGains(fy?)` + `app/page.tsx` Tax nav. NEW `tests/test_tax_service.py`. Verified on EC2: 49 pytest passed, hygiene guard PASSED, `/tax/capital-gains` live (default FY, 422 guards, OpenAPI), `/tax` → 200. Filed bonus/demerger holding-period nuance as #53. backend `32088c9`, frontend `747ae4f` |
| 40 | Chat 9 | Realized P&L UI hide. Backend field stays (FIFO needs it); frontend stops rendering everywhere it currently shows. | Frontend: holdings-table, holding-stats, totals-row, holding-header | SHIPPED 2026-07-03 (Chat C) — frontend-only. The ONLY realized-P&L render site in the UI was the "Realized P&L" StatCard in `components/totals-row.tsx` (`totals.realized_pnl_lifetime` + closed-positions subvalue); removed it + dropped the now-unused `TrendingUp` import + grid 4->3 cols, and shrank the `app/page.tsx` DashboardSkeleton 4->3 to match. `grep -rn realized` at frontend HEAD confirmed no other passive realized-P&L figure renders: `app/tax/page.tsx` (#39 capital-gains) KEPT; sell-sheet realized preview/toast KEPT (sell decision-support); transactions delete-dialog + audit-intro copy KEPT (no figure). Backend `holdings.realized_pnl` field intentionally unchanged (FIFO + #39 read it). Verified on EC2 via `~/deploy-ui.sh` (npm build + lint clean, no unused-import warning). frontend `16fab5ae` |
| 41 | Chat C / TD6 | Wire `holdings.stop_loss`. Intraday stop-loss breach alert on the EXISTING intraday write path + `alerts_log` first writer + read-only stop-loss strip on the holding drill-down. Rising-edge, success-gated dedup, ntfy-only. Reader/writer already sufficient (PATCH whitelist + `get_holding` already carry `stop_loss`); editing stays in `notes-panel.tsx`. Closes TD6. | `services/price_service.py` (NEW `evaluate_stop_loss_alerts`), `app/db/indexes.py` (dedup index `isin_type_sent_desc`), `scripts/refresh_prices_intraday.py` (guarded call after insert), frontend `components/holding-stats.tsx` (read-only strip) | SHIPPED 2026-07-03 (Chat C) — backend `eb964cc8`, frontend `a4b27bd7` |

## PHASE 10.5 — USER ACCEPTANCE REVIEW (second-to-last; gates GO LIVE)

NEW (Chat 8, at the user's explicit request). This is the second-to-last stage. The tool is considered "production-ready" only AFTER this review is complete and all blocking findings are resolved or explicitly deferred. GO LIVE (Phase 11 / #42) MUST NOT start until then.

| # | Source | Item | Files / surface | Status |
|---|---|---|---|---|
| 52 | User request (Chat 8) | Complete end-to-end user acceptance review / UAT. The user (Sahil) personally walks every page, every flow, every cron output, and every endpoint and logs every bug, issue, or change request they want implemented. Each finding is filed as its OWN row in the NEW ITEMS phase (do not renumber existing rows) with a Source of `User review`, then triaged into a chat/bundle. **Gating:** Phase 11 GO LIVE (#42) must NOT start until this review is complete and all blocking findings are resolved or explicitly deferred. Run this in its own dedicated chat (the "User Review chat") AFTER Phase 9 (Chat B) + Phase 10 (Chats 9 + C) so the surface under review is feature-complete. Output: a triaged list of new rows + a go/no-go call on GO LIVE. | Whole product (frontend pages, backend endpoints, cron outputs, digests/alerts); findings land in `docs/master_todo.md` NEW ITEMS + `Project_State.md` | OPEN |

## PHASE 11 — GO LIVE

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 42 | Chat 10 / F7 | One-time real ICICI data import. Wipe + re-import via `scripts/refresh_from_icici.py` wrapper. Default wipe-and-replace scope: ONLY `transactions`, `transactions_staging`, `holdings`. Preserve `monitored_stocks`, `suggestion_outcomes`, `digest_deliveries`, all `instruments_*`, `prices_*`, `news_articles`. **GATED behind Phase 10.5 (#52): do not start until the user acceptance review is complete and blocking findings are cleared or deferred.** | `scripts/refresh_from_icici.py` (NEW or wrapping existing import_orderbooks) | OPEN |

## PHASE 12 — Post-launch deferred work

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 43 | TD1 | Make `monitored_stocks` direction-aware: add `direction` field, dual rows per ISIN. Reconcile with #26 — if that already solved the practical pain, this becomes optional internal cleanup. **Chat A note: #26 added direction-aware feedback RELABEL (a `direction` field on the payload + a direction filter on the outcome query) but did NOT make `monitored_stocks` itself direction-aware (still one row per ISIN, direction-agnostic). This row stays DEFERRED; revisit whether the practical pain is gone now that relabel is direction-correct.** **Chat 8 note: F13 watchlist reuses the same single-doc-per-ISIN model (status="watchlist"); if TD1 is ever taken, the watchlist status interplay with a direction field must be reconciled at that point.** | `models/monitored_stock.py`, `routers/suggestions.py`, `services/suggestion_engine.get_excluded_isins` | DEFERRED |
| 44 | TD3 | Split `dossier_service.valuation_verdict` from single string into `{verdict, rationale}` for cleaner UI. **SHIPPED 2026-08-01 — backend `e79f2309`, frontend `ce7960fa`. Both dossier prompts (buy+sell) now ask for `valuation_verdict` = the label ONLY (deep value/reasonable/fairly priced/premium/overpriced) + a new `valuation_rationale` (the "why"). `_parse_dossier` adds `valuation_rationale` as coerce-and-default (NOT required, so an older prompt never nukes the narrative; blank → "(insufficient data)"). `_empty_dossier` carries `valuation_rationale`. `lib/api.ts` `SuggestionDossier.valuation_rationale?: string`. `suggestion-card.tsx` renders the label (capitalized) + the rationale beneath (muted, guarded by `isAvail()`). +4 hermetic tests. Mirrors the #55 hold-horizon coerce-and-default pattern. Live buy run confirmed: `valuation_verdict: "premium"`, `valuation_rationale: "P/E of 27.8x and P/B of 4.9x are well above typical real estate developer multiples…"`.** | `services/dossier_service.py`, frontend `lib/api.ts` + `components/suggestion-card.tsx` | SHIPPED 2026-08-01 |
| 45 | TD7 | Refactor `CandidateScore` so sell-side groups live as first-class fields instead of flowing through `group_meta`. Removes the buy/sell asymmetry in the data model. | `models/suggestion.py`, `services/scoring_service.py`, `services/explainability.py`, frontend | DEFERRED |

## NEW ITEMS — filed Chat 5.9 / 5.16 / 5.19 / Chat 6 / Chat 9 (#53) / Phase 10.5 #52 round 1 (#54-#69, 2026-07-07) (do not renumber existing rows)

| # | Source | Item | Files / surface | Status |
|---|---|---|---|---|
| 46 | TD21 | Registry-generated crontab migration (the deferred scheduler-architecture work). `CRON_REGISTRY` gains a parseable cron expression per `CronSpec` → new `scripts/render_crontab.py` renders a committed `ops/crontab` → `deploy.sh` installs it + a drift-validation step (`crontab -l` diff vs rendered). Version-controls the schedule and makes TD14-class drift structurally impossible, while keeping process isolation + deploy-safety (explicitly chosen OVER in-process APScheduler, which on the 1 GB t3.micro would let the ~5-min Sunday dossier run compete with the live API and die on every `systemctl restart`). Update the F4 "no silent failures" triad in Project_State §9 when it lands. Its own dedicated chat. | `app/services/cron_heartbeat_service.py`, `scripts/render_crontab.py` (NEW), `ops/crontab` (NEW), `deploy.sh` | OPEN |
| 47 | TD22 | `track_suggestion_outcomes` cron FAILS every weekday (19:45 IST; the 21:00 IST health email shows `track_suggestion_outcomes: 0 success / 1 failure` daily). Distinct from TD14 — surfaced during the Chat 5.9 TD14 investigation. Root-cause the daily failure (read `cron-outcomes.log` at HEAD + the script body) and fix. **Chat A SHIPPED: root cause was a stats-dict key drift. `snapshot_open_outcomes()` returns its count under `active_outcomes` (renamed in Commit A.5 when selection broadened from "open" to all non-expired), but the cron's `main()` still read `stats["open_outcomes"]` → `KeyError` on every run, raised inside the `cron_run` context (at line 27) before any result line flushed. Fix: read `stats["active_outcomes"]` and rename the heartbeat metadata key + print label to match. Safe rename. Reproduced live at `6032b64`, then verified the fix at `4b638e6`.** | `scripts/track_suggestion_outcomes.py`, `app/services/outcome_tracker.py` | SHIPPED 2026-06-14 (Chat A) — commit `4b638e6` |
| 48 | TD36 | Stale-doc cleanup (NEW Chat 5.16, NO CODE): README.md (backend) + `docs/data_flow.md` still describe the Tavily quota as "monthly", but the code is DAILY — `TAVILY_DAILY_CALL_LIMIT`, one `tavily_quota` doc per `date_utc`, resets 00:00 UTC. Correct the "monthly" wording in both files to "daily (resets 00:00 UTC)". Doc-only. **Chat A SHIPPED: corrected all "monthly" → "daily (resets 00:00 UTC)" in both files. A code check also surfaced and fixed in the same commit: data_flow's "resets at 00:00 IST on the 1st" → "00:00 UTC each day", and a README env var that does not exist (`TAVILY_MONTHLY_QUOTA` → the real `TAVILY_DAILY_CALL_LIMIT`, enforced in `tavily_client`). Verified via grep.** | `README.md`, `docs/data_flow.md` | SHIPPED 2026-06-14 (Chat A) — commit `fae6edf` |
| 49 | TD40 | (NEW Chat 5.19, surfaced during #24 verification) The idle `weekly_suggestions_sell` CronSpec carries `expected_weekdays={6}` (Sunday), so `cron_health_check` flags it `MISSING: weekly_suggestions_sell` and sends a REAL anomaly alert (ntfy + email) every Sunday — even though the umbrella `weekly_suggestions` cron (`--direction=both`) covers both buy and sell. False positive. **Chat A SHIPPED (option 1): set the idle spec's `expected_weekdays=set()` so `is_expected_today()` is always False; the spec stays in the registry/dashboard as `expected=False`. Re-read the #23/TD38-hardened file at HEAD and left `_append_fallback` / `count_today_heartbeats_from_fallback` / `_persist` untouched. Verified on EC2.** | `app/services/cron_heartbeat_service.py` (CRON_REGISTRY `weekly_suggestions_sell` spec) | SHIPPED 2026-06-14 (Chat A) — commit `6032b64` |
| 50 | Chat 6 | News entity mis-tagging in `news_articles.entities_isins`: the #27 F3 chat for HDFCBANK (INE040A01034) returned classified articles about TCS and Kenya's Family Bank — i.e. articles tagged with HDFC Bank's ISIN that are not about HDFC Bank. The chat query correctly filters `entities_isins: <isin>`; the defect is upstream, in how `news_fetcher` / `news_classifier` attach `entities_isins` during fetch/classify (likely over-broad Tavily matching or classifier entity attribution). Directly degrades chat (#27) and likely the news_score signal + dossier news block. Investigation-first: sample mis-tagged docs at HEAD, find where the wrong ISIN is attached, then tighten. **Chat 8 note: now also affects watchlist names (F13) — every watchlisted ISIN pulls daily news, so the mis-tagging blast-radius grows with watchlist size.** **SHIPPED 2026-07-26 — backend `28e57f6` (commit 1 of `4a9851c` bundle). Root cause: `_persist_results` stamped the QUERY isin onto `entities_isins` for every result + `$addToSet` accumulated unrelated companies' ISINs via shared URLs. Fix: deterministic `_article_mentions_company(title, summary, symbol, name)` matcher (NSE symbol whole-token OR distinctive company-name token; generic/corporate tokens stripped); `_persist_results` gains `name` param and gates BOTH the insert-path `entities_isins` AND the `$addToSet` on relevance; `fetched_for_*` provenance stays unconditional. NEW `scripts/retag_news_entities.py` backfill (1540 scanned, 187 changed, 873 tags stripped, 115 emptied). NEW `tests/test_news_fetcher.py` (10 hermetic tests). NEW `news_classifier.confirm_entities_llm()` Phase 2b Haiku pass (65 pairs checked, 2 LLM-rescued, 63 rejected, 0 failed batches). GO-LIVE (#42) blocking set drops #50.** | `app/services/news_fetcher.py`, `app/services/news_classifier.py`, `news_articles` | SHIPPED 2026-07-26 |
| 51 | Chat 6 | `dividend_yield` ×100 formatting inconsistency: the #27 chat showed Reliance "Dividend Yield: 46%" (should be ~0.46%). Verified the chat formatter (`conversation_service._fmt_pct`) is byte-consistent with `dossier_service._fmt_pct` (`f"{v*100:.2f}%"`), so this is a PRE-EXISTING app-wide issue, not new to #27 — some yfinance `dividend_yield` values are stored already-as-percent (0.46) while `_fmt_pct` assumes a fraction and multiplies by 100. Decide the canonical unit (store as fraction OR percent) and fix at the source (`fundamentals_service` ingest) and/or both `_fmt_pct` formatters consistently — do NOT diverge the chat formatter from the dossier. Affects the dossier news/fundamentals block and the chat answer. **SHIPPED 2026-08-01 — backend `e79f2309`. Investigation confirmed: the ingest already normalizes `dividend_yield` via `_normalize_dividend_yield` (values >1 are divided by 100, so the stored value is always decimal-fraction; `0.0046 → "0.46%"` is correct). The genuine defect was the TWO SEPARATE `_fmt_pct` functions (one could drift from the other). Fix: promoted a single canonical `fmt_pct` module-level function in `dossier_service.py` (with documentation of the decimal-fraction ingest contract) and made `conversation_service._fmt_pct` an alias (`_fmt_pct = fmt_pct`). The two formatters are now the SAME function object (`fmt_pct is _fmt_pct → True`). No ingest re-thresholding was needed (would risk corrupting correctly-stored values). Frontend renders `dividend_yield` nowhere. 123 pytest, hygiene PASSED.** | `app/services/dossier_service.py`, `app/services/conversation_service.py` | SHIPPED 2026-08-01 |

| 53 | Chat 9 | Bonus/demerger holding-period nuance in the F11 capital-gains view (#39). #39 is read-only on the Phase-1 FIFO replay, so its buy_date/cost for BONUS & DEMERGER lots reflect the ledger encoding: BONUS is modelled by diluting existing lots in place (the original buy `trade_date` is kept) rather than zero-cost lots dated at the allotment, and DEMERGER receipts are `manual_demerger` BUY rows carrying the apportioned §49(2C) cost at the manual-entry date. Under the IT Act, bonus shares have zero cost + holding period from ALLOTMENT, and demerger shares INHERIT the original holding period. So STCG/LTCG classification + `holding_period_days` can be wrong for bonus/demerger lots. Fixing this means changing how Phase-1 FIFO records these events (out of #39 scope, which must stay read-only on Phase 1). Investigate whether to (a) special-case in `tax_service` from event metadata or (b) fix at the Phase-1 FIFO source (`_fifo_replay` bonus/demerger handling). **BROADENED (Phase 10.5 / #52 item 10, 2026-07-07): scope now covers full corporate-action COST-BASIS correctness, not just holding period — BONUS shares = zero cost (allotment-dated), SPLIT = cost-per-share divides across the increased quantity (total cost unchanged), DEMERGER = §49(2C) apportionment + INHERITED holding period. Complete all three in one go at the FIFO source so /tax LTCG/STCG numbers are correct without manual work. Re-tagged BLOCKING.** **SHIPPED 2026-07-21 (backend-only) — SCOPED DOWN after re-reading the ACTUAL ledger encoding at HEAD `eb611a3f`: (1) SPLIT cost is already correct — `_fifo_replay` divides price / scales qty via `corporate_action.ratio_from/ratio_to`, total cost preserved (TATASTEEL 1:10). (2) BONUS cost is already correct — real bonuses are recorded as zero-cost `type="BUY" price=0` rows (RELIANCE/ASHOKLEY/CONCOR/BPCL), NOT the `type="BONUS"` branch (which is dead code for real data); so bonus lots already have zero cost AND their holding period runs from the allotment date, which is the IT-Act-correct treatment. Changing either would corrupt correct data. (3) There is NO `DEMERGER` transaction type — the Literal is `BUY/SELL/DIVIDEND/BONUS/SPLIT`; demergers are `source="manual_demerger"` BUY rows carrying the apportioned §49(2C) cost (doc-drift corrected). The ONE genuine defect was demerger HOLDING PERIOD: a receipt (TMCV, JIOFIN) is a BUY dated at the RECEIPT date, so `tax_service._classify` measured the period from receipt instead of the parent's original acquisition — misclassifying a genuine LTCG disposal as STCG. FIX (Option A): NEW optional `Transaction.acquired_date` (holding-period date ONLY — does NOT change cost basis or FIFO ordering; absent -> `trade_date`); `_fifo_replay._Lot.acquired_date` (defaults to `trade_date`) is emitted as `_realized_lots.buy_trade_date` in the SELL branch — single FIFO source of truth, no parallel path, `tax_service` unchanged (already reads `buy_trade_date`); `add_manual_transactions.py` TMCV + JIOFIN rows carry `acquired_date` as a PLACEHOLDER + `TODO(sahil)` to set the real earliest parent-buy date before #42; 4 new tests (2 fifo_replay + 2 tax_service). Verified on EC2: 53 pytest, hygiene PASSED, `/tax/capital-gains` 200. KNOWN LIMITATION: single inherited date per receipt row — exact for this portfolio's single-block demergers; multi-lot pro-rata inheritance (Option B, full FIFO rewrite) deliberately out of scope. Bundle D's #68 (data-entry/automation front-end for the same corporate-action surface) remains OPEN — the cost-basis math it would drive is already correct at the FIFO source per this row.** | `app/models/transaction.py`, `app/services/holdings_service.py` (`_fifo_replay`), `scripts/add_manual_transactions.py`, `tests/test_fifo_replay.py`, `tests/test_tax_service.py` | SHIPPED 2026-07-21 — demerger holding-period inheritance at the FIFO source (backend `5604840`) |

> User-review findings (Phase 10.5 / #52) will be appended here as additional rows once that chat runs — Source `User review`, do not renumber existing rows.

| # | Source | Item | Files / surface | Status |
|---|---|---|---|---|
| 54 | User review | Independent add-a-holding entry point. Today the only "buy" affordance adds MORE of an already-held stock; there is no way to record a brand-new purchase of a stock not yet held. Add a UI entry point (reuse instrument search → buy-sheet) that creates a new holding via the existing create/transaction path. **SHIPPED 2026-07-25 — frontend-only.** `BuySheet` REPLACED with discriminated `holding`|`instrument` prop; NEW `add-holding-dialog.tsx`; `lib/api.ts` `BuyPayload.isin?`; `app/page.tsx` mounts `<AddHoldingDialog />` in Holdings section. Backend confirmed already sufficient. `tsc --noEmit` clean; `~/deploy-ui.sh` build+lint clean. | frontend `components/buy-sheet.tsx`, `components/add-holding-dialog.tsx` (NEW), `lib/api.ts` | SHIPPED 2026-07-25 |
| 55 | User review | Hold-horizon on buy suggestions. Suggestions say "buy" but not FOR HOW LONG; the holding timeframe is what drives the return, so each buy candidate needs an LLM-authored target horizon / expected hold period (short/medium/long + rationale + review trigger). Must be as accurate as possible → LLM-authored in the dossier. **SHIPPED 2026-07-25 — backend `931943b` + frontend `7324573`. Scoped to the two REAL seams after reading the code at HEAD: `dossier_service.py` (buy prompt + buy branches of `_parse_dossier`/`_empty_dossier`) + frontend (`lib/api.ts` type + `suggestion-card.tsx` render). Four LLM-authored flat keys on the BUY dossier — `hold_horizon` (short\|medium\|long, with prompt bands 1-3mo / 3-12mo / 12mo+ the last aligned to the 12-month LTCG boundary), `hold_horizon_expected_move` (time-bound expected gain that justifies the capital commitment), `hold_horizon_rationale`, `hold_horizon_review_trigger`. Coerce-and-default leniency (keys NOT in `required`; off-list bucket → medium, blank prose → marker) so a horizon glitch never nukes the narrative. NEW `_clamp_sentence` helper (sentence/word-boundary clamp, 400-char budget) replaces the hard `[:300]` slice that had truncated horizon prose mid-word. NO `models/suggestion.py` / `scoring_service.py` / `explainability.py` / `routers/suggestions.py` change — the dossier is a plain dict and `_serialize_run` already passes new keys through; the horizon is LLM-authored in the dossier from the CandidateScore already in the prompt, not a scored signal. Frontend: Clock horizon badge visible without expanding (buy-side only) + "Expected hold horizon" section, `!startsWith("(")` guard; sell cards + old runs unaffected. Plus a #55-follow-up manual "Run now" trigger (see #71). NEW `tests/test_dossier_service.py` (9 tests). Verified on EC2: 62 pytest, hygiene PASSED, live buy run authors valid horizons end-to-end, sell carries none, deploy-ui clean.** | `services/dossier_service.py`, frontend `lib/api.ts` + `components/suggestion-card.tsx` (`scoring_service.py` / `models/suggestion.py` / `explainability.py` confirmed NOT needed — dict passthrough) | SHIPPED 2026-07-25 |
| 56 | User review | Target / sell-price breach alerts so the user can sell actively. Direct mirror of the #41 stop-loss path on the intraday write path: rising-edge fire-once when latest intraday price crosses ABOVE a holding's target_price, success-gated dedup, ntfy + persist to alerts_log. `holdings.target_price` + `alert_on` already exist. **SHIPPED 2026-07-25 — backend `6f1aa57` + frontend `a2ffef1`.** NEW `price_service.evaluate_target_price_alerts(rows)` beside `evaluate_stop_loss_alerts` (EVOLVED the evaluator, no parallel path); `scripts/refresh_prices_intraday.py` calls it guarded on the SAME rows right after the stop-loss block (no parallel price-fetch loop; new `target_alerts_fired` heartbeat key). Fires rising-edge when the latest intraday price crosses strictly ABOVE `target_price`; gated on `target_price` set AND `"target"` in `alert_on` — the token is **`"target"`** (the Holding model default `alert_on=["stop_loss","target","earnings","news","52w_high"]`), NOT `"target_price"` (read the model at HEAD, not the row wording); success-gated dedup (only a DELIVERED `target_hit` suppresses; re-arm on a later at/below tick); **#67 Guard-B analog** — a first-ever fire requires a prior intraday tick at/BELOW `target_price` (proof the price genuinely crossed UP through it), so a garbage target below every observed price never fires; ntfy-only via `push_public("price")`; persists `Alert(alert_type="target_hit", channel="ntfy_public_price", severity="high")` to `alerts_log`. Message `"<SYMBOL> ₹<ltp> crossed above target ₹<tp>"`. **NO `app/db/indexes.py` change** — the existing `alerts_log isin_type_sent_desc` (isin, alert_type, sent_at) is generic across `alert_type`, so `target_hit` reuses it (the row listed indexes.py on the named surface, but reading the code showed it already sufficient — did NOT invent a redundant index). NO `lib/api.ts` change — `Holding.target_price`/`alert_on` already present. Frontend `components/holding-stats.tsx` REPLACED to add a read-only Target strip mirroring the stop-loss strip (target value + `"X% to target"` / `"target reached"` green when `current >= target`; dashed hint otherwise); editing stays in `notes-panel.tsx`. NO hermetic unit test — #41 had none either, and `FakeCollection` lacks `$gt/$lte` + Decimal128 range support (adding it just for this would over-build the harness, a Chat B anti-pattern); verified live on EC2 mirroring #41. Verified on EC2: hygiene PASSED, 66 pytest, `/health` ok/ok; fire→suppress→re-arm functional drive = `1/0/1` with 2 delivered `target_hit` rows + 2 ntfy pushes received, Guard-B garbage-target = 0. | `services/price_service.py` (new target-price evaluator), `scripts/refresh_prices_intraday.py`, ~~`app/db/indexes.py`~~ (no change needed), `alerts_log`, frontend `components/holding-stats.tsx` | SHIPPED 2026-07-25 (backend `6f1aa57`, frontend `a2ffef1`) |
| 57 | User review | News alerts for names the user cares about (holdings + watchlist/flagged). A damaging or highly favourable news item should push an alert so buy/sell decisions can react. Merge "companies I flag / am monitoring" into the watchlist concept. New alert evaluator on the news-fetch path, gated by alert_on, transported via notify + persisted to alerts_log. DEPENDS ON #50 (news entity mis-tagging) being fixed first or alerts fire on wrong-company news. **SHIPPED 2026-07-26 — backend `4a9851c` (second commit of the bundle, on top of the #50 fix). NEW `app/services/news_alerts.evaluate_news_alerts(window_days=3)`: eligible = active holdings with `"news"` in `alert_on` (array-membership) UNION `get_watchlist_isins()` (29 eligible names on EC2); fires on `classified=True, severity="high"`, non-noise themes (not subset of `{"noise"}`), `fetched_at >= now - window_days`; dedup per (isin, article) via `alerts_log.find_one({isin, alert_type:"news_event", delivery_status:"sent", cited_news_ids: article_id})` — success-gated, a failed ntfy never silences a real story, next run retries; ntfy-only `push_public("news", ...)`, tone badge (favourable/damaging/material); persists `Alert(alert_type="news_event", channel="ntfy_public_news", severity="high", llm_reasoning=classifier_summary, cited_news_ids=[article_id], trigger_data.extras={sentiment,themes,url})`. `scripts/fetch_news_for_universe.py` gains Phase 2b `confirm_entities_llm` (guarded) + Phase 3 `evaluate_news_alerts` (guarded, after classify); heartbeat keys `entity_confirm_pairs_checked/confirmed` + `news_alerts_fired/error`. NO model/index/collection/dep change — `AlertType="news_event"`, `channel="ntfy_public_news"`, `cited_news_ids`, `llm_reasoning` already existed on `Alert`; `alerts_log isin_type_sent_desc` covers the dedup; `Holding.alert_on` default already includes `"news"`. Verified on EC2: 29 eligible names, 0 high-sev fresh articles this week (correct), dedup second-run = 0, 0 failed batches on LLM confirm. GO-LIVE (#42) blocking set drops #50 + #57 → remaining: #65, #68, #69.** | `app/services/news_alerts.py` (NEW), `app/services/news_classifier.py`, `scripts/fetch_news_for_universe.py`, `alerts_log` | SHIPPED 2026-07-26 |
| 58 | User review | Dashboard top-nav declutter — too many items in the top tab bar; restructure/group into a cleaner navigation. | frontend `app/page.tsx` header nav (+ shared nav shell) | SHIPPED 2026-07-08 (Bundle A, frontend-only, frontend HEAD e3a9a44b3aaf1597edeeca7500779c0eb2de3361). Grouped the 6 top-nav links into two dropdown menus — Research (Suggestions/Watchlist/Tags) + Manage (Transactions/Audit/Tax) — keeping ReconciliationBadge/RefreshButton/ThemeToggle inline (9->5 header controls); reused the existing dropdown-menu primitive, no new dep. `app/page.tsx`. |
| 59 | User review | Audit page has no back-to-dashboard / back-to-portfolio navigation — the flow is broken (user gets stranded on `/transactions/audit`). Add the standard back link/shell that other pages carry. | frontend `app/transactions/audit/page.tsx` | SHIPPED 2026-07-08 (Bundle A, frontend-only, e3a9a44b3aaf1597edeeca7500779c0eb2de3361). Added a standard "Back to portfolio" (-> /) ghost link to `app/transactions/audit/page.tsx` alongside the existing "Back to transactions" — user no longer stranded. |
| 60 | User review | Multi-recipient email + Tailscale access management for additional people (e.g. the user's financial advisor) to view/access the tool with minimal complexity. DOC/PREP ONLY for now — produce onboarding + access documentation; no code this cycle. | NEW ops doc; later `RESEND_TO`/digest recipient list, Tailscale ACL notes | OPEN — non-blocking (doc/prep only) |
| 61 | User review | One-time deploy script for other people to stand up their own instance. DOC/PREP ONLY for now — prepare the script outline + onboarding documentation; no code this cycle. | NEW `scripts/`/`ops/` deploy helper + onboarding doc (generalize `deploy.sh`) | OPEN — non-blocking (doc/prep only) |
| 62 | User review | Collapsible dashboard sections — dropdown arrows to show/hide each section of the dashboard. | frontend `app/page.tsx` + section components (`totals-row`, `sector-breakdown`, `top-movers`, `recent-activity-card`, `risk-summary-card`) | SHIPPED 2026-07-08 (Bundle A, frontend-only, e3a9a44b3aaf1597edeeca7500779c0eb2de3361). NEW `components/collapsible-section.tsx` + wrapped the four dashboard sections (Portfolio totals, Allocation & movers, Risk, Holdings) in default-expanded, per-session show/hide toggles; `app/page.tsx`. No new dep. |
| 63 | User review | Show dividends in the realised-P&L / total-return figure. Dividends are money made and count as profit from a value-proposition perspective. COMPLIANCE NOTE: dividends are "income from other sources," NOT capital gains — they must be reflected in realised P&L / total return ONLY, and must NOT enter the STCG/LTCG `/tax` capital-gains computation. | `services/holdings_service.py` (realized_pnl / total-return), reconciliation, frontend totals; `/tax` view intentionally untouched | SHIPPED 2026-07-21 — `portfolio_service.compute_summary` extended to sum `total_dividends_received` across the existing all-holdings scan, exposing `totals.total_dividends_lifetime` + `totals.total_realized_with_dividends`; `realized_pnl_lifetime` stays capital-only; `holdings_service._fifo_replay` already persisted `total_dividends_received` (no FIFO change); `routers/portfolio.py` empty-portfolio zero-block extended; `lib/api.ts` `PortfolioTotals` extended; tax_service + /tax untouched (compliance). backend `2792cefa`, frontend `3cde0d39` |
| 64 | User review | Re-show the realised P&L figure on the dashboard (REVERSES #40, which hid it 2026-07-03). Backend field was never removed, so this is a frontend re-enable, extended by #63 to include dividends. | frontend `components/totals-row.tsx`, `app/page.tsx` DashboardSkeleton (3→4) | SHIPPED 2026-07-21 — `components/totals-row.tsx` REPLACED: re-added the "Realized P&L" StatCard; headline = `total_realized_with_dividends` (total return), subtext = `Capital: … / Dividends: … · N closed` (compliance split visible); grid 3→4; re-imported `TrendingUp`; tooltip explains dividends-are-NOT-capital-gains + links to /tax. `app/page.tsx` DashboardSkeleton 3→4 cards. Verified: deploy-ui build+lint clean, 4-card totals row renders. frontend `3cde0d39` |
| 65 | User review | Capture & flag corporate dividend announcements and build a realised-P&L reconciliation drift matrix (announced vs received vs booked). Ties to #63/#64. | NEW dividend-announcement capture (yfinance corp actions), `services/reconciliation.py`, `app/routers/reconciliation.py`, frontend reconciliation page | **SHIPPED 2026-07-27 — backend `1f390c0` (Unit 1 `2df217d`, Unit 2 `d8b9d35`, Unit 3 `1f390c0`) + frontend `92a8974` (`f5d8e2b`).** Dividends count toward actual gains (feed `total_dividends_*` via #63/#64) though not taxable capital gains, so an announced-and-ex-but-never-recorded payout understates realised gain. AUTOMATED capture via BOTH sources per the "use the news tooling" directive: (a) yfinance corp-actions = structured "announced" truth — NEW `app/models/dividend_announcement.py` (`DividendAnnouncement`, one doc per `(isin, ex_date)`, `amount_per_share: Money`; mirrors `earnings_event.py`), NEW `Collections.dividend_announcements()`, NEW indexes `(isin,ex_date)` unique / `ex_date_desc` / `isin`, `fundamentals_service.fetch_dividends_yfinance`/`refresh_dividends_for`/`refresh_dividends_universe`/`get_dividend_announcements_for_isin` (`Ticker.dividends`, replace-window-400d upsert, mirrors the earnings refresh — same provider/universe/idempotency), `scripts/refresh_fundamentals.py` guarded dividend refresh on the SAME universe after the earnings block (best-effort, never affects exit code); (b) the #50/#57 news pipeline `corporate_action` theme = corroboration (the classifier does NOT extract structured amounts — verified by reading it — so news FLAGS, yfinance supplies the number). Matrix: `reconciliation.compute_dividend_drift()` PURE read joining announced × recorded `DIVIDEND` txns × booked `total_dividends_received` per held name → `matched`/`missing_receipt`/`pending` (`±21d` match window; `missing_receipt` only when ex-date passed ≥21d settle margin AND held across it via `first_purchased_at<=ex_date` — point-in-time FIFO reconstruction deliberately out of scope for round 1), news corroboration reuses the #50 entity-accurate query. Nudge: `evaluate_dividend_drift_alerts()` ntfy-only on the `price` channel for NEW `missing_receipt`s, rising-edge deduped via `type="dividend_drift_marker"` docs in `reconciliation_snapshots` (`dividend_missing:<isin>:<ex_date>`), success-gated (marker only after a delivered push), FLOOD-CAPPED (`_DIVIDEND_NUDGE_INDIVIDUAL_CAP=3` individual + 1 summary — 40 missing → 4 pushes, markers written for all 40 so the count is exact and never re-floods); mirrors `_send_auto_drift_alert` — NO new AlertType / `alerts_log` write / new collection. NEW `GET /reconciliation/dividend-drift` (read-only; NOT a tax view — `tax_service`/`/tax` untouched, compliance). `scripts/take_reconciliation_snapshot.py` guarded caller in the daily reconciliation cron + `dividend_drift_nudges` heartbeat (rides the reconciliation cron, not the news cron). NEW `tests/test_dividend_drift.py` (11 hermetic); `FakeCollection` +`$gte/$gt/$lte/$lt`+array-multikey equality (existing 76 unaffected); `fake_db` +`dividend_announcements`/`news_articles`. Frontend `lib/api.ts` `DividendDriftStatus`/`DividendAnnouncementDrift`/`DividendDriftRow` + `getDividendDrift()`; `app/reconciliation/page.tsx` read-only Dividend-drift card (per-stock ex-date/per-share/est-received + status badge, missing-count banner, corporate-action-news icon with shadcn `Tooltip` + always-visible legend; reuses existing useQuery+shadcn+`lib/format`, no new dep). Verified on EC2: 87 pytest, hygiene PASSED, endpoint 44 announcements / 40 missing across 29 held names, nudge 4-then-0, `/health` ok, deploy-ui build+lint + browser smoke. No new dep, only the one new collection. GO-LIVE (#42) blocking set drops #65 → remaining: #68, #69. |
| 66 | User review | Daily 9pm (21:00 IST) heartbeat health-check has produced NO email and NO push since 2026-06-14. LIKELY EXPLANATION: `cron_health_check` is anomaly-only by design (silent on healthy days); the daily email the user used to receive was the #47/TD22 false-positive (`track_suggestion_outcomes` failing daily), fixed on 2026-06-14 — dates match exactly. User actually wants a POSITIVE daily "all N crons healthy" heartbeat (behavior change), AND a verification that the health check itself still fires on real anomalies. Investigation-first. | `scripts/cron_health_check.py`, `services/cron_heartbeat_service.py`, crontab, `services/notify.py` (email + push_public) | SHIPPED 2026-07-08 — INVESTIGATION confirmed cron_health_check is anomaly-only by design (docstring "silent success"); the pre-2026-06-14 daily mail was the #47/TD22 false-positive, fixed 2026-06-14 (dates match), so the silence was CORRECT. Fix: POSITIVE daily heartbeat added to the healthy branch of `scripts/cron_health_check.py` (unconditional, no crontab/argparse change) — low-priority ntfy on the `errors` channel + a Resend email listing the N expected crons healthy today; anomaly + self-failure paths + own heartbeat write byte-unchanged; best-effort (never raises). Verified live on EC2 (email + ntfy received; healthy branch fires at the real 21:00 IST slot). backend `beaadf70`, frontend unchanged. |
| 67 | User review | Spurious stop-loss alerts fired: "GAEL 164.00 crossed below stop loss 1000.00", "OLAELEC 44.07 crossed below stop loss 212312", "RELIANCE 100 crossed below stop loss 200". Stop values are implausible (1000 / 212312 / 200) and RELIANCE at ₹100 is not a real price — points to test/seed-data pollution AND a missing sanity guard (a stop set at/above current price fires on the first tick). #42 wipe-and-replace clears bad data, but add a guard + verify #41 rising-edge/success-gated dedup. Investigation-first. Regression against shipped #41. | `services/price_service.py` (`evaluate_stop_loss_alerts`), `scripts/refresh_prices_intraday.py`, `holdings` data hygiene | SHIPPED 2026-07-08 — INVESTIGATION confirmed #41's evaluator fired CORRECTLY on garbage `stop_loss` (GAEL stop 1000 vs ~161, OLAELEC stop 212312 vs ~42); NOT a #41 regression. No RELIANCE holding/alert exists in the DB, so no price-feed bug. Fix: Guard B in `price_service.evaluate_stop_loss_alerts` — a first-ever fire (no prior delivered alert) now REQUIRES proof of arming (some intraday tick at/above `stop_loss`, reusing the existing `intraday >= stop_loss` lookup); garbage stops never armed -> suppressed; TATASTEEL (stop 123 < price 189) unaffected. Verified on EC2: 49 pytest, arm-proof GAEL/OLAELEC=False & TATASTEEL=True, `stop_loss_hit` rows before==after==2 (zero spurious fires). Garbage stops also cleared by the #42 wipe. backend `beaadf70`. |
| 68 | User review | Automate corporate-action processing (demerger / split / bonus) in the transactions section: a UI + backend flow that records the event once and auto-calculates + maps the effect onto holdings and cost basis (e.g. the TMPV/TMCV demerger) so future events need no heavy manual work and the numbers stay accurate. Overlaps broadened #53 (cost-basis math) — this is the DATA-ENTRY/automation front-end for it. Should also auto-create the 49(2C) `cost_basis_adjustment` for demergers. **SHIPPED 2026-07-27 — backend `f8778c8`, frontend `4031a80`. Investigation-first re-read of the actual surfaces at HEAD `c7026a4e` scoped this to a pure DATA-ENTRY front-end: the FIFO cost math is already correct (#53), and the transactions router had no create endpoint (BUY/SELL creation lives on the holdings router), so a corp-action recorder is a genuinely new endpoint that produces the SAME ledger rows `scripts/add_manual_transactions.py` produces, then recomputes. NEW `app/services/corporate_action_service.py` — pure, DB-free builders: `build_split_row` (`type="SPLIT"`, meaning in `corporate_action` ratios), `build_bonus_row` (zero-cost `type="BUY" price=0` — the real-ledger pattern, NOT the dead `type="BONUS"` branch), `build_demerger_child_row` (`source="manual_demerger"` child BUY with apportioned §49(2C) `price` + inherited `acquired_date`), `compute_bonus_quantity`, `compute_demerger_cost_split`, `compute_parent_reprice`. NEW `POST /transactions/corporate-action` (`RecordCorporateActionRequest` discriminated on `action_type` split/bonus/demerger; idempotent on `source_ref`; `validate_replay` guard on split; bonus qty from the live holding OR an explicit override for odd broker allotments like CONCOR 1-per-6; demerger inserts the child BUY, auto-creates the §49(2C) `cost_basis_adjustments` doc, and RETURNS the parent BUY-row reprice as an AUDITED follow-up — apply via `PATCH /transactions/{id}` — rather than silently mutating immutable BUY rows, preserving the immutability invariant; recompute mirrors the add_buy/sell `recorded_with_warning` contract). NO `holdings_service`/`_fifo_replay` change (math already correct), NO new collection/index/dependency, `/tax` + `tax_service` untouched (compliance). NEW `tests/test_corporate_action.py` (18 hermetic: pure builders + endpoint wiring via the FakeCollection harness); `tests/conftest.py` `fake_db` gains `cost_basis_adjustments`. Frontend NEW `components/corporate-action-dialog.tsx` (a "Record corporate action" dialog mounted in the transactions header next to "View audit log"; action-type selector + per-type fields, client validation, synchronous `refetchQueries` fan-out; a demerger surfaces the §49(2C) adjustment + parent-reprice follow-up toast) + `lib/api.ts` `CorporateActionType`/`CorporateActionPayload`/`ParentRepriceInstruction`/`CorporateActionResponse` types + `recordCorporateAction()` binding + `app/transactions/page.tsx` mount. Verified on EC2: 105 pytest (87 + 18), hygiene PASSED, `/transactions/corporate-action` on the OpenAPI surface, bad-ISIN → 422, split-with-no-ratios → 400, `~/deploy-ui.sh` build+lint clean, `/transactions` → 200, `tsc --noEmit` clean. Bundle D fully SHIPPED (#53 + #68). GO-LIVE (#42) blocking set drops #68 → remaining: #69 (round-2 review, LAST).** | frontend `app/transactions/page.tsx`, `app/routers/transactions.py`, NEW `app/services/corporate_action_service.py`, `cost_basis_adjustments`; NO `services/holdings_service.py` (`_fifo_replay`) change | SHIPPED 2026-07-27 |
| 69 | User review | Second user-acceptance review round after the round-1 blocking findings are cleared — walk the whole tool again to catch regressions and any new bugs/fixes before GO LIVE. Same protocol as #52; gates #42. **CLOSED 2026-08-01 — all eight #72–#79 fix bundles SHIPPED + #44 + #51 SHIPPED + round-3 sanity pass PASSED + GO/NO-GO = GO. #42 GO-LIVE is UNBLOCKED.** | Whole product; findings in NEW ITEMS #72–#79 | SHIPPED 2026-08-01 (round-3 sanity PASSED; GO/NO-GO = GO) |
| 70 | #53 follow-up | **DATA ACTION (must be done before the #42 GO-LIVE import): set the real `acquired_date` on the two demerger rows.** #53 shipped the demerger holding-period-inheritance MECHANISM but seeded the two `source="manual_demerger"` rows in `scripts/add_manual_transactions.py` with a PLACEHOLDER `acquired_date` (`datetime(2022, 1, 1, ...)`) because the true parent acquisition dates live in the ICICI order book, not in the script. Until these are set to the real dates, TMCV and JIOFIN capital-gains STCG/LTCG classification (`/tax`) uses the placeholder 2022-01-01, which may be wrong. **EXACT STEPS: (1) Find the earliest TMPV (`INE155A01022`) BUY `trade_date` and the earliest RELIANCE (`INE002A01018`) BUY `trade_date` — from the ICICI order book / demat statement, OR from Mongo after an import: `mongosh "$MONGODB_URI" --quiet --eval 'db.getSiblingDB("portfolio").transactions.find({isin:{$in:["INE155A01022","INE002A01018"]},type:"BUY",deleted_at:null},{isin:1,symbol:1,trade_date:1,_id:0}).sort({trade_date:1})'`. (2) On the Mac, edit `scripts/add_manual_transactions.py`: in the TMCV row (source_ref `DEMERGER_TATAMOTORS_2025:31.15pct_to_TMCV`) replace the `acquired_date` placeholder with the earliest TMPV BUY date; in the JIOFIN row (source_ref `DEMERGER_JIOFIN_2023:1to1_from_RELIANCE`) replace it with the earliest RELIANCE BUY date. Keep the `tzinfo=timezone.utc`, e.g. `datetime(2021, 3, 15, tzinfo=timezone.utc)`. Remove the `# PLACEHOLDER` marker + the `TODO(sahil)` comment on each once real. (3) Commit + push. (4) These rows only take effect through the staging → promote flow, so re-seed as part of the #42 GO-LIVE import: `import_orderbooks.py` (wipe+reload staging) → `add_manual_transactions.py` (re-append with the corrected dates) → `reconcile_staging.py` → `promote_staging.py --confirm --wipe-live`. If GO-LIVE has ALREADY happened, instead patch the two live rows directly then recompute: `mongosh` `db.transactions.updateOne({isin:"INE1TAE01010",source:"manual_demerger"},{$set:{acquired_date:ISODate("<real TMPV date>")}})` and the same for `INE758E01017` (RELIANCE date), then `curl -X POST http://localhost:8000/admin/recompute/INE1TAE01010` and `.../INE758E01017` (#36 admin endpoint). (5) Verify: `curl -sS "http://localhost:8000/tax/capital-gains?fy=<FY of the sell>"` and confirm TMCV/JIOFIN disposals show the inherited (earlier) `buy_date` and the correct STCG/LTCG `gain_type`. NOTE the #53 single-inherited-date limitation: if a parent was bought across multiple dates, the earliest date is a conservative approximation — exact only for single-block holdings (TMCV from one TMPV block, JIOFIN from one RELIANCE block). **SHIPPED 2026-07-25 (backend `6f1aa57`, part of the #56 commit).** Read the ICICI order-book CSVs (2022-2026) directly: earliest TMPV (`INE155A01022`) BUY = **18-Oct-2023** (5 sh @ ₹661.95); the 100-sh TMPV parent block was actually THREE lots — 5 sh 18-Oct-2023 + 22 sh 05-Jun-2024 + 73 sh 02-Dec-2024 — so `acquired_date` carries the EARLIEST (LTCG-favourable) date onto the whole 100-sh TMCV receipt = the accepted multi-lot approximation (exact per-lot inheritance = deferred Option B). Earliest RELIANCE (`INE002A01018`) BUY = **25-Jul-2022** (5 sh @ ₹2423.70); single-block parent (all 5 JIOFIN from that one buy; the 2024 1:1 bonus post-dates the 20-Jul-2023 demerger and is NOT the parent) → JIOFIN's inherited date is EXACT. Set TMCV `acquired_date=datetime(2023,10,18,tzinfo=timezone.utc)`, JIOFIN `acquired_date=datetime(2022,7,25,tzinfo=timezone.utc)`; removed the `# PLACEHOLDER` + `TODO(sahil)` markers on both. No schema/behaviour change (covered by the existing green pytest); takes effect through the #42 GO-LIVE staging→promote re-seed. | `scripts/add_manual_transactions.py` (TMCV + JIOFIN `acquired_date`); optionally live `transactions` + `POST /admin/recompute/{isin}` | SHIPPED 2026-07-25 (backend `6f1aa57`) |
| 71 | #55 follow-up (User request) | Manual "Run now" trigger for the suggestions pipeline, so fresh dossiers/horizons can be generated mid-week without waiting for the Sunday cron. Fire-and-forget so the ~5-min pipeline never blocks the single-worker sync request. **SHIPPED 2026-07-25 — backend `90c23f7`/`931943b`, frontend `b095522`/`7324573`. NEW `POST /suggestions/run?direction=buy|sell` returns 202 and enqueues `suggestion_engine.run_suggestions(run_type="manual", notify=False, direction=)` — the SAME entry point the Sunday cron uses (no parallel pipeline) — on FastAPI BackgroundTasks; `notify=False` so no digest email/ntfy and no `suggestion_outcomes` rows (mirrors the script's `--no-notify` manual default). NEW `GET /suggestions/run/status?direction=` returns `{running}` for polling. NEW `suggestion_run_locks` collection (`_id==direction`, `started_at`) + 900s TTL index provides a per-direction fail-fast advisory lock — a double-click or an overlapping cron gets 409, not a stacked pipeline; the TTL reclaims a crashed run; mirrors the TD20 recompute_locks primitive keyed by direction not ISIN. Both routes on the existing `/suggestions` router (static paths before the dynamic `/{isin}` routes). Frontend `lib/api.ts` `triggerSuggestionRun`/`getSuggestionRunStatus` + `ManualRunResponse`/`ManualRunStatus`; `app/suggestions/page.tsx` "Run now" button (header + empty-state, direction = active tab) with 10s status polling that refetches the latest run + toasts when it finishes; toast copy avoids the misleading "poll the endpoint" wording. Verified on EC2: `POST` 202 then immediate second `POST` 409, `suggestion_run_locks` TTL index created, a fresh buy run completed (~4.5 min) and populated `hold_horizon` end-to-end.** | `app/routers/suggestions.py`, `app/db/client.py` (`suggestion_run_locks` accessor), `app/db/indexes.py` (TTL index), frontend `lib/api.ts` + `app/suggestions/page.tsx` | SHIPPED 2026-07-25 |
| 72 | User review (#69, round 2) | **BUNDLE U1 — Corporate-action (#68) data-integrity.** Highest-stakes: money math + immutable-ledger invariant. Reviewed at backend `38f43c1` / frontend `4031a80`; each sub-finding verified against the actual code. **(U1-a) Idempotency is effectively OFF.** `RecordCorporateActionRequest.source_ref` defaults to `""`, and `transactions.py::_corp_action_exists` returns `False` whenever `source_ref` is falsy (`if not source_ref: return False`). AND there is NO unique index on `transactions.source_ref` (confirmed: `indexes.py` has only a plain non-unique `source_ref` index; `crontab -l` + Q5 verified). So a double-click / retry on the Record-corporate-action dialog inserts a DUPLICATE ledger row with no guard. A duplicated SPLIT re-scales every lot again (1:2 applied twice = 4×) → silent cost-basis + quantity corruption at the FIFO source. **(U1-b) TOCTOU even with a source_ref.** `_corp_action_exists` check → `insert_one` is not atomic and not backstopped by a unique index, so two concurrent same-`source_ref` requests can both insert. **(U1-c) Demerger swallows `recorded_with_warning`.** The demerger branch calls `_finish_corp_action(child_isin, ...)` but DISCARDS its return and hardcodes `{"status":"recorded"}`; SPLIT/BONUS correctly `return _finish_corp_action(...)`. A failed child recompute is reported as full success (breaks the TD19 contract for the highest-complexity action). **(U1-d) `compute_demerger_cost_split(parent_quantity=...)` is fed `req.child_quantity`** (call site: `parent_quantity=req.child_quantity  # 1:1 receipt`). Numerically correct ONLY for 1:1 demergers; the §49(2C) audit `calculation`/`broker_treatment`/`our_treatment` strings then MISLABEL the parent block, and the param name invites a wrong number for any non-1:1 demerger. **(U1-e) Dialog client-validation NaN gaps** (`corporate-action-dialog.tsx`): `Number("abc") <= 0` is `false` and `NaN >= 1` is `false`, so a non-numeric `bonus_quantity` / `child_quantity` / `parent_total_cost` / `child_cost_pct` passes client validation and is POSTed as a bad string (backend 422, or bad Decimal parse on §49(2C) money math). **(U1-f)** the dialog never sends `exchange` or `it_act_section` (both optional in the payload type; backend defaults exist, so conditional — resolve whether needed for dual-listed instruments). **(U1-g)** the demerger success toast interpolates `resp.child_cost_per_share` / `resp.parent_retained_factor` without an availability guard → can render `₹undefined/sh`. **(U1-h) Mutation fan-out gap:** the dialog's `onSuccess` refetches `["transactions"]`+parent+child holding+`["dashboard"]` but NOT `["cost-basis","adjustments"]` (a demerger AUTO-CREATES a §49(2C) `cost_basis_adjustment`) nor `["reconciliation"]` (quantities/cost changed) → those views stay stale after a corp action. ESTABLISHED FACTS RESPECTED: FIFO math already correct (do not touch `_fifo_replay`); `/tax` untouched; SPLIT/BONUS cost + demerger holding-period already correct. | `app/routers/transactions.py` (`_corp_action_exists`, `record_corporate_action` demerger branch), `app/services/corporate_action_service.py` (`compute_demerger_cost_split` param name), `app/db/indexes.py` (source_ref uniqueness), frontend `components/corporate-action-dialog.tsx` (validation + fan-out + toast) | SHIPPED 2026-08-01 — backend `e79f2309`, frontend `ce7960fa`. U1-a/b: partial-unique index `source_ref_unique_nonempty` on `transactions.source_ref` + `_resolve_source_ref` auto-derives a deterministic key when caller omits one; VERIFIED LIVE (duplicate POST → `already_recorded`, one SPLIT row). U1-c: demerger propagates `recorded_with_warning`. U1-d: param renamed `child_quantity`. U1-e: NaN-safe `isPosNum`/`isFraction` validators. U1-f: `exchange` sent in payload. U1-g: toast guards undefined fields. U1-h: fan-out adds `["cost-basis"]`+`["reconciliation"]`. +2 tests (non-1:1 demerger, auto-source_ref idempotency). 107 pytest (at time of bundle). |
| 73 | User review (#69, round 2) | **BUNDLE U2 — Alert & notification reliability.** All alert evaluators fire real pushes; these are silent-swallow / spurious-fire / re-fire risks. **(U2-a) Audit row written AFTER the push** in `price_service.evaluate_stop_loss_alerts` + `evaluate_target_price_alerts` + `news_alerts.evaluate_news_alerts`: order is `push_public(...)` → `Alert(...)` → `alerts_log.insert_one`. If the insert (or `Alert(...)` validation, e.g. a malformed 12-char ISIN in news alerts) throws AFTER the push, the delivered push has no `delivery_status:"sent"` row → the dedup lookup finds nothing → the SAME push re-fires every 15-min tick (stop/target) or next run (news). Duplicate real pushes to the phone; news-alert exception also aborts the rest of the per-article loop. **(U2-b) Tavily first-call-of-day race → false "quota exceeded".** `tavily_client._increment_quota` maps ANY `DuplicateKeyError` to `TavilyQuotaExceeded`; two concurrent first-calls of a UTC day both miss the filter and both attempt the upsert-insert, and the loser's benign `DuplicateKeyError` (calls_today=1, not the cap) is misreported → with `stop_on_quota_exceeded=True` this ABORTS the whole Sunday news run. **(U2-c) Guard-B arm probe is an unbounded all-time scan** (`intraday.find_one({price:{$gte:stop}})` with no `captured_at` lower bound): a single ancient tick above the stop (or below the target) permanently "arms" a garbage stop/target so it can fire on the first real tick; and because `prices_intraday` has a 90-day TTL, whether a stop is "armed" silently changes over time as old ticks age out. **(U2-d) Dividend-drift marker write not guarded** (`reconciliation.evaluate_dividend_drift_alerts`): `_write_dividend_drift_marker` does a bare `insert_one`; if it throws after a push already fired, the exception aborts the cron mid-batch AND the pushed dividend has no marker → re-pushes next run. **(U2-e) `insert_intraday_quotes` re-raises on any bad row** (confirmed: the intraday cron wraps insert in try/except but RE-RAISES), so a single BSON/duplicate error suppresses BOTH the stop-loss and target evaluators for that tick (they run after the insert). ESTABLISHED FACTS RESPECTED: advisory-only; success-gated dedup is the intended design (these are gaps in it, not a redesign). | `app/services/price_service.py` (evaluators — push-then-insert ordering, arm probe), `app/services/news_alerts.py` (push-then-insert + ISIN validation), `app/services/reconciliation.py` (`evaluate_dividend_drift_alerts` marker write), `app/services/tavily_client.py` (`_increment_quota` DuplicateKey mapping), `scripts/refresh_prices_intraday.py` (insert re-raise vs eval) | SHIPPED 2026-08-01 — backend `e79f2309`. U2-a: Alert built+validated BEFORE push in both evaluators + guarded post-push insert. U2-b: `_increment_quota` retries once on DuplicateKeyError (first-call-of-day race → real increment, not false quota). U2-c: Guard-B arm probe bounded to 30-day window (`_ARM_WINDOW_FLOOR`). U2-d: `_write_dividend_drift_marker` guarded — returns bool, a failed write logs+re-nudges. U2-e: intraday insert failure no longer suppresses evaluators; re-raise happens AFTER both evaluators run. |
| 74 | User review (#69, round 2) | **BUNDLE U3 — Dividend-drift (#65) correctness.** The matrix understates/oversates missed dividends in real edge cases. **(U3-a) Window-wipe on transient yfinance failure.** `fundamentals_service.refresh_dividends_for` runs `delete_many({isin, ex_date>=floor})` UNCONDITIONALLY before checking results, and `fetch_dividends_yfinance` returns `[]` for BOTH "no dividends" AND "fetch failed" (both logged, not raised). So a flaky yfinance call ERASES real recent announcement history for a name for a week → `compute_dividend_drift` sees no announcement → a genuinely-missed dividend is NOT flagged (silently understates realised gain — the exact thing #65 exists to catch). Same delete-then-insert non-atomicity in `refresh_earnings_for`. **(U3-b) "Held on ex-date" heuristic ignores `quantity>0`.** The #65 docstring claims `quantity>0`, but `_held_names()` filters only `{deleted_at:None}`; a fully-exited-but-not-soft-deleted position (qty 0) with `first_purchased_at <= ex_date` still classifies as `missing_receipt` → spurious "unrecorded dividend" nudge. **(U3-c) `expected_amount` uses CURRENT quantity, not ex-date quantity** → the "~₹X received" figure in the push is wrong if the position was trimmed/added since the ex-date. **(U3-d) Same-ex-date dividends collide** — the `(isin, ex_date)` upsert key means two payouts on one ex-date (special+ordinary, or a correction) overwrite rather than sum → wrong announced amount. **(U3-e) N+1 on every render** — `compute_dividend_drift` is called by the endpoint on every reconciliation-page render and issues ~4 Mongo queries per held name (announcements + DIVIDEND txns + booked find_one + corporate-action news); ~120 queries for a 30-name portfolio (perf, not correctness). ESTABLISHED FACTS RESPECTED: dividends are income NOT capital gains, `/tax` untouched; `/reconciliation/dividend-drift` stays NOT-a-tax-view. | `app/services/fundamentals_service.py` (`refresh_dividends_for` delete-before-check, `fetch_dividends_yfinance` empty-on-error, same-ex-date upsert), `app/services/reconciliation.py` (`_held_names` qty filter, `compute_dividend_drift` expected_amount + N+1) | SHIPPED 2026-08-01 — backend `e79f2309`. U3-a: `fetch_dividends_yfinance` returns `None` on failure (not `[]`); `refresh_dividends_for` bails before `delete_many` on None (window preserved); universe loop counts failed fetches as failed. U3-b: `_held_names` adds `quantity: {$gt: 0}` filter. U3-c: `expected_amount` flagged `expected_basis: "current_quantity"` (documented estimate). U3-d: same-ex-date amounts summed before upsert. U3-e: booked dividends from `_held_names` projection (drops per-name find_one); `_corporate_action_news_isins` batches the news corroboration into ONE query. +2 tests; `$in`/`$nin` FakeCollection extended for multikey-intersect. VERIFIED on EC2: `/reconciliation/dividend-drift` returns 29 names, `expected_basis` field present. |
| 75 | User review (#69, round 2) | **BUNDLE U4 — Cron observability & scheduling.** F4's "no silent failures" promise has holes. **(U4-a) `refresh_fundamentals.py` heartbeat closes before any work.** VERIFIED at HEAD: the `with cron_run("refresh_fundamentals") as hb:` block contains ONLY three `hb.metadata[...]` assignments; `refresh_universe` / `refresh_earnings_universe` / `refresh_dividends_universe` and the exit-code all run OUTSIDE the `with`. So the heartbeat is written `success` immediately and a crash/failure in the actual Sunday fundamentals+earnings+#65-dividends refresh is NEVER recorded → F4 health check always shows it healthy. **(U4-b) `ctx.meta` never persists.** `run_weekly_suggestions.py` sets `with cron_run(job_name) as ctx: ctx.meta = _do_buy()`, but the heartbeat object's real field is `hb.metadata` (as `refresh_fundamentals.py` correctly uses). `ctx.meta` creates a throwaway attribute `_persist` never reads → every weekly_suggestions heartbeat stores empty metadata (buy/sell status + counts lost for forensics). **(U4-c) DOC-DRIFT — documented crontab flags don't exist.** `crontab -l` on EC2 shows the live line is `run_weekly_suggestions.py --direction=both` (argparse-valid). But README §7, Project_State §3/§4, and data_flow all document `--direction=both --notify --run-type scheduled`; the script's argparse defines only `--direction`/`--no-notify`/`--skip-dossiers` and would REJECT `--notify`/`--run-type`. Not a live bug (docs corrected in this same pass), but a trap for the TD21/#46 registry-generated-crontab work if pasted verbatim. **(U4-d) minor:** intraday crontab comment says `09:15-15:45` while the field is `*/15 9-15` (09:00-15:45); news cron `fetch_news_for_universe.py` docstring says "daily" but runs Sunday-only. ESTABLISHED FACTS RESPECTED: schedule stays in crontab (no in-process scheduler); CRON_REGISTRY has 11 entries incl. idle `weekly_suggestions_sell`, crontab has 10 lines — consistent, not a bug. | `scripts/refresh_fundamentals.py` (heartbeat scope), `scripts/run_weekly_suggestions.py` (`ctx.meta` → `hb.metadata`), docs (crontab lines — FIXED in this pass), crontab comments | SHIPPED 2026-08-01 — backend `e79f2309`. U4-a: entire `refresh_fundamentals.main` body now inside `with cron_run(...)` block; per-step stats persisted to `hb.metadata`; VERIFIED on EC2 — heartbeat shows `metadata.fundamentals/.earnings/.dividends` populated with real data and `finished_at` after actual work. U4-b: all three `ctx.meta = ...` in `run_weekly_suggestions.py` replaced with `ctx.metadata.update(...)`. U4-d: intraday docstring corrected (`09:00-15:45 IST`); news-fetch docstring corrected (weekly Sunday 06:30 IST, not daily; `--include-held` shown). |
| 76 | User review (#69, round 2) | **BUNDLE U5 — Suggestions-engine robustness.** Latent crashes + scoring edge cases. **(U5-a) `SuggestionTrackingStatus` Literal missing `"rejected"`.** `models/suggestion.py` types it `Literal["open","acted","passed","expired"]`, but the feedback writer stamps `tracking_status: payload.action` (can be `"rejected"`) and `outcome_tracker` counts `{"tracking_status":"rejected"}` in `bucket_keys`. Raw-dict writes succeed, but any future `SuggestionOutcome(**doc)` hydration on a rejected row raises a Pydantic ValidationError — latent crash + silent schema rot (the drift the typed feedback-patch pattern exists to prevent). **(U5-b) `_parse_dossier` brittle brace extraction + truncation.** `find("{")`→`rfind("}")` spans any stray brace in narrative prose (or a two-object response) → `json.loads` fails → dossier degrades to "narrative unavailable" even when Claude produced good content; `max_tokens=2048` with 3×3-item arrays + 4 hold-horizon prose fields is tight, so mid-JSON truncation is plausible and both retry attempts can truncate identically. **(U5-c) Concentration / portfolio-weight denominator inflated.** `compute_portfolio_value` skips priceless holdings AND `_run_sell_pipeline` computes the denominator from post-exclusion `filtered_holdings` (rejected/acted removed) → every `portfolio_weight_pct` is overstated, dossier can state a weight >100%. **(U5-d) News velocity/recency windowed on `fetched_at`, not `published_at`** → any news backfill inflates `story_velocity` and corrupts `days_since_latest_news`, distorting the news/risk groups. **(U5-e) Tiny-universe normalization.** `_normalize_to_100` returns 50 for every value when <2 valid points; a small sell universe (0-2 eligible holdings) makes every sell composite ≈50 (non-discriminative ranking). **(U5-f) Manual-vs-cron lock gap.** `suggestion_run_locks` (#71) is claimed to guard against an overlapping Sunday cron, but `run_weekly_suggestions.py` never acquires/checks it (only the manual `POST /suggestions/run` does) → a manual run during the cron runs concurrently. **(U5-g) `get_excluded_isins` is direction-agnostic** (acknowledged in-code) → rejecting a SELL suppresses the same ISIN from BUY suggestions for 90d. FALSE POSITIVE KILLED (do NOT file): the dossier "naive/aware datetime TypeError" — `utcnow()` is naive and `_build_position_context_block` strips tz off both operands, so no crash. ESTABLISHED FACTS RESPECTED: advisory-only; `_normalize_to_100` correctly EXCLUDES missing values from mean/stdev (missing does not poison normalization — verified). | `app/models/suggestion.py` (`SuggestionTrackingStatus`), `app/services/dossier_service.py` (`_parse_dossier`, `max_tokens`), `app/services/suggestion_engine.py` (`compute_portfolio_value`, `_run_sell_pipeline` denominator, `get_excluded_isins`, cron lock), `app/services/scoring_service.py` (`_normalize_to_100`), `app/services/news_signals.py` (fetched_at window) | SHIPPED 2026-08-01 — backend `e79f2309`, frontend `ce7960fa`. U5-a: `SuggestionTrackingStatus` gains `"rejected"` — VERIFIED `SuggestionOutcome(tracking_status="rejected")` hydrates without error. U5-b: `_extract_json_object` (balanced-brace scan, quote-aware) replaces brittle `find/rfind`; `max_tokens` 2048→4096. U5-c: portfolio_value denominator uses ALL active holdings (not post-exclusion subset). U5-d: news velocity/recency uses `published_at` (fallback `fetched_at`). U5-f: Sunday cron now acquires the SAME per-direction `suggestion_run_locks` — VERIFIED LIVE: manual run held the lock, cron printed "Skipping … run_in_flight". U5-e (tiny-universe 50): investigated → intentional/correct (n<2 has no cross-section; neutral 50 is right; n=1 ranking is moot). U5-g (direction-agnostic exclusion): documented-for-v1 design; proper fix is #43 TD1 (deferred post-launch). +8 hermetic tests; `tests/test_suggestion_model.py` NEW. |
| 77 | User review (#69, round 2) | **BUNDLE U6 — Transaction edit/delete & tax/FIFO edges.** **(U6-a) `edit`/`delete` recompute is uncaught → 500 AFTER the ledger mutated.** VERIFIED: `transactions.py::edit_transaction` + `delete_transaction` call `recompute_holding(before["isin"])` with NO try/except, unlike add_buy/sell/corp-action (which wrap it to return `recorded_with_warning`). A TD20 lock-timeout `RuntimeError` surfaces as a 500 to the caller AFTER the audit row + ledger mutation already persisted — the exact "did it land?" ambiguity TD19 was meant to kill, now on the edit/delete paths. **(U6-b) `preview_sell` remaining-invested omits residual fees.** `holdings_service.preview_sell` computes `remaining_invested` from `qty*price` only and never adds residual `lot["fees"]`, but `_fifo_replay` computes `invested = sum(qty*price) + sum(fees)`. So the preview's remaining avg-cost differs from what the SELL actually persists whenever any lot carries fees (docstring claims it mirrors `_fifo_replay`). **(U6-c) Same-day FIFO tie-break divergence.** `validate_replay` sorts same-`trade_date` rows by `(trade_date, created_at)`, but `_recompute_holding_impl` and `tax_service` sort by `trade_date` ONLY (Mongo-unstable for equal keys). So an edit/delete can PASS validation under one ordering yet the real replay processes a different order → different lot matching, realized P&L, and STCG/LTCG attribution. Editing a `trade_date` to collide with another row is the concrete trigger. **(U6-d) `search_transactions` future-bound uses UTC, not IST** (`datetime.now(timezone.utc)...` for the "cannot be in the future" check) — a legitimately-today (IST) `to_date` can be rejected between 18:30 IST and midnight IST; the rest of the system is IST for dates. **(U6-e) Duplicate `recompute_holding` import** in `transactions.py` (two import lines) — hygiene/lint. ESTABLISHED FACTS RESPECTED: FIFO source of truth is `_fifo_replay` (do not fork it); `/tax` capital-gains-only; STCG/LTCG >12-month boundary verified CORRECT incl. leap-year clamp. | `app/routers/transactions.py` (edit/delete recompute guard, search future-bound, dup import), `app/services/holdings_service.py` (`preview_sell` residual fees, `_recompute_holding_impl` sort vs `validate_replay`), `app/services/tax_service.py` (same-day sort) | SHIPPED 2026-08-01 — backend `e79f2309`. U6-a: `_safe_recompute()` wraps edit+delete recompute → post-mutation failure returns `recompute_warning` not 500. U6-b: `preview_sell` `remaining_invested` includes residual per-lot fees (mirrors `_fifo_replay`). U6-c: `_recompute_holding_impl`, `preview_sell`, and `tax_service` all sort `(trade_date, created_at)` matching `validate_replay` — VERIFIED LIVE (today-IST `to_date` → 200; future → 400); `FakeCollection._Cursor.sort` extended to support pymongo multi-key `[(field, dir),...]` form. U6-d: search future-bound uses IST (imports `timedelta` — a timedelta-missing-import bug was caught and fixed in the follow-up commit after live verification). U6-e: duplicate `recompute_holding` import removed. +2 tests (residual-fee preview, same-day tie-break). |
| 78 | User review (#69, round 2) | **BUNDLE U7 — Frontend correctness & cache fan-out.** **(U7-a) Sell-sheet realized-P&L preview hardcodes `total_fees:"0"`.** `sell-sheet.tsx` `previewQuery` calls `previewSell(..., {total_fees:"0"})` regardless of the fees the user entered (and `fees` isn't in the preview query key), while the actual `sellMutation` sends the real fees → the previewed realized P&L (shown in the confirm dialog) differs from the booked value by the fee amount, on a money surface. **(U7-b) Reconciliation page lazy-invalidate + no await.** `reconciliation/page.tsx` snapshot mutation uses `invalidateQueries(["reconciliation"])` (lazy) with no await before the toast — the only write path in the app not using synchronous `refetchQueries`-before-toast (the app-wide §14.4 convention); status/history can paint the prior snapshot for a frame. **(U7-c) Drift badge disagrees with backend + is sign-blind.** `DeltaRow` badges drift on a flat `parseFloat(drift) > 1000` for BOTH invested and current-value, but the page copy states the CV threshold is ₹15,000; and `> 1000` (not `Math.abs`) never badges negative drift. **(U7-d) `qty*price` recomputed client-side, ignoring `trade_value`.** Both `transactions/page.tsx` and `transactions-list.tsx` recompute the Amount column instead of using the backend-authoritative `trade_value` string → can disagree with the ledger. **(U7-e) `notes-panel.tsx` form state never re-syncs on prop change** — `useState(holding.x)` seeds only at mount; after an external recompute (e.g. a corp action), clicking Edit shows STALE field values, risking overwrite of fresh backend data on Save (this is the SOLE editor of stop_loss/target/tags/notes/thesis — money-adjacent). **(U7-f) `transaction-edit-sheet.tsx` has no guard against editing SPLIT/BONUS/DIVIDEND rows** — generic `positive()`/`min=1` validation would reject/garble a corp-action row (the #68 parent-reprice flow routes users here for BUY rows, which is fine, but nothing prevents a corp-action-type edit). **(U7-g) minor:** `holdings/[isin]` 404 detection is string-based (`msg.includes("404")`) not `ApiError.status`; suggestions-page error toasts don't unwrap `ApiError.detail`; buy-sheet fan-out `["transactions", isin]` doesn't prefix-match the `["transactions","search",...]` page key; corp-action/edit fan-outs omit `["reconciliation"]`. ESTABLISHED FACTS RESPECTED: Recharts `'use client'` boundary present (verified); MarkdownLite is XSS-safe (no `dangerouslySetInnerHTML`). | frontend `components/sell-sheet.tsx` (preview fees), `app/reconciliation/page.tsx` (invalidate→refetch, drift badge), `app/transactions/page.tsx` + `components/transactions-list.tsx` (`trade_value`), `components/notes-panel.tsx` (prop re-sync), `components/transaction-edit-sheet.tsx` (corp-action guard), `app/holdings/[isin]/page.tsx` + `app/suggestions/page.tsx` (error handling), `components/buy-sheet.tsx` (fan-out) | SHIPPED 2026-08-01 — frontend `ce7960fa`. U7-a: sell-sheet preview passes real fees + keys on them. U7-b: reconciliation snapshot mutation awaits `refetchQueries` before toast. U7-c: drift badge uses `Math.abs(parseFloat(drift)) > 0` (trusts backend threshold, sign-aware). U7-d: `txAmount()` uses `trade_value` when present (both transactions-list + transactions-page). U7-e: `notes-panel` `useEffect` re-syncs on holding prop change while NOT editing. U7-f: edit-sheet shows read-only guard notice for SPLIT/BONUS/DIVIDEND rows. U7-g: `holdings/[isin]` 404 detection via `ApiError.status===404`; `buy-sheet` fan-out uses `["transactions"]` prefix; suggestions error toasts unwrap `ApiError.detail`. VERIFIED on EC2: `~/deploy-ui.sh` build+lint clean, reconciliation+transactions → 200. |
| 79 | User review (#69, round 2) | **BUNDLE U8 — Docs & low-risk hygiene** (several FIXED in this same doc pass to eliminate doc-drift). **(U8-a) Crontab-line drift — FIXED here:** README §7 / Project_State §3+§4 / data_flow all now show the LIVE line `run_weekly_suggestions.py --direction=both` and note `--notify`/`--run-type` are NOT valid argparse flags (they would break the Sunday cron if pasted). **(U8-b) Stale "last updated 2026-05-24" stamps — FIXED here** on both READMEs + data_flow, which predated ~15 shipped items; added the missing crons (`purge_news_bodies` 02:30 IST / TD27), collections (`dividend_announcements`, `alerts_log`), endpoints (`/tax/capital-gains`, `/watchlist*`, `/admin/recompute/{isin}`, `/reconciliation/dividend-drift`, `/transactions/corporate-action`, `/suggestions/run*`, `/portfolio/risk-summary`, `/portfolio/by-tag`, `/chat/*`, `/instruments/search`), and the alert types (`stop_loss_hit`, `target_hit`, `news_event`, dividend-drift nudge, positive daily heartbeat). **(U8-c) #51 `dividend_yield` ×100 (still OPEN, backend-only)** — confirmed the frontend renders it NOWHERE; the ×100 lives only in `dossier_service._fmt_pct` + `conversation_service._fmt_pct` (`f"{v*100:.2f}%"`) applied to yfinance values already stored as percent for some names. Fix at the ingest source (`fundamentals_service`) and/or both `_fmt_pct` consistently (do not diverge the two). **(U8-d) data_flow Open-Question Q3 (`holdings.stop_loss` "never read/written") is stale — FIXED here** (SHIPPED via #41). **(U8-e) code hygiene (LOW, defer):** `holdings-table.tsx` search placeholder says "symbol or name" but filters symbol-prefix only; `transactions-list.tsx` has a dead no-op sort-direction ternary; `top-movers.tsx` has a mid-file `import Link`; `refresh-button.tsx` omits `["holding"]`/`["transactions"]` keys (matters only if reused off-dashboard); `instruments.py` search regex is now `re.escape`d (good) — verified. ESTABLISHED FACTS RESPECTED: no code redesign; doc fixes only bring docs in line with the shipped code. | backend `README.md`, `docs/data_flow.md`, `docs/Project_State.md`; frontend `README.md`; #51 → `app/services/fundamentals_service.py` + `dossier_service.py` + `conversation_service.py`; hygiene in the named frontend components | SHIPPED 2026-08-01 — backend `e79f2309`, frontend `ce7960fa`. U8-a/b/d: fixed in the #69 filing pass. U8-c (#51): fmt_pct unified — see #51 row above. U8-e: holdings-table placeholder corrected to "Search symbol…" (name-substring match reverted after "GA" matched ONGC via "Natural Gas" — symbol-prefix only is correct for a ticker box); top-movers mid-file import moved to top; refresh-button fan-out adds ["holding"]/["transactions"]; dead sort-direction ternary removed (in #78 commit). |

---

## Summary by phase

| Phase | Items | Theme | Gating |
|---|---|---|---|
| 1 | 1-3 | Unblock ops + reconcile docs | No code — SHIPPED Chat 5.9 |
| 2 | 4-8 | Transactions/holdings/audit invariants | Foundation for Chat 9 + 10 — SHIPPED Chat 5.10 |
| 3 | 9-11 | Intraday & price correctness | Foundation for Chat 8 sell-side — SHIPPED Chat 5.11 |
| 4 | 12-13 | Storage hygiene (TTL + purge) | Foundation for Chat 10 real data — SHIPPED Chat 5.12 |
| 5 | 14-18 | Frontend correctness + quick wins | Independent — SHIPPED Chat 5.13 |
| 6 | 19-24 | External-service hardening | Foundation for Chat 8 (parallelism) — COMPLETE: #19 SHIPPED Chat 5.14, #20 SHIPPED Chat 5.15, #21 SHIPPED Chat 5.16, #22 SHIPPED Chat 5.17, #23 SHIPPED Chat 5.18, #24 SHIPPED Chat 5.19 |
| 7 | 25-26 | Reconciliation alerting + feedback direction | COMPLETE — #25 + #26 SHIPPED Chat A |
| 8 | 27-29 | New features (Chats 6, 7, 8) | COMPLETE — #27 SHIPPED Chat 6; #28 SHIPPED Chat 7; #29 SHIPPED Chat 8 |
| 9 | 30-38 | Pre-launch sweep + ops gaps | Last clean state before GO LIVE — #34 + #35 SHIPPED (Chat A); #30 + #31 + #32 SHIPPED 2026-06-15 (Chat B); #33 SHIPPED 2026-06-26 (Chat B); #36 SHIPPED 2026-07-01 (Chat B); #37 SHIPPED 2026-07-01 (Chat B); #38 SHIPPED 2026-07-01 (Chat B); Phase 9 COMPLETE |
| 10 | 39-41 | Chat 9 pre-launch cleanup (F11 + realized P&L hide + stop_loss) | COMPLETE — #39 F11 capital-gains SHIPPED 2026-07-01 (Chat 9); #40 realized-P&L UI hide SHIPPED 2026-07-03 (Chat C); #41 stop_loss wiring SHIPPED 2026-07-03 (Chat C) |
| 10.5 | 52, 54-71, 72-79, 44, 51 | USER ACCEPTANCE REVIEW — round 1 run 2026-07-07, round 2 run 2026-07-28, round-3 sanity pass 2026-08-01. ALL BLOCKING FINDINGS SHIPPED. Round-1: #66/#67 SHIPPED 2026-07-08; #58/#59/#62 SHIPPED 2026-07-08; #63/#64 SHIPPED 2026-07-21; #53/#54/#55/#56/#70/#71 SHIPPED 2026-07-25; #50/#57 SHIPPED 2026-07-26; #65/#68 SHIPPED 2026-07-27. Round-2 fix bundles: **#72–#79 ALL SHIPPED 2026-08-01** (backend `e79f2309`, frontend `ce7960fa`): #72 U1 corp-action idempotency (CLEARED — the provisional NO-GO blocker); #73 U2 alert reliability; #74 U3 dividend-drift correctness; #75 U4 cron observability; #76 U5 suggestions robustness; #77 U6 txn edit/delete & FIFO; #78 U7 frontend correctness & fan-out; #79 U8 docs & hygiene. Also shipped: **#44 TD3** valuation_verdict split 2026-08-01; **#51** dividend_yield unified fmt_pct 2026-08-01. Round-3 sanity pass PASSED. **GO/NO-GO = GO. #69 CLOSED. #42 UNBLOCKED.** Non-blocking open: #60/#61 (doc/prep only); #43/#45 (deferred Phase 12); #46 (Scheduler — dedicated chat, after GO-LIVE). | COMPLETE — GO/NO-GO = GO (2026-08-01) |
| 11 | 42 | Chat 10 GO LIVE (F7 real data import) | OPEN — gated behind Phase 10.5 |
| 12 | 43-45 | Deferred TDs (TD1, TD3, TD7) | DEFERRED |
| NEW | 46-51, 53-71 | TD21 scheduler migration (OPEN) + TD22 outcomes-cron failure (SHIPPED Chat A) + TD36 Tavily doc cleanup (SHIPPED Chat A) + TD40 weekly_suggestions_sell Sunday false MISSING (SHIPPED Chat A) + #50 news entity mis-tagging (OPEN, Chat 6) + #51 dividend_yield ×100 formatting (OPEN, Chat 6) + #53 demerger holding-period inheritance at the FIFO source (SHIPPED 2026-07-21) + #63/#64 dividends-in-P&L + re-show (SHIPPED 2026-07-21) + #55 LLM hold-horizon on buy dossiers (SHIPPED 2026-07-25) + #71 manual "Run now" trigger (SHIPPED 2026-07-25) | Filed Chat 5.9 (46-47) / Chat 5.16 (48) / Chat 5.19 (49) / Chat 6 (50-51) / Chat 9 (53) / Phase 10.5 (54-71); #53 + #63/#64 SHIPPED 2026-07-21, #54 + #55 + #71 SHIPPED 2026-07-25; schedule independently |

---

## Cross-references

- `docs/Project_State.md` Section 0 — bootstrap (this file is item 5 on the read list)
- `docs/Project_State.md` Section 13 — historical "shipped" log and chat split plan
- `docs/Project_State.md` Section 18 — TD registry (each row cross-references its master_todo `#N`)
- `docs/data_flow.md` — data flow reference

## How to update this file

- **Every chat ends with two doc commits:** `Project_State.md` full-file replacement AND a `master_todo.md` update (status changes + current-position pointer advance).
- **Never delete shipped rows.** Change `OPEN` → `SHIPPED <YYYY-MM-DD> (Chat <N>)`. Shipped rows are the audit trail.
- **Adding new items mid-stream** (e.g., a fresh bug discovered): append the row at the END of the appropriate phase (or in the NEW ITEMS phase); do not renumber existing items (item numbers are stable references).
- **Re-ordering between phases:** allowed if the rationale block explains why; document the reason in the row's Notes column if needed.
- **If a row becomes obsolete** (e.g., TD15 reconciliation reveals an F-ticket already shipped a fix): change Status to `DROPPED <YYYY-MM-DD> (Chat <N>) — <one-line reason>`. Do not delete.
- **Current-position pointer** at the top must advance every time the lowest-numbered OPEN row changes.

End of master_todo.md.