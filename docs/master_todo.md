# Master Todo — Personal AI Stock Advisor

This file is the canonical, ordered, end-to-end task list to reach product completion. It is the source of truth for what to do next. Every new chat reads it after `Project_State.md`.

**Created:** 2026-05-29 (Chat 5.8 — review + planning)
**Last updated:** 2026-06-15 (Chat 8 — #29 F13 watchlist SHIPPED across 4 units + 1 CORS fix; Phase 8 COMPLETE; new PHASE 10.5 USER ACCEPTANCE REVIEW stage added as #52, second-to-last before GO LIVE; pointer advanced to #30)
**Updated:** 2026-06-15 (Chat B — Phase 9 hygiene sweep: #30 P2-1, #31 P2-8, #32 P3-2 all SHIPPED; 11-site `datetime.utcnow()`->`utcnow()` sweep, tree-wide tz-aware Mongo-write sweep + NEW tokenize-based `scripts/check_datetime_hygiene.py` lint guard, Python ceiling pin; pointer advanced to #33; #33, #36, #37, #38 remain OPEN in Chat B)
**Updated:** 2026-06-26 (Chat B — #33 Review-note pytest harness SHIPPED: hermetic, zero-dependency `tests/*` harness — 28 tests across the 6 review-mandated targets (`_fifo_replay`, `validate_replay`, `preview_sell`, `recompute_holding` idempotency, `submit_feedback` write-before-apply ordering, `take_auto_snapshot` drift math); new `tests/_fakes.py` (in-memory `FakeCollection` + `tx()`/`oid()` factories) + `tests/conftest.py` (`fake_db` fixture patching the `Collections.*` accessors); runs via `uv run python -m pytest`; verified on EC2 (28 passed) + hygiene guard still PASSED. Backend code HEAD now `04fd97090cd189b7f9a62fbc6fe3564c9cccb55d`; pointer advanced to #36; #36, #37, #38 remain OPEN in Chat B)
**Updated:** 2026-07-01 (Chat B — #36 Ops-gap `POST /admin/recompute/{isin}` SHIPPED: NEW `app/routers/admin.py`, Tailscale-only, delegates to the existing `holdings_service.recompute_holding` (TD20 per-ISIN advisory lock; NO parallel recompute path); ISIN pattern-validated; registered in `app/main.py`; returns `{status: recomputed/no_active_holding, isin, holding}`, 409 on lock contention; verified on EC2 (active recompute idempotent re-run, no-txn ISIN -> no_active_holding/null 200, bad ISIN -> 422, route on OpenAPI surface, 28 pytest + hygiene guard still green); backend code HEAD now `1ef0eadf4e95349fdce12156fc2c37d90bc3719d`; pointer advanced to #37; #37, #38 remain OPEN in Chat B. Frontend HEAD reconciled to `c5bb1a34d09dfc6d6879a3b2264310e4a9771c24` — one docs-only commit `c5bb1a3` (frontend README per-page reference + nav cover /watchlist, #29) since `58bf6369`, NOT a code change.)
**Audit baseline:** Backend SHA `c6b1437b90c9555ab9090657af74ab550cf6e1cd`, Frontend SHA `4f31b49b103f92ea5b4721f9728156041e908f49`
**Current backend SHA (Chat B close):** deployed code HEAD `1ef0eadf4e95349fdce12156fc2c37d90bc3719d` (Chat B #36 — the `POST /admin/recompute/{isin}` admin-recompute commit; the Chat B `Project_State.md` + `master_todo.md` doc commit advances HEAD further). #36 (Ops gap): NEW `app/routers/admin.py` exposes `POST /admin/recompute/{isin}` (Tailscale-only) — the HTTP replacement for SSH-shell recovery of a stuck holding (the TD19/#7 fallback) — delegating to the existing `holdings_service.recompute_holding` (per-ISIN advisory-locked, TD20; no parallel recompute path); ISIN `pattern=r"^[A-Z0-9]{12}$"` (422 at the boundary); returns `{status: "recomputed", isin, holding}` on an active result, `{status: "no_active_holding", isin, holding: null, message}` when `recompute_holding` returns None (full exit / no transactions — 200, not 404), 409 on TD20 lock contention, 500 otherwise; registered in `app/main.py`. Verified on EC2 against localhost:8000: active recompute -> `recomputed` + idempotent re-run (byte-identical aggregates), no-transaction ISIN -> `no_active_holding`/`null`, bad-shape ISIN -> 422, route on the OpenAPI surface, `uv run python -m pytest` => 28 passed + `uv run python -m scripts.check_datetime_hygiene` still PASSED. Prior: Chat B #33 was the pytest-harness commit on `04fd97090cd189b7f9a62fbc6fe3564c9cccb55d`. Chat B shipped Phase 9 #30 + #31 + #32 (2026-06-15) on `025b8a0688a67d181f09ed800b6692f574fdb9bc`, then #33 (2026-06-26) on `04fd970`. #33 (Review note): hermetic, zero-dependency `tests/*` pytest harness — 28 tests across the 6 review-mandated targets; new `tests/_fakes.py` (in-memory `FakeCollection`: `find/find_one/insert_one/update_one` upsert/`delete_one`/`delete_many`/`count_documents` with `$ne`/`$in`/`$exists`/`$or`/`$and`; `tx()`/`oid()` factories) + `tests/conftest.py` (`fake_db` fixture monkeypatching the `Collections.*` accessors); `recompute_holding` gets full DB idempotency (run twice -> stable aggregates + preserved `created_at` + exactly one active doc); `fetch_metadata`/`_get_our_numbers`/`_send_auto_drift_alert`/`log_change` monkeypatched so nothing hits Atlas/yfinance/ntfy; verified on EC2 `uv run python -m pytest tests/ -v` => 28 passed + `uv run python -m scripts.check_datetime_hygiene` still PASSED. Backend + doc only; frontend unchanged `58bf6369`. #30 (P2-1): 11 `datetime.utcnow()`->`utcnow()` sites across 5 files (`routers/portfolio.py`, `services/scoring_service.py`, `services/dossier_service.py`, `services/fundamentals_service.py`, plus 2 stragglers in `services/conversation_service.py` the row's stale list missed). #31 (P2-8): tree-wide tz-aware `datetime.now(timezone.utc)` Mongo-write sweep->`utcnow()` (15 write/query sites, incl. the `reconciliation.take_manual_snapshot` + `price_service.bulk_get_latest_intraday` sites the hygiene guard caught after a `replace_all` partial-match miss) + 19 `# tz-ok:` annotations + NEW `scripts/check_datetime_hygiene.py` tokenize-based lint guard. #32 (P3-2): `requires-python = ">=3.12,<3.14"` pin + `uv lock` relock (no dep churn). Chat B opened at backend `0b6e1147` (the Chat 8 doc-commit HEAD). Prior: **Current backend SHA (Chat 8 close):** deployed code HEAD `67704025650bed4cbce9549ea45a064bae892c12` (#29 CORS fix — add PUT to allow_methods; the Chat 8 `Project_State.md` + `master_todo.md` doc commit advances it further). Chat 8 shipped #29 (F13 watchlist) across four backend/frontend code commits plus one CORS fix — Unit 1 write-model + universe set-math (`34ff906d7cf6b2c8d1e1ccc210810023f069f7aa`: `MonitoredStockWatchlistPatch` + `build_universe` = NIFTY 100 ∪ watchlist + `get_watchlist_isins`), Unit 2 `/watchlist` CRUD (`a250d00189c67ddc073715affc9175b9fc68383d`: new `routers/watchlist.py` + widened audit-action Literal + `main.py` include), Unit 3 cron coverage (`9857570b8e1799c274d2ce422b44bef419f17d11`: `refresh_fundamentals.py` + `fetch_news_for_universe.py` fold in watchlist ISINs), and the CORS fix (`67704025`: `main.py` `allow_methods` += `PUT`). **Current frontend SHA (Chat 8 close):** `58bf6369e73916c26e534fc517ac92f2f3dfedb5` (Unit 4 — `lib/api.ts` watchlist types + bindings, new `app/watchlist/page.tsx`, `app/page.tsx` Watchlist nav link). Chat 8 opened at backend `803e6610` (Chat 7 code HEAD; the Chat 7 doc commit `c162d9c2` was the actual open base for the file re-reads) and frontend `e14d6a75`. Prior: **Current backend SHA (Chat 7 close):** deployed code HEAD `803e6610ec21a8bad9a56840abc059cf92db5890` (#28 F15 by-tag — Unit 2; the Chat 7 `Project_State.md` + `master_todo.md` doc commit advances it further). Chat 7 shipped #28 (F12 + F15) across two backend code commits — Unit 1 risk-summary (`97041621eedd94947a2ce2c1843d23f317b1b31b`: extract `_annotate_holdings` from `compute_summary` behaviour-preserving + `compute_risk_summary` + `GET /portfolio/risk-summary`) and Unit 2 by-tag (`803e6610`: `GET /portfolio/by-tag`) — plus two frontend code commits. **Current frontend SHA (Chat 7 close):** `e14d6a750f802dae941d512837ff1788a7a3a0f0` (Unit 3 risk-summary card + api bindings + Tags nav, then Unit 4 `/tags` page). Chat 7 opened at backend `5e787c9842496a720768f867560d18c31220ea81` (Chat 6 code HEAD; the Chat 6 doc commit `a104993` was the actual open base) and frontend `6093f6342e6a6ddb1ecf0c8a1b7fa2239d825c7d`. Prior: **Current backend SHA (Chat 6 close):** deployed code HEAD `5e787c9842496a720768f867560d18c31220ea81` (#27 F1+F3 ad-hoc chat shipped across five backend commits — Unit 1 data layer, Unit 2 enrichment orchestrator, Unit 3 chat service + endpoints, then the `/instruments/search` route-shadow fix; the Chat 6 doc commit advanced it further). **Current frontend SHA (Chat 6 close):** `6093f6342e6a6ddb1ecf0c8a1b7fa2239d825c7d` (Unit 4 chat UI — ChatPanel + StockResearchPanel + lib/api.ts; no new npm dependency). Chat 6 opened at backend `4403bb5348c37e323824efc03777050e3631ce15` and frontend `f59958015b8b07b6e84e3add7b4a302d32b43490`. Prior: **Current backend SHA (Chat A close):** deployed code HEAD `fae6edf446dab815982b767b8f9a15c2fe36e6b5` (the #48 doc-only commit; the Chat A `Project_State.md` + `master_todo.md` doc commit advanced it further). Chat A shipped seven items across five code/doc commits: #34 + #35 (Ops gaps) on `bd52c6baf9a37325cc91d50f495bee8d4be70421`; #25 (P2-7) on `1340396eb1d614a1d171c5c5c3a97f35330bf64e`; #49 (TD40) + #26 (P2-6) on `6032b644c43132aeda96807448836573cadc154a`; #47 (TD22) on `4b638e6f5282883c094f4233e288b24294157565`; #48 (TD36, doc-only) on `fae6edf446dab815982b767b8f9a15c2fe36e6b5`. Chat A was backend + doc only (frontend unchanged). Chat A opened at backend `c4b50364eb5dd12bca46649c702afcd00677eb5d` (the Chat 5.19 doc commit). Prior: **Current backend SHA (Chat 5.19 close):** deployed code HEAD `7fcda9eb1b0e7a5cee7aba684ef7129d77b9b977` (Phase 6 TD39 / #24 self-failure dual-transport alert wrapping `cron_health_check.main`'s Mongo reads shipped on ONE backend code commit; HEAD shown is after that code commit — the Chat 5.19 doc commit advances it further). Chat 5.19 opened at backend `247510f105969f991cf661eabada2b25d47ce799` (the Chat 5.18 doc commit). Prior: **Current backend SHA (Chat 5.18 close):** deployed code HEAD `0515fef4c7384710124dbc2f73ff726662d61137` (Phase 6 TD38 / #23 fallback heartbeat log + dual-source health check shipped on ONE backend code commit; HEAD shown is after that code commit — the Chat 5.18 doc commit advances it further). Chat 5.18 opened at backend `98ead2a2e499f09bdade95b3201b7d76d7531ee6` (the Chat 5.17 doc commit). Prior: **Current backend SHA (Chat 5.17 close):** deployed code HEAD `1d627d7861ead18b05df0158fe41c06c42de89d2` (Phase 6 TD37 / #22 reject NaN in `_to_decimal` shipped on ONE backend code commit; HEAD shown is after that code commit — the Chat 5.17 doc commit advances it further). Chat 5.17 opened at backend `f4168b3343725c0f873d5fd99d1c68bc5d6c88cc` (the Chat 5.16 doc commit). Prior: **Current backend SHA (Chat 5.16 close):** deployed code HEAD `f4168b3343725c0f873d5fd99d1c68bc5d6c88cc` (Phase 6 TD35 / #21 explicit `inserted_id` flow into digest delivery shipped on ONE backend code commit — the Chat 5.16 doc commit advances it further). Chat 5.16 opened at backend `67fd6803d5ffd609e378b4219c5a4710046247aa` (the Chat 5.15 doc commit). Prior: Chat 5.15 closed at deployed code HEAD `7d77b9cbee9f3155f22c86057b20640f21599ee9` (Phase 6 TD34 / #20 `notify.email()` transient-5xx/429 retry on ONE backend code commit); it opened at backend `582cd18d5d50d90b1ae4d1174a22a59799d69ca0` (the Chat 5.14 doc commit). Prior: Chat 5.14 closed at deployed code HEAD `4ac2c955782490818eefa6024c9daead92b0b0eb` (Phase 6 TD33 / #19 atomic Tavily quota claim on ONE backend code commit) + frontend `f59958015b8b07b6e84e3add7b4a302d32b43490` (unchanged — backend-only); it opened at backend `5ab01ef0df2ebb3c3d1d0aba26cdce9be17c17fe` (the Chat 5.13 doc commit). Prior: Chat 5.13 closed at deployed code HEAD `090d96c0042e7d5ccd154dcaf6329a0bba57ebb7` (TD29 dead-import, TD31 ISIN pattern, TD32 regex `$options` drop on three backend code commits) + frontend `f59958015b8b07b6e84e3add7b4a302d32b43490` (TD28 refetchQueries swap on one frontend commit); it opened at backend `07d9a413b39d330e3ea9047dec4e38917a446449` and frontend `4f31b49b103f92ea5b4721f9728156041e908f49`.

> Note (Chat 5.9): the on-disk copy of this file had the "Ordering rationale" + "When you finish an item…" paragraph duplicated 8 times (a paste/commit artifact). The Chat 5.9 full-file replacement collapsed it back to a single copy. No item rows were affected.

---

## Current position

**Next item to start: #37 (Chat B / Ops gap — rehearse Atlas backup -> fresh-DB restore for `monitored_stocks` + `suggestion_outcomes` + `digest_deliveries`; document the exact `mongorestore` command in `Project_State.md`). It is the lowest-numbered OPEN row. Chat B (Phase 9 pre-GO-LIVE hygiene sweep; bundle #30, #31, #32, #33, #36, #37, #38) has shipped #30, #31, #32 (2026-06-15), #33 (2026-06-26), and #36 (2026-07-01); #37, #38 remain OPEN in Chat B.**

Phase 1 + Phase 2 + Phase 3 + Phase 4 + Phase 5 + Phase 6 + Phase 7 + Phase 8 are now fully SHIPPED. Phase 8's last item #29 (Chat 8 / F13 watchlist) SHIPPED 2026-06-15. Chat B (Phase 9) has shipped #30, #31, #32 (2026-06-15), #33 (2026-06-26), and #36 (2026-07-01). The remaining OPEN work is Phase 9's #37, #38 (Chat B continues), Phase 10 (Chat 9 pre-launch cleanup), the NEW Phase 10.5 (USER ACCEPTANCE REVIEW, #52), Phase 11 (GO LIVE), Phase 12 (deferred TDs), and the NEW-ITEMS rows (#46 scheduler, #50 + #51 Chat-6 data bugs).

**Chat B (#30 + #31 + #32 + #33 + #36 — Phase 9 pre-GO-LIVE hygiene sweep) — in progress, opened 2026-06-15.** Bundle = #30, #31, #32, #33, #36, #37, #38; each row shipped individually with the pointer advanced per row. Backend code HEAD at #33 is `04fd970` (the Chat B doc commit advances it further). Every aware-now site was re-read at HEAD before patching; the master_todo line numbers in the Phase 9 rows are a stale map, not ground truth.
- **#30 (P2-1) — `datetime.utcnow()` sweep.** Replaced 11 call sites with `utcnow()` from `app.models._common` across 5 files: the row's listed 9 (`routers/portfolio.py`, `services/scoring_service.py` x3, `services/dossier_service.py` x2, `services/fundamentals_service.py` x3) PLUS 2 stragglers in `services/conversation_service._format_position_block` (`days_held` + `days_to_earn`) that postdate the row's hardcoded list (added Chat 6). `utcnow()` returns `datetime.now(timezone.utc).replace(tzinfo=None)` (naive UTC) so every swap is byte-identical. Added the `utcnow` import to portfolio/scoring/dossier; fundamentals + conversation_service already imported it. Verified on EC2: `grep -rn "datetime.utcnow(" app/ scripts/` -> zero; the F3 holding chat ran both swapped `conversation_service` calls and returned a real LTCG answer.
- **#31 (P2-8) — tz-aware Mongo-write sweep + CI lint guard (two legs, one chat).** Leg 1: a tree-wide audit reclassified every `datetime.now(timezone.utc)` as Mongo-WRITE (swap to `utcnow()`) vs in-memory compare (keep aware + annotate). 15 write/query sites swept across `reconciliation` (incl. the `take_manual_snapshot:213` site a first-pass `replace_all` missed because the two `taken_at` writes were not byte-identical), `news_classifier`, `transactions_audit_service`, `cron_heartbeat_service`, `instrument_service` x3, `price_service` (`_df_to_rows` fetched_at, `fetch_intraday_quotes` + `bulk_get_latest_intraday` now_utc — the latter a second `replace_all` miss caught by the guard), `seed_nifty100`, `import_orderbooks`, `add_manual_transactions`, `seed_cost_basis_adjustments`. In-memory aware sites (subtraction vs aware-coerced reads, `astimezone(IST)`, JSON `as_of` responses, date-string computes) were LEFT aware — swapping them would TypeError. Leg 2: NEW `scripts/check_datetime_hygiene.py` — a tokenize-based guard that bans stdlib `utcnow` outright and fails any aware-`now()` lacking a trailing `# tz-ok: <reason>` annotation; comment-aware, self-skipping, fragment-built needles (won't trip the existing greps). 19 in-memory sites annotated `# tz-ok:`. The guard was first written line-based and FAILED 5 sites where the formatter wrapped a long statement and parked the `# tz-ok` comment on the closing-bracket line; rewritten to group by logical line (tokenize, NEWLINE-delimited) so the annotation matches wherever the formatter parks it. Verified on EC2: guard PASSED (exit 0); negative controls (a planted real `datetime.utcnow()` call AND a planted unannotated aware-now) both FAILED (exit 1) then cleared on revert. Run: `uv run python -m scripts.check_datetime_hygiene`. The `app.models._common.utcnow()` storage-invariant convention is now machine-enforced.
- **#32 (P3-2) — Python ceiling.** Pinned `requires-python = ">=3.12,<3.14"` in `pyproject.toml` (lower bound unchanged) + `uv lock` relock (no dependency version churn). Verified on EC2: `uv sync` clean, interpreter `Python 3.12.3` in range, `/health` ok/ok, hygiene guard still PASSED.
- **#33 (Review note) — pytest harness.** Stood up a hermetic, zero-dependency `tests/*` harness — 28 tests across the 6 review-mandated targets. NEW `tests/_fakes.py`: an in-memory `FakeCollection` implementing only the operators the SUTs use (`find`/`find_one` with `sort`/`skip`/`limit`, `insert_one`, `update_one` with `$set`/`$setOnInsert`/upsert, `delete_one`/`delete_many`, `count_documents`; `$ne`/`$in`/`$nin`/`$exists`/`$or`/`$and`) + `tx()` (Mongo-shaped transaction dict) + `oid()` factories. NEW `tests/conftest.py`: a `fake_db` fixture that monkeypatches every `Collections.*` accessor the targets touch (transactions/holdings/instruments/recompute_locks/reconciliation_snapshots/user_profile/monitored_stocks/monitored_stocks_audit/suggestion_outcomes/suggestion_runs) to a `FakeCollection`. Six test files: `test_fifo_replay.py` (7 — single buy, partial sell realizes P&L, full exit, buy fees fold into invested, dividend accrues without touching position, SPLIT scales qty/price, BONUS dilutes cost), `test_validate_replay.py` (5 — valid buy-then-sell, oversell rejected, chronology-by-trade_date not input-order, split enables post-split sell, deleted txns skipped), `test_preview_sell.py` (6 — partial/full/oversell/non-positive/no-txn/split-aware), `test_recompute_holding.py` (2 — full DB idempotency: run twice -> stable aggregates + preserved `created_at` + exactly one active doc; full-exit soft-delete returns None), `test_submit_feedback.py` (3 — audit-written-before-apply ordering via call-order capture, previous_status captured, action->status mapping), `test_take_auto_snapshot.py` (5 — no-manual-baseline skips drift block, no-drift on match, drift>threshold fires alert, rising-edge dedupe suppresses repeat, exactly-at-threshold is strict-`>` not drift). DB-coupled targets monkeypatch `fetch_metadata`/`_get_our_numbers`/`_send_auto_drift_alert`/`log_change` so nothing hits Atlas, yfinance, or ntfy. Every fixture grounded against the real signatures re-read at HEAD before construction. No new deps (pytest>=8.3 already in the dev group; `[tool.pytest.ini_options] pythonpath=["."]` already present). Verified on EC2: `uv run python -m pytest tests/ -v` => 28 passed in ~1.1s (Python 3.12.3, pytest-9.0.3); `uv run python -m scripts.check_datetime_hygiene` still PASSED (the test files use only fixed datetimes — no `utcnow()`/aware-now). Backend + test only; frontend unchanged.

- **#36 (Ops gap) — `POST /admin/recompute/{isin}` (Tailscale-only).** NEW `app/routers/admin.py` registered in `app/main.py` — the HTTP replacement for SSH-shell recovery of a stuck holding (the TD19/#7 fallback). Delegates to the existing `holdings_service.recompute_holding` (per-ISIN advisory-locked, TD20) — NO parallel recompute path. ISIN `pattern=r"^[A-Z0-9]{12}$"` (422 at the boundary); returns `{status: "recomputed", isin, holding}` on an active result, `{status: "no_active_holding", isin, holding: null, message}` when `recompute_holding` returns None (full exit / no transactions — 200, not 404), 409 on TD20 lock contention (`RuntimeError`), 500 on any other failure. Router-local `_jsonable` serializer (the watchlist precedent — no reach into another router's helper). "Tailscale-only" = the existing app-wide perimeter (no in-app auth gate added). No new test file — the thin HTTP wiring delegates to `recompute_holding`, already regression-guarded by `tests/test_recompute_holding.py` (#33). Verified on EC2 against localhost:8000: active ISIN -> `recomputed` + idempotent re-run (byte-identical quantity/avg_cost/invested/realized_pnl), no-transaction ISIN -> `no_active_holding`/`null`, bad-shape ISIN -> 422, `/openapi.json` shows the route, `uv run python -m pytest` => 28 passed + `uv run python -m scripts.check_datetime_hygiene` still PASSED (admin.py constructs no datetimes). Commit on `1ef0ead`.

#37/#38 (restore rehearsal, JSON logging) remain OPEN in Chat B. No new tech-debt or follow-up items were filed.

**Chat 8 (#29 — F13 watchlist) — closed 2026-06-15.** Standalone large feature, kept one-per-chat by design (it multiplies data volume so it was gated LAST among the Phase-8 features, after Phase 4 storage hygiene + Phase 6 Tavily race, both COMPLETE). A design proposal was approved before any code (the universe set-math change + how `get_excluded_isins` interacts with watchlist membership; the `/watchlist` CRUD contract + reuse of `monitored_stocks` + the partial-unique-index implications; how the two crons pick up watchlist ISINs and the Tavily-quota blast-radius; where the frontend surface lives). Built + verified in four units, each its own commit, plus one CORS fix surfaced during browser testing:
- **Unit 1 — write-model + universe set-math (`models/monitored_stock.py` + `services/suggestion_engine.py`, commit `34ff906d`):** added `MonitoredStockWatchlistPatch` (mirrors `MonitoredStockFeedbackPatch` — `ConfigDict(extra="forbid")`, `status` pinned to `"watchlist"`, carries only the mutable watchlist fields, `exclude_none` on dump so a re-PUT never wipes unspecified fields). Evolved `build_universe()` from `instruments.find({"in_nifty100": True})` to **NIFTY 100 ∪ watchlist**, deduped by ISIN, resolving watchlist ISINs outside NIFTY 100 from `instruments` by ISIN (warns on any not found); added `get_watchlist_isins()` as the single source of truth for membership (`monitored_stocks` `status=="watchlist"`). Held stays filtered DOWNSTREAM by `filter_universe` exactly as before (a held+watchlist ISIN is not a buy candidate), so net candidates = `(NIFTY 100 ∪ watchlist) − held − rejected − acted`. **No change to `get_excluded_isins`** — it scans only `rejected`/`tracking`, so a watchlist row is structurally never excluded; flipping `rejected→watchlist` auto-un-suppresses. **No change to `indexes.py`** — `monitored_stocks` is one-doc-per-ISIN (upsert-on-`{isin}`), so a watchlist state and a tracking state can't be separate rows; the `isin_unique_active` partial index (`partialFilterExpression status:"tracking"`) is untouched and only constrains tracking docs. Verified on EC2: write-model dumps clean (Decimals coerced, no nulls); universe 100→101 on a temp non-NIFTY100 watchlist insert, restores to 100 on cleanup.
- **Unit 2 — `/watchlist` CRUD (`routers/watchlist.py` NEW + `services/monitored_stocks_audit_service.py` + `main.py`, commit `a250d00189`):** `GET /watchlist` (list, price-enriched via `bulk_get_latest_prices`), `GET /watchlist/{isin}`, `PUT /watchlist/{isin}` (idempotent create-or-update), `DELETE /watchlist/{isin}` (hard-delete, watchlist-only). Reuses `monitored_stocks` with `status="watchlist"` (no new collection). PUT flips status to watchlist (un-excludes a previously rejected/acted ISIN), 404s if the ISIN isn't a known instrument, seeds identity (`symbol/name/exchange/sector/industry`) from the instrument doc on insert, and runs the `Money` patch through `_convert_decimals_to_decimal128` before Mongo. **Write-before-apply audit (F10)** via `log_change` on every mutation — the audit-action `Literal` was WIDENED (not forked) to `AuditAction` (adds `watchlist_add/watchlist_update/watchlist_remove`) so the watchlist path reuses the SAME `monitored_stocks_audit` collection. DELETE 404s on a non-watchlist doc so a feedback row is never nuked. Verified on EC2 (12 checks): `watchlist_add`→`watchlist_update` audit trail, `target_buy_price` survives a re-PUT, un-exclude wiring (`in_universe: True`), and the 404/422 guards all fire.
- **Unit 3 — cron data-volume coverage (`scripts/refresh_fundamentals.py` + `scripts/fetch_news_for_universe.py`, commit `9857570b`):** the default weekly fundamentals universe (`get_nifty100_union_holdings`) now folds in watchlist ISINs via new `get_watchlist_instruments()`; the news fetch universe (both `--include-held` and default branches) folds them in via new `get_watchlist_for_news()`, deduped by ISIN. Both reuse `suggestion_engine.get_watchlist_isins()` (no third copy of the `status=="watchlist"` filter). **This is the F13 data-volume multiplier:** each watchlist name now pulls weekly fundamentals + earnings + daily news + consumes one Tavily call/run against the daily quota (TD33). Tavily blast-radius is a DOCUMENTED soft guardrail (no hard cap on watchlist size — consistent with the calls-only TD33 ceiling, which degrades safely via the atomic quota guard if exceeded). Verified on EC2: union size 117 (NIFTY100 + held + 1), news fold-in present; end-to-end `uv run python -m scripts.refresh_fundamentals --symbols 20MICRONS` → `Succeeded: 1/1`, `18/20 fields present`.
- **Unit 4 — frontend watchlist surface (`lib/api.ts` + `app/watchlist/page.tsx` NEW + `app/page.tsx`, frontend commit `58bf6369`):** added `WatchlistEntry` + `WatchlistUpsertPayload` types + `getWatchlist`/`getWatchlistEntry`/`upsertWatchlist`/`deleteWatchlist` bindings; widened `MonitoredStocksAuditEntry.action` to `FeedbackAction | WatchlistAuditAction` and `new_status` to include `"removed"`. New `/watchlist` page reuses the `/tags` page shell + `components/ui/table` primitives (watchlist rows are `monitored_stocks`, not `Holding[]`, so a purpose table — not `HoldingsTable`); add-control reuses `api.searchInstruments` (symbol→ISIN) and PUTs; per-row Remove DELETEs; mutations use `refetchQueries` + toast + `ApiError.detail` (the buy-sheet convention); target-buy cell highlights when current price is at/below target. Added a Watchlist nav link (Eye icon) to the dashboard header. Per the §21 anti-standalone-sprawl stance, reused existing components where they fit.
- **CORS fix (surfaced during browser testing, `main.py`, commit `67704025`):** add-to-watchlist failed from the dashboard with a CORS preflight error — `PUT` was missing from `CORSMiddleware allow_methods` (`["GET","POST","PATCH","DELETE","OPTIONS"]`). GET/DELETE worked (so list/search/remove worked) but the `PUT /watchlist/{isin}` upsert preflight `OPTIONS` was rejected. curl from inside the box doesn't do CORS preflight, so Unit 2's curl tests passed — it only surfaced from the browser. Fixed by adding `"PUT"` to the explicit method list (no switch to `["*"]`). Verified on EC2 with a simulated preflight `OPTIONS` (now 200 + advertises PUT) + a real PUT (200).

End-to-end verified on EC2 against localhost:8000 and from the browser over Tailscale (add INFY → success toast, row appears with price; remove → disappears). No new tech-debt or follow-up items were filed.

Per the standing rule, the next item (#30, Chat B) continues in a fresh chat to keep context clean.

**Chat 7 (#28 — F12 risk-summary + F15 by-tag) — closed 2026-06-15.** Standalone large feature, built and verified in two backend units + two frontend units, each its own commit. A design proposal was approved before any code (concentration by holding + sector with two-tier alerts; by-tag contract reading `holdings.tags`; both read-only over the existing Phase-1 holdings/price surfaces; frontend risk card on the dashboard + a dedicated `/tags` page):
- **Unit 1 — F12 backend (`app/services/portfolio_service.py` + `app/routers/portfolio.py`, commit `97041621`):** extracted the per-holding annotation loop + running totals out of `compute_summary` into a shared private helper `_annotate_holdings(holdings, latest_prices) -> (annotated, accum)`, then refactored `compute_summary` to call it — behaviour-preserving, gated by a `/portfolio/summary` curl-diff (minus the volatile `as_of`) that printed `OK: /summary unchanged`. Added `compute_risk_summary(holdings, latest_prices)` on the SAME helper (no parallel aggregation), returning `concentration_by_holding` (EVERY priced holding, desc by % — not just top 5), `concentration_by_sector`, and an `alerts` array. Alerts are two-tier from four module constants (the `TOP_MOVERS_LIMIT` pattern): `SINGLE_HOLDING_CONCENTRATION_WARN_PCT=10.0` / `HIGH_PCT=20.0`, `SECTOR_CONCENTRATION_WARN_PCT=30.0` / `HIGH_PCT=50.0`, plus a low-severity `stale_price` data-quality note (reuses the existing `price_stale`/missing-price flag — holdings with no price are excluded from the % denominator, so their weight is understated). New `GET /portfolio/risk-summary` mirrors the `/summary` router skeleton (`find({"deleted_at": None})` → `bulk_get_latest_prices` → compute → `_serialize`). Verified on EC2: 2 warn alerts (GAEL 13.67%, LT 10.02%), 28 holdings, 7 sectors; the risk-summary top concentration is byte-identical to `/summary`'s `concentration[0]`.
- **Unit 2 — F15 backend (`app/routers/portfolio.py`, commit `803e6610`):** `GET /portfolio/by-tag?tag=X` — `tag` is a REQUIRED query param (`Query(..., min_length=1)`; missing/empty → 422). Filters `holdings().find({"deleted_at": None, "tags": tag})` (exact, case-sensitive Mongo array-membership), annotates each row via the SAME path as `holdings.list_holdings` (`bulk_get_latest_prices` + `bulk_get_previous_closes` + `annotate_with_current_price`) so rows render in the existing `HoldingsTable` unchanged, plus a tag-scoped `totals` block (count, invested, current_value, unrealized_pnl, unrealized_pnl_pct). Unknown tag → empty holdings + zeroed totals (200, not 404). Imports the existing `_to_dec` from `portfolio_service` for the totals math (no parallel converter). Verified on EC2: tags discovered (`long-term`, `retail`); `retail` → TATASTEEL, count 1, matching the cross-check against the full holdings list; by-tag holding shape == `/portfolio/holdings` shape; unknown tag → zeroed 200; missing + empty tag → 422.
- **Unit 3 — F12 frontend (`lib/api.ts` + new `components/risk-summary-card.tsx` + `app/page.tsx`, frontend commit):** added `RiskSummary` / `RiskConcentrationHolding` / `RiskConcentrationSector` / `RiskAlert` types + `HoldingsByTag` / `TagTotals` types + `getRiskSummary` and `getHoldingsByTag` wrappers to the hand-typed `lib/api.ts` (the existing `ConcentrationItem` / `SectorBucket` did NOT match the risk-summary shapes, so new types — not reuse). New `RiskSummaryCard` (shadcn Card/Badge/Skeleton, reuses `inr`/`pct` from `lib/format`, severity-colored alert rows, top-5 holdings by weight linking to `/holdings/{isin}`, sector weights). Mounted on the dashboard as its own full-width section below the Sector/Top-Movers grid via an independent `useQuery(["dashboard","risk"])` so a risk-endpoint failure doesn't block the main dashboard. Added a "Tags" nav link to the header.
- **Unit 4 — F15 frontend (new `app/tags/page.tsx`, frontend commit):** dedicated `/tags` route that derives the tag universe from `getHoldings().tags`, renders a tag-pill selector, and on selection calls `getHoldingsByTag(tag)` to show a tag-scoped totals row + the existing `<HoldingsTable>` (reused wholesale). `useQuery(["tags","by-tag",tag])`, `enabled` only when a tag is picked. Verified on EC2: `npm run build` + lint clean via `~/deploy-ui.sh`; the two consumed endpoints alive; dashboard risk card + `/tags` page render correctly.

Both endpoints are READ-ONLY over the existing Phase-1 holdings/price surfaces; no new collections, no new indexes, no new npm dependency. No new tech-debt or follow-up items were filed.

Per the standing rule, the next item (#29, Chat 8) continued in a fresh chat to keep context clean.

**Chat A (Ops & alerting quick-wins) — closed 2026-06-14.** Bundled seven small, independent, backend/doc-only rows and cleared all of them, each marked SHIPPED individually:
- **#34 (Ops gap, `app/main.py`):** `GET /health` was hardcoding `{"status":"ok"}` + HTTP 200 even when the Mongo `ping()` failed (it only flipped the `mongo` field). Now returns 503 + `{"status":"degraded","mongo":"fail"}` on ping failure, 200 + `{"status":"ok","mongo":"ok"}` on success — aligns code with the README §10 claim. yfinance deliberately NOT probed on the hot path (a Yahoo throttle would cause false 503s; price-source health stays in the `refresh_prices*` cron heartbeats). Commit `bd52c6b`.
- **#35 (Ops gap, `scripts/refresh_prices_intraday.py`):** `insert_intraday_quotes` wrapped in try/except — on failure (market is open by construction once `fetch_intraday_quotes` returns rows) it fires a GUARDED `push_public("errors", ...)` then re-raises so `cron_run` still records the failure heartbeat. Commit `bd52c6b`.
- **#25 (P2-7, `app/services/reconciliation.py`):** `take_auto_snapshot` was silent to Mongo only (notes string). Now fires `push_public("price", ...)` when invested drift vs the last manual snapshot exceeds `DRIFT_ALERT_THRESHOLD_INVESTED` (₹1,000). ntfy ONLY (the manual `_send_drift_alerts` keeps its dual ntfy+email transport; the daily auto cron would make email noise). Rising-edge dedupe: alert fires only when this snapshot has drift AND the most recent prior auto snapshot did not, so a standing divergence does not re-push every day; a fresh manual snapshot resets the baseline and re-arms. Current-value drift intentionally NOT alerted on the auto path (multi-day gap dominated by live-price movement). `drift_invested`/`has_drift`/`alerts_sent` recorded on the auto snapshot (the model already declares them optional; route returns `_serialize(dict)` with no `response_model`, so no shape break). New `_send_auto_drift_alert` mirrors only the ntfy half of the existing alerting — evolved in place, not a parallel alerter. Commit `1340396`.
- **#49 (TD40, `app/services/cron_heartbeat_service.py`):** the idle `weekly_suggestions_sell` CronSpec carried `expected_weekdays={6}`, so `cron_health_check` fired a false `MISSING: weekly_suggestions_sell` every Sunday even though the umbrella `weekly_suggestions --direction=both` cron covers both directions. Set `expected_weekdays=set()` so `is_expected_today()` is always False for the idle entry and the MISSING/FAILED checks skip it; the spec stays in the registry/dashboard as `expected=False`. NO change to the #23/TD38-hardened `_persist` / `_append_fallback` / `count_today_heartbeats_from_fallback` paths (re-read at HEAD; left untouched). Commit `6032b64`.
- **#26 (P2-6 / TD1, `app/routers/suggestions.py`):** the literal "add `payload.direction`" would have crashed — `SuggestionFeedback` had only `action` + `note` with `extra="forbid"`, so there was no `payload.direction`. Added `direction: Literal["buy","sell"] = "buy"` (default keeps the current frontend, which sends only `{action, note}`, working — a required field would 422 every existing submit). The relabel query filters by direction, using the repo's existing back-compat guard `{"$or":[{"direction":"buy"},{"direction":{"$exists":False}}]}` for the buy branch (matching `get_latest_run` / `compute_system_performance`) and a bare `{"direction":"sell"}` for sell. Does NOT close TD1/#43 — cross-direction relabel policy stays DEFERRED. Commit `6032b64`.
- **#47 (TD22, `scripts/track_suggestion_outcomes.py`):** investigation-first. The daily 19:45 IST cron failed every weekday (0 success / 1 failure) → fired the 21:00 IST health email daily. Root cause (reproduced live at `6032b64`): `snapshot_open_outcomes()` returns its count under `active_outcomes` (renamed in Commit A.5 when selection broadened from "open" to all non-expired), but the cron's `main()` still read `stats["open_outcomes"]` in both `hb.metadata.update(...)` and the result print → `KeyError` on every run, raised inside the `cron_run` context so the heartbeat recorded a failure at line 27 before any result line flushed (explaining the empty `cron-outcomes.log` — the traceback went to stderr, not the log redirect). Fix: read `stats["active_outcomes"]` and rename the heartbeat metadata key + print label to match. Safe rename — the heartbeat never persisted `open_outcomes` (it always KeyError'd first), so nothing downstream depends on the old key; no schema/migration. Verified on EC2: pre-fix run reproduced the KeyError + failure heartbeat; post-fix run exits 0, prints "Active outcomes: 115", persists a `success` heartbeat with `active_outcomes` in metadata. Root cause was a deterministic single-file key drift — well within a quick fix, no STOP needed. Commit `4b638e6`.
- **#48 (TD36, DOC-ONLY, `README.md` + `docs/data_flow.md`):** corrected the stale "monthly" Tavily-quota wording to "daily (resets 00:00 UTC)". The code check (per the read-the-code rule) also surfaced two further doc bugs fixed in the same commit: data_flow said "resets at 00:00 IST on the 1st" (the code keys the quota on `date_utc` and `TavilyQuotaExceeded` states "Resets at 00:00 UTC"), and README documented a non-existent env var `TAVILY_MONTHLY_QUOTA` (the real one is `TAVILY_DAILY_CALL_LIMIT`, enforced in `tavily_client`, not `news_fetcher`). No `app/` or `scripts/` code touched. Project_State Sections 5/7/10/12/14 were already correct. Commit `fae6edf`.

Per the standing rule, the next item (#27, Chat 6) continued in a fresh chat to keep context clean.

**Chat 6 (#27 — F1 + F3 ad-hoc chat) — closed 2026-06-14.** Standalone large feature, built and verified in four units plus one latent-bug fix:
- **Unit 1 (data layer):** `app/models/conversation.py` evolved from placeholder to the real `Conversation(BaseDoc)` write model + added a `scope: Literal["suggestions","holding"]` discriminator (kept distinct from `intent` — surface vs question classification); added `(scope, created_at desc)` index `scope_created_desc`; added `instrument_service.lookup_by_isin(isin)` reverse lookup (ISIN -> instrument, NSE-preferred) to support researching not-yet-owned stocks. `Collections.conversations()` accessor and the four base conversations indexes already existed at HEAD; `init_db.py` needed no edit (calls `ensure_all_indexes()` generically). Commit `4403bb5` open base → Unit 1 commit.
- **Unit 2 (enrichment orchestrator):** `app/services/conversation_service.ensure_stock_context(isin)` — resolves identity, then ensures fundamentals (14d freshness), upcoming earnings, and recent classified news exist, fetching on demand via the existing `fundamentals_service` / `news_fetcher` / `news_classifier` (the same cron-path services). Freshness gates keep a warm turn external-call-free; `TavilyQuotaExceeded` + fetch failures degrade gracefully to cached data. Writes ONLY Phase-2 reference collections (fundamentals/earnings/news), never Phase-1 portfolio data.
- **Unit 3 (chat service + API):** context builders — buy-research framing for not-yet-owned names, position/tax overlay for held names (LTCG>365d / near-LTCG / weight, reimplemented from `_build_position_context_block` semantics since the dossier helper is `CandidateScore`-coupled), latest buy+sell run summary for F1 — a single structured Sonnet call returning a `{answer, intent}` JSON envelope (mirrors `dossier_service._generate_one`: lazy Anthropic import, `ANTHROPIC_MODEL_PRIMARY`, collect `block.text`, parse + one retry + graceful fallback), token/cost capture (Sonnet $3/$15 per MTok module constants -> `Money` cost_usd), and Conversation persistence (insert then re-read the doc so the router serializes POST responses and history rows through one path). New `app/routers/conversations.py`: `POST /chat/suggestions` (F1), `POST /chat/holdings/{isin}` (F3, ISIN `pattern=r"^[A-Z0-9]{12}$"`, 404 on unknown instrument), `GET /chat/history` (scope/isin filtered); registered in `app/main.py`. Reuses `dossier_service._to_float`/`_format_news_summaries`, `suggestion_engine`, `price_service` verbatim.
- **Unit 4 (frontend):** `lib/api.ts` chat + instrument-search types and wrappers (`chatSuggestions`, `chatHolding`, `getChatHistory`, `searchInstruments`); new `components/chat-panel.tsx` (transcript + composer + sentiment toggle + pending state + a self-contained markdown renderer, since the repo has no markdown dependency — keeps deploy a plain `npm run build`); new `components/stock-research-panel.tsx` (symbol search via the existing `/instruments/search` -> ChatPanel bound to `/chat/holdings/{isin}` for a not-yet-owned stock). Embedded on `/suggestions` (F1 + research panel) and `/holdings/[isin]` (F3). TanStack `refetchQueries` convention. Frontend HEAD `6093f63`.
- **Route-shadow fix (surfaced during F3 testing, `app/routers/instruments.py`):** the dynamic `/{exchange}/{symbol}` route was declared before the static `/search/{symbol_prefix}`, so `GET /instruments/search/INFY` was captured as `exchange=search, symbol=INFY` and 404'd — the search endpoint was unreachable over HTTP (latent since it was added; scripts call the service layer directly, so the chat research panel was its first HTTP caller). Reordered search-before-dynamic + added a NOTE so the ordering isn't reintroduced. Filed under #27 (no separate row). Backend HEAD `5e787c9`.

End-to-end verified on EC2 against localhost:8000: F1 returns the real weekly run with cited ISINs (HINDZINC/JSWSTEEL/VBL); F3 held (Reliance) works the +6.3% gain / LTCG / 1.6% weight / July-17 earnings overlay; F3 not-held (HDFC Bank, INFY) gives buy-research analysis with fundamentals + classified news; unknown ISIN -> 404; bad ISIN shape -> 422; history newest-first + scope/isin filtered; `model_used=claude-sonnet-4-5`, token counts + `cost_usd` populated.

Two issues surfaced during #27 testing, filed as new rows (NOT fixed this chat): **#50** news entity mis-tagging (the HDFCBANK chat returned TCS/Kenya articles — wrong `entities_isins` in `news_articles`, upstream of chat in `news_fetcher`/`news_classifier`) and **#51** `dividend_yield` ×100 formatting shared by `dossier_service._fmt_pct` and the chat formatter (Reliance showed "46%"; verified the chat formatter is byte-consistent with the dossier `_fmt_pct`, so it is a pre-existing app-wide yfinance unit inconsistency, not new to #27).

Items completed since this file was created:
- #1 (TD14) — SHIPPED 2026-06-02 (Chat 5.9)
- #2 (TD10) — SHIPPED 2026-06-02 (Chat 5.9)
- #3 (TD15) — SHIPPED 2026-06-02 (Chat 5.9)
- #4 (TD16) — SHIPPED 2026-06-06 (Chat 5.10)
- #6 (TD18) — SHIPPED 2026-06-06 (Chat 5.10)
- #5 (TD17) — SHIPPED 2026-06-06 (Chat 5.10)
- #7 (TD19) — SHIPPED 2026-06-06 (Chat 5.10)
- #8 (TD20) — SHIPPED 2026-06-06 (Chat 5.10)
- #9 (TD23) — SHIPPED 2026-06-08 (Chat 5.11)
- #10 (TD24) — SHIPPED 2026-06-08 (Chat 5.11)
- #11 (TD25) — SHIPPED 2026-06-08 (Chat 5.11)
- #12 (TD26) — SHIPPED 2026-06-08 (Chat 5.12)
- #13 (TD27) — SHIPPED 2026-06-08 (Chat 5.12)
- #14 (TD28) — SHIPPED 2026-06-08 (Chat 5.13)
- #15 (TD29) — SHIPPED 2026-06-08 (Chat 5.13)
- #16 (TD30) — SHIPPED 2026-06-08 (Chat 5.13)
- #17 (TD31) — SHIPPED 2026-06-08 (Chat 5.13)
- #18 (TD32) — SHIPPED 2026-06-08 (Chat 5.13)
- #19 (TD33) — SHIPPED 2026-06-09 (Chat 5.14)
- #20 (TD34) — SHIPPED 2026-06-12 (Chat 5.15)
- #21 (TD35) — SHIPPED 2026-06-12 (Chat 5.16)
- #22 (TD37) — SHIPPED 2026-06-12 (Chat 5.17)
- #23 (TD38) — SHIPPED 2026-06-12 (Chat 5.18)
- #24 (TD39) — SHIPPED 2026-06-14 (Chat 5.19)
- #34 (Ops gap) — SHIPPED 2026-06-14 (Chat A) — commit `bd52c6b`
- #35 (Ops gap) — SHIPPED 2026-06-14 (Chat A) — commit `bd52c6b`
- #25 (P2-7) — SHIPPED 2026-06-14 (Chat A) — commit `1340396`
- #49 (TD40) — SHIPPED 2026-06-14 (Chat A) — commit `6032b64`
- #26 (P2-6 / TD1) — SHIPPED 2026-06-14 (Chat A) — commit `6032b64`
- #47 (TD22) — SHIPPED 2026-06-14 (Chat A) — commit `4b638e6`
- #48 (TD36) — SHIPPED 2026-06-14 (Chat A) — commit `fae6edf`
- #27 (Chat 6 / F1 + F3) — SHIPPED 2026-06-14 (Chat 6) — backend `5e787c9`, frontend `6093f63`
- #28 (Chat 7 / F12 + F15) — SHIPPED 2026-06-15 (Chat 7) — backend Unit 1 `97041621`, Unit 2 `803e6610`; frontend `e14d6a75`
- #29 (Chat 8 / F13 watchlist) — SHIPPED 2026-06-15 (Chat 8) — backend Unit 1 `34ff906d`, Unit 2 `a250d001`, Unit 3 `9857570b`, CORS fix `67704025`; frontend `58bf6369`
- #30 (P2-1) — SHIPPED 2026-06-15 (Chat B) — 11 `datetime.utcnow()`->`utcnow()` sites across 5 files (backend `025b8a0`)
- #31 (P2-8) — SHIPPED 2026-06-15 (Chat B) — tree-wide tz-aware Mongo-write sweep + NEW `scripts/check_datetime_hygiene.py` tokenize-based guard (backend `025b8a0`)
- #32 (P3-2) — SHIPPED 2026-06-15 (Chat B) — pin `requires-python = ">=3.12,<3.14"` + `uv lock` relock (backend `025b8a0`)
- #33 (Review note) — SHIPPED 2026-06-26 (Chat B) — hermetic zero-dependency pytest harness, 28 tests across 6 targets + `tests/_fakes.py` FakeCollection + `tests/conftest.py` fake_db fixture (backend `04fd970`)
- #36 (Ops gap) — SHIPPED 2026-07-01 (Chat B) — `POST /admin/recompute/{isin}` Tailscale-only admin recompute endpoint; NEW `app/routers/admin.py` delegating to `holdings_service.recompute_holding` (TD20), registered in `app/main.py` (backend `1ef0ead`)

When you finish an item, change its row's Status column from `OPEN` to `SHIPPED <YYYY-MM-DD> (Chat <N>)` and advance the "Next item to start" pointer. Do not delete shipped rows — they are the audit trail.

---

## Chat bundles

Execution overlay (added Chat 5.19) to reduce the number of chats to open. This GROUPS the existing numbered rows into chats — it does NOT renumber, move, or merge rows. Item numbers, phase membership, Status, and the ordering rationale below are all unchanged and remain the source of truth. Bundle = "open one chat, clear these items together."

Bundling policy: bundle only small, independent, same-surface items; keep each large feature one chat (avoids the Section-16 context-loss risk). Conservative plan (chosen Chat 5.19).

**Next chat to open: Chat B (#37 — continuing)** — Chat A is COMPLETE (all seven rows SHIPPED 2026-06-14), Chat 6 is COMPLETE (#27 SHIPPED 2026-06-14), Chat 7 is COMPLETE (#28 SHIPPED 2026-06-15), and Chat 8 is COMPLETE (#29 F13 watchlist SHIPPED 2026-06-15). Phase 8 is fully shipped. Chat B has shipped #30, #31, #32 (2026-06-15), #33 (2026-06-26), and #36 (2026-07-01); the lowest-numbered OPEN item is now #37.

| Chat | master_todo items | Theme | Surface | Notes |
|---|---|---|---|---|
| **A — Ops & alerting quick-wins** | #25, #26, #34, #35, #47, #48, #49 | Reconciliation/feedback alerting + ops gaps + cron false-positives + doc | Backend + doc only | COMPLETE — all seven SHIPPED 2026-06-14 (Chat A). #34/#35 `bd52c6b`; #25 `1340396`; #49/#26 `6032b64`; #47 `4b638e6`; #48 `fae6edf`. |
| **B — Pre-GO-LIVE hygiene sweep** | #30, #31, #32, #33, #36, #37, #38 | Cross-cutting cleanup (whole Phase 9) | Backend + ops | IN PROGRESS (Chat B). #30 + #31 (same datetime sweep) + #32 SHIPPED 2026-06-15; #33 pytest harness SHIPPED 2026-06-26; #36 admin recompute SHIPPED 2026-07-01; #37, #38 remain. |
| **C — UI cleanup** | #40, #41 | Realized-P&L hide + stop_loss wiring | Frontend-led (#41 adds backend) | Both share the holding drill-down / holding-stats surface. |
| **D — Deferred TDs** | #43, #44, #45 | Post-launch internal cleanup (TD1/TD3/TD7) | Backend + frontend | Run after GO LIVE is stable. |
| **Chat 6** (standalone) | #27 | F1+F3 ad-hoc chat feature | Backend + frontend | COMPLETE — SHIPPED 2026-06-14 (Chat 6). 4 units + 1 route-shadow fix. Filed #50 + #51. |
| **Chat 7** (standalone) | #28 | F12 risk-summary + F15 tag views | Backend + frontend | COMPLETE — SHIPPED 2026-06-15 (Chat 7). 2 backend units (`97041621`, `803e6610`) + 2 frontend units (`e14d6a75`). Read-only; no new collections/indexes/deps. |
| **Chat 8** (standalone) | #29 | F13 watchlist | Backend + frontend | COMPLETE — SHIPPED 2026-06-15 (Chat 8). 4 units (`34ff906d`, `a250d001`, `9857570b`, frontend `58bf6369`) + 1 CORS fix (`67704025`). Evolved build_universe + reused monitored_stocks; no new collection/index. |
| **Chat 9** (standalone) | #39 | F11 capital-gains pack | Backend + frontend | Large feature — new tax router/service + page. Keep alone. |
| **User Review chat** (standalone) | #52 | F-USERACCEPT complete user acceptance review / UAT | Cross-cutting (you drive) | NEW (Chat 8). Second-to-last stage. You walk every page/flow/cron and file findings as their own rows in NEW ITEMS (no renumbering). GO LIVE (#42) must not start until blocking findings are cleared or deferred. |
| **Chat 10 — GO LIVE** (standalone) | #42 | F7 real ICICI data import | Backend | Last, alone, so test pollution wipes in one operation. Gated behind the User Review chat (#52). |
| **Scheduler chat** (standalone) | #46 | TD21 registry-generated crontab migration | Backend + ops | Section 9 explicitly mandates its own dedicated chat. |

Net: remaining OPEN work goes from ~7 chats → 6 (Chats B–D + 4 standalone: Chat 9, User Review, GO LIVE, Scheduler; Chats A, 6, 7 and 8 done). Sequencing still follows the Ordering rationale + per-row gating; bundles never override a gating dependency. Recommended order: Chat B → Chat 9 → Chat C → User Review (#52) → Chat 10 GO LIVE → Scheduler → Chat D, adjusting for whatever you want next. When you finish a bundle, mark each of its rows SHIPPED individually (the bundle is a chat-grouping, not a single work item) and advance the "Next item to start" pointer to the lowest-numbered OPEN row as usual.

---

## Ordering rationale

Ordered to minimize rework. Principle: **fix the code surface before adding features on top of it.**

1. **Phase 1** — Unblock ops first (no code; immediate value; restores weekly digest).
2. **Phase 1** — Reconcile documentation (TD15) before any chat that reads files with F-comments; otherwise future chats hallucinate against unmapped F-numbers.
3. **Phase 2** — Fix transactions/holdings/audit invariants BEFORE Chat 9 touches `holdings` (stop_loss + realized P&L hide). SHIPPED Chat 5.10.
4. **Phase 3** — Fix intraday correctness early; every dashboard load and every sell-side suggestion depends on it. SHIPPED Chat 5.11.
5. **Phase 4** — Storage hygiene (TTL + body purge) BEFORE Chat 10 GO LIVE — real ICICI data import is when collections start filling for keeps. SHIPPED Chat 5.12.
6. **Phases 5-7** — Frontend correctness + external-service hardening + reconciliation alerting; mostly independent of one another, can be batched. Phase 5 SHIPPED Chat 5.13; Phase 6 #19 SHIPPED Chat 5.14, #20 SHIPPED Chat 5.15, #21 SHIPPED Chat 5.16, #22 SHIPPED Chat 5.17, #23 SHIPPED Chat 5.18, #24 SHIPPED Chat 5.19 — Phase 6 COMPLETE. Phase 7 (#25, #26) SHIPPED Chat A — Phase 7 COMPLETE.
7. **Phase 8** — New features (Chats 6-8) AFTER underlying services are correct; Chat 8 (watchlist) must come after Phase 4 (storage) + Phase 6 (Tavily race) since it multiplies data volume. Chat 6 (#27 F1+F3 ad-hoc chat) SHIPPED Chat 6; Chat 7 (#28 F12 risk-summary + F15 by-tag) SHIPPED Chat 7 — both read-only over existing surfaces. Chat 8 (#29 F13 watchlist) SHIPPED Chat 8 — evolved build_universe + reused monitored_stocks; the two crons fold in watchlist ISINs (the data-volume multiplier, Tavily blast-radius documented as a soft guardrail). **Phase 8 COMPLETE.**
8. **Phase 9** — Cross-cutting cleanup (`datetime.utcnow()` sweep, Python ceiling, pytest harness, ops gaps) right before GO LIVE so launch lands on one clean state. (The two ops-gap quick-wins #34 + #35 were pulled forward into Chat A as small/independent alerting fixes; #30 + #31 (datetime sweep) + #32 (Python ceiling) SHIPPED in Chat B 2026-06-15; #33 (pytest harness) SHIPPED in Chat B 2026-06-26; the rest of Phase 9 — #36-#38 ops gaps — remains in Chat B.)
9. **Phase 10** — Chat 9 pre-launch cleanup (F11 + realized P&L hide + stop_loss wiring).
10. **Phase 10.5** — USER ACCEPTANCE REVIEW (#52, NEW Chat 8). You personally walk the entire tool end-to-end and file every bug / issue / change request. Second-to-last stage; GO LIVE must not start until blocking findings are resolved or explicitly deferred. The system is "production-ready" only AFTER this review is cleared.
11. **Phase 11** — Chat 10 GO LIVE (F7 real data import) — last, so test pollution gets wiped in one operation. Gated behind Phase 10.5.
12. **Phase 12** — Deferred TDs (TD1, TD3, TD7) — after launch is stable.
13. **New items (Chat 5.9 / 5.16 / 5.19 / Chat 6)** — TD21 (registry-generated crontab migration) remains OPEN; TD22 (track_suggestion_outcomes daily failure) SHIPPED Chat A; TD36 (Tavily monthly→daily doc cleanup) SHIPPED Chat A; TD40 (weekly_suggestions_sell Sunday false MISSING) SHIPPED Chat A; #50 (news entity mis-tagging) + #51 (dividend_yield ×100 formatting) filed Chat 6 and OPEN; see the NEW ITEMS phase below.

---

## Item legend

- **Source column codes:**
 - `TD<N>` — Tech debt registry row in `Project_State.md` Section 18
 - `P0/P1/P2/P3-<N>` — Code review finding (see `code_review_findings_chat_6_audit.md` if archived, or the Chat 5.8 review)
 - `Chat <N>` — Pre-existing chat in the Section 13 chat split plan
 - `F<N>` — Feature ticket (mirrored from external registry; see TD15)
 - `Ops gap` — Operational gap called out in the review's ops-gaps section
 - `User request` — Stage/row added at the user's explicit direction in a chat
- **Status column codes:**
 - `OPEN` — Not started
 - `IN PROGRESS (Chat <N>)` — Being worked in the named chat
 - `SHIPPED <YYYY-MM-DD> (Chat <N>)` — Done; commit landed
 - `DEFERRED` — Acknowledged and intentionally pushed to a later phase
 - `DROPPED` — Explicitly de-scoped; note rationale in the row

---

## PHASE 1 — Unblock operations (no code; do this week) — SHIPPED Chat 5.9

| # | Source | Item | Files / surface | Status |
|---|---|---|---|---|
| 1 | TD14 / P1-5 | Fix Sunday 07:00 IST crontab line: drop bogus `--notify --run-type scheduled` flags. Optional: run `scripts/run_weekly_suggestions.py --direction=both` manually for immediate digest recovery. Also confirm whether nightly `cron_health_check` ntfy + email alerts have actually been arriving — if not, second silent failure in dual-transport path. **Chat 5.9 closed build-right: Part A flags removed from crontab (verified via `crontab -l`); Part B `CRON_REGISTRY` entry renamed `run_weekly_suggestions` → `weekly_suggestions` (commit `c097b473`) to match the heartbeat the script writes, killing the phantom Sunday MISSING. Dual-transport confirmed HEALTHY by inspection (email + ntfy both arrive daily). The daily 21:00 health alert is a SEPARATE failure → filed as #47 (TD22).** | EC2 `crontab -e` + `app/services/cron_heartbeat_service.py` | SHIPPED 2026-06-02 (Chat 5.9) |
| 2 | TD10 | Remove redundant `0 0 * * 0 find ... -size +10M ...` crontab line (logrotate replaces it). Verify first logrotate cycle completed (next: 2026-05-31 Sun 00:00 IST window) then remove. **Chat 5.9: GATE PASSED (rotation trail `cron-*.log.1` 2026-05-31 + `.2.gz` 2026-05-24 present for all 10 logs) AND the `find -size +10M` line was found ABSENT from the live crontab — already removed in a prior session or never deployed. End state satisfied; no edit needed.** | EC2 `crontab -e` | SHIPPED 2026-06-02 (Chat 5.9) |
| 3 | TD15 | Reconcile F-number fix registry: read every file at HEAD that carries an F-comment (F2/F3/F4/F5/F7/F8/F12/F14/F16-F21/F23/F27-F29/F79/F80/F82), map each F-number to its file + one-line description, add rows to `Project_State.md` Section 18. Do BEFORE any code chat. Also: this may surface that items #26 (P2-6) and #43 (TD1) are already partially addressed by an F-ticket. **Chat 5.9: grepped at HEAD `c097b473` — 25 unique in-code F-numbers (the "~20" estimate was low; fix-registry subset is 21) across TWO colliding namespaces (feature-F vs fix-Chat-5.5+-F). Authored the "F-number fix registry" subsection in Section 18 with a Kind column. Recovered the truncated Section 18 (and Sections 16-tail through 22) that the Chat 5.8 doc commit `8f74b50` had silently amputated. No overlap found that lets #26 or #43 drop — both stay OPEN/DEFERRED.** | `docs/Project_State.md` | SHIPPED 2026-06-02 (Chat 5.9) |

## PHASE 2 — Transactions / holdings / audit consistency — SHIPPED Chat 5.10

Fix this surface before Chat 9 touches it. All five items shipped + verified on EC2 against localhost:8000.

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 4 | P1-1 | Make `PATCH /transactions/{id}` and `DELETE /transactions/{id}` write `transactions_audit` BEFORE applying the change. Mirror the suggestions feedback handler pattern. **Chat 5.10 SHIPPED: both handlers now `log_change(...)` BEFORE `update_one(...)`; PATCH audits a computed `{**before, **update_fields}` after-state then applies then re-reads for the response; `validate_replay` still runs first so a rejected edit/delete writes no audit row.** | `app/routers/transactions.py` `edit_transaction` + `delete_transaction` | SHIPPED 2026-06-06 (Chat 5.10) — commit `17f9f94` |
| 5 | P1-3 | Add `validate_replay` to `/portfolio/holdings/{isin}/sell` and to the manual import path so backdated SELLs that produce negative quantity get 400'd, not silently logged. **Chat 5.10 SHIPPED: `/sell` replays `existing_txns + [proposed_sell]` and 400s before the ledger write; `add_manual_transactions.py` gates SELL inserts on `validate_replay` and aborts with `RuntimeError` instead of silently staging. Existing point-in-time `held_qty` check kept for the clearer common-case message.** | `app/routers/holdings.py` `sell`; `scripts/add_manual_transactions.py` insert loop | SHIPPED 2026-06-06 (Chat 5.10) — commit `5cf3087` |
| 6 | P1-2 | Delete duplicate route handler `list_transactions` (lines ~329-335); keep `get_holding_transactions` (~163-180). **Chat 5.10 SHIPPED: deleted the shadowed EOF `list_transactions` handler; `get_holding_transactions` is the sole handler for `GET /portfolio/holdings/{isin}/transactions`. Behaviour-neutral (first-registered handler already won every request).** | `app/routers/holdings.py` | SHIPPED 2026-06-06 (Chat 5.10) — committed after `17f9f94`, before `5cf3087` |
| 7 | P2-9 | Make `add_buy` / `sell` non-atomic path safer: wrap `recompute_holding` in try/except, return success with a warning flag if recompute fails. Or use Mongo M10 transactions for atomicity. **Chat 5.10 SHIPPED (warning-flag path, user-confirmed): both handlers wrap `recompute_holding` in try/except; on exception they `log.exception(...)` and return 2xx `{status:"recorded_with_warning", isin, warning}` so the persisted ledger write isn't masked. `recompute_holding` returning None stays a legitimate full-exit success outside the except. M10 multi-doc transactions explicitly REJECTED — per-step session latency on a single-user box not worth it.** | `app/routers/holdings.py` `add_buy` + `sell` (+ module logger) | SHIPPED 2026-06-06 (Chat 5.10) — commit `fb23307` |
| 8 | P2-10 | Serialize `recompute_holding` per-ISIN: per-ISIN advisory lock doc with TTL, OR API-layer mutex keyed by ISIN. **Chat 5.10 SHIPPED (advisory-lock doc, user-confirmed): `recompute_holding` wraps its body (renamed `_recompute_holding_impl`) in `_per_isin_recompute_lock`, a CM that inserts a doc keyed `_id==isin` into the new `recompute_locks` collection (atomic via unique `_id` index), releases in `finally`, and is TTL-reclaimed after 60s. `asyncio.Lock` REJECTED — confirmed at HEAD that every holdings handler is sync `def` under sync Uvicorn, so asyncio.Lock wouldn't apply; `threading.Lock` rejected because it's blind to the out-of-process scripts. Lock lives at the service layer so all callers (API + scripts) are covered.** | `app/services/holdings_service.py`; `app/db/client.py`; `app/db/indexes.py` | SHIPPED 2026-06-06 (Chat 5.10) — commit `b34721e` |

## PHASE 3 — Intraday & price correctness — SHIPPED Chat 5.11

All three items shipped in ONE backend code commit (`a2806cd`) + verified on EC2 against localhost:8000. All three touch only `app/services/price_service.py`.

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 9 | P1-4 / TD23 | Guard `_intraday_row_from_df` against yfinance returning yesterday's bar on market holidays: if bar timestamp's IST date != today, return None. **Chat 5.11 SHIPPED: the function now reads the latest 5m bar's index timestamp (`clean.index[-1]`) and returns None when its IST date != today's IST date — yfinance `period="1d"` returns the prior trading day's bars on an NSE holiday. Added module-level `IST = timezone(UTC+5:30)` (India has no DST) + `_to_ist()` helper (tz-aware → `astimezone`; tz-naive → treated as UTC first, matching the existing `_df_to_rows` / `annotate_with_current_price` naive→UTC convention). "today" is derived from the passed-in `captured_at`. Verified on EC2: a today-dated synthetic bar returns a dict; a yesterday-dated bar returns None.** | `app/services/price_service.py` `_intraday_row_from_df` (+ new `IST`/`_to_ist` helpers) | SHIPPED 2026-06-08 (Chat 5.11) — commit `a2806cd` |
| 10 | P2-14 / TD24 | Align `price_stale` docstring vs code: doc says "4 trading days", code says `timedelta(days=6)`. Pick one; pragmatic fix is update docstring. **Chat 5.11 SHIPPED (CODE is canonical — user-delegated decision): kept `timedelta(days=6)`; updated the docstring "more than 4 trading days old" → "more than 6 calendar days old" and added an inline comment noting 6 calendar days ≈ 4 NSE trading days across a weekend (the clear original intent). Doc-/comment-only; zero behaviour change. Verified: `/portfolio/holdings` still returns `price_stale` correctly (false on a fresh price).** | `app/services/price_service.py` `price_stale` docstring + inline comment | SHIPPED 2026-06-08 (Chat 5.11) — commit `a2806cd` |
| 11 | P2-13 / TD25 | Rewrite `bulk_get_previous_closes` to push the filter into the Mongo pipeline (or loop `find_one` per ISIN). Currently pulls ~34k price docs per dashboard request. **Chat 5.11 SHIPPED (per-ISIN `find_one` — user-delegated decision): rewrote the body to delegate to the existing single-ISIN `get_previous_close` (an indexed `find_one({"date": {"$lt": latest}}, sort=[("date",-1)])` point-query per ISIN) instead of `$push`-ing every `{date, close}` for every ISIN into an in-memory `all_dates` array and filtering in Python. Eliminates the ~34k-doc pull per dashboard request; keeps the Decimal128/Decimal normalization in exactly one place. Chosen over an aggregation-pipeline rewrite because it evolves existing code (no new query pattern) and the `(isin, date)` index makes each call a single-doc point-query. Verified on EC2: `bulk_get_previous_closes` result is byte-identical to per-ISIN `get_previous_close` for all held ISINs.** | `app/services/price_service.py` `bulk_get_previous_closes` | SHIPPED 2026-06-08 (Chat 5.11) — commit `a2806cd` |

## PHASE 4 — Storage hygiene (must land BEFORE Chat 10 GO LIVE) — SHIPPED Chat 5.12

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 12 | P2-3 | Add TTL index on `prices_intraday.captured_at` with `expireAfterSeconds = 90 * 86400`. **Chat 5.12 SHIPPED (TD26): confirmed at HEAD that `_intraday_row_from_df` writes `captured_at` as a BSON Date (`datetime.now(timezone.utc)`, serialized to UTC Date) so the TTL actually expires docs — a TTL silently no-ops on a string/Decimal field. Added `captured_at_ttl` (ASC, `expireAfterSeconds = 90*86400 = 7776000`) alongside the existing non-TTL `captured_at_desc` (ASC vs DESC are different key patterns, so both coexist — mirrors `cron_heartbeats` `started_at_ttl` + `started_at_desc`). `ensure_all_indexes` stays purely additive (no drop). Verified on EC2: `db.prices_intraday.getIndexes()` shows `captured_at_ttl` on `{captured_at:1}` with `expireAfterSeconds:7776000`; all four indexes intact.** | `app/db/indexes.py` | SHIPPED 2026-06-08 (Chat 5.12) |
| 13 | P2-4 | New `scripts/purge_news_bodies.py` daily cron: `$unset {body_text: ""}` on classified `news_articles` whose `fetched_at` is older than 30 days; stamp `body_purged_at`. Register in `CRON_REGISTRY`. **Chat 5.12 SHIPPED (TD27): field corrections vs the original spec — the bulky field is `body_text` (NOT `body`), and age keys on `fetched_at` (always present via `default_factory=utcnow`; `published_at` is nullable so it would strand undated docs). Script mirrors `refresh_prices_intraday.py` (`cron_run("purge_news_bodies")` heartbeat, `hb.metadata[...]`, `hb.mark_skipped("no_expired_bodies")` on nothing-to-do); adds `--dry-run` (count-only). Filter `{classified:True, fetched_at:{$lt:cutoff}, body_purged_at:None, body_text:{$nin:["",None]}}`; write `{$unset:{body_text:""}, $set:{body_purged_at: now}}` (idempotent). `CronSpec.cron_name == cron_run() name == "purge_news_bodies"` (TD14 contract). Crontab line at 02:30 IST (`30 2 * * *`) with `>> cron-news-purge.log 2>&1`. Verified on EC2 against the real `portfolio` DB: dry-run reported 1 candidate, live run purged 1, sentinel returned `body_text` absent + `body_purged_at` a Date, success heartbeat `metadata.purged:1`; `/cron/heartbeats` shows it `healthy:true, expected_today:true`.** | `scripts/purge_news_bodies.py` (NEW) + crontab + `CRON_REGISTRY` | SHIPPED 2026-06-08 (Chat 5.12) |

## PHASE 5 — Frontend correctness & quick wins — SHIPPED Chat 5.13

Spans BOTH repos: #14 is frontend (one commit, frontend HEAD `f59958`); #15/#17/#18 are backend (three commits, deployed code HEAD `090d96c`); #16 is a doc-drift confirmation (no code). All verified on EC2 — frontend via `~/deploy-ui.sh` + `npm run build`; backend via `ssh ubuntu@100.112.20.41` + `curl localhost:8000` with positive landed-assertions per item.

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 14 | P2-2 | Swap `invalidateQueries` → `refetchQueries` in `notes-panel.tsx` (lines 43, 46) and `refresh-button.tsx` (lines 17-19). **Chat 5.13 SHIPPED (TD28): minimal name-swap (no `async`/`await` reorder). At HEAD the two `notes-panel.tsx` calls were at lines 42 + 45 (`["holding", holding.isin]`, `["dashboard"]`); the three `refresh-button.tsx` calls were at lines 17-19 inside the existing `await Promise.all([...])` (`["dashboard"]`, `["reconciliation"]`, `["cost-basis"]`). Aligns these two outliers with the project-wide synchronous-refetch convention (Project_State Section 14). Verified on frontend HEAD `f59958`: `grep invalidateQueries` → 0; `refetchQueries` counts notes-panel:2 + refresh-button:3 = 5; `~/deploy-ui.sh` build clean.** | `components/notes-panel.tsx`, `components/refresh-button.tsx` | SHIPPED 2026-06-08 (Chat 5.13) — frontend HEAD `f59958` |
| 15 | P3-3 | Remove unused `from pydoc import doc` import. **Chat 5.13 SHIPPED (TD29): dead import at line 6, immediately shadowed by local `doc` variables in the serializer helpers. Behaviour-neutral. Verified on backend HEAD `090d96c`: `grep "from pydoc import doc"` → empty (literal string, true negative).** | `app/routers/holdings.py` line 6 | SHIPPED 2026-06-08 (Chat 5.13) — backend HEAD `090d96c` |
| 16 | P3-6 | Fix doc drift: `Project_State.md` Section 10 says `MONGODB_URL`, code uses `MONGODB_URI`. **Chat 5.13 SHIPPED (TD30): confirmed at HEAD that Project_State Section 10 already reads `MONGODB_URI` (with the explicit master_todo #16 note that earlier versions said `MONGODB_URL`) — the correction had landed in the Chat 5.12 Project_State. Row closed with no further code/doc edit beyond stamping it SHIPPED here. Doc-only confirmation; zero behaviour change.** | `docs/Project_State.md` | SHIPPED 2026-06-08 (Chat 5.13) |
| 17 | P3-7 | Add `pattern=r"^[A-Z0-9]{12}$"` to ISIN Path params on `/suggestions/{isin}/audit` and `/suggestions/{isin}/feedback`. **Chat 5.13 SHIPPED (TD31): added `pattern=r"^[A-Z0-9]{12}$"` alongside the existing `min_length=12, max_length=12` on the two ISIN `Path(...)` params — `get_feedback_audit_for_isin` (line 240, GET `/suggestions/{isin}/audit`) and `submit_feedback` (line 260, POST `/suggestions/{isin}/feedback`). `/runs/{run_id}` left untouched (it's an ObjectId, not an ISIN). Malformed ISINs now 422 at the boundary instead of reaching Mongo. Verified on backend HEAD `090d96c`: `grep -F 'pattern=r"^[A-Z0-9]{12}$"'` → 2 matches; a 12-char lowercase ISIN `INE002a01018` → 422 (rejected by pattern, not length); valid `INE002A01018` → 200.** | `app/routers/suggestions.py` ~245 | SHIPPED 2026-06-08 (Chat 5.13) — backend HEAD `090d96c` |
| 18 | P3-8 | Drop `"$options": "i"` from `transactions/search` regex — symbols already uppercased pre-query; "i" disables the index. **Chat 5.13 SHIPPED (TD32): the regex was at lines 91-92 in `search_transactions` (not ~102-115; that range is the date-bound block). Input is already `symbol.upper()` and symbols are stored uppercase, so the `"i"` flag was pure redundancy that disabled the `(symbol, trade_date)` index on the prefix regex. Dropped the flag (`query["symbol"] = {"$regex": f"^{escaped}"}`) and corrected the now-false "(case-insensitive)" inline comment. Behaviour-neutral for callers; restores index use. Verified on backend HEAD `090d96c`: `grep '$options'` → empty; clean regex present at line 113; `GET /transactions/search?symbol=tr` returns `total: 20` (parity with pre-change).** | `app/routers/transactions.py` ~102-115 | SHIPPED 2026-06-08 (Chat 5.13) — backend HEAD `090d96c` |

## PHASE 6 — External-service hardening — SHIPPED Chat 5.14–5.19 (COMPLETE)

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 19 | P2-5 | Replace Tavily quota check-then-act with atomic `find_one_and_update` against `tavily_quota`. **Chat 5.14 SHIPPED (TD33): collapsed the `get_today_quota()` pre-check + separate `_increment_quota()` `$inc` into ONE conditional `find_one_and_update` filtered on `{date_utc: today, calls_today: {$lt: TAVILY_DAILY_CALL_LIMIT}}` with `upsert=True, return_document=AFTER` and the existing `$inc`/`$setOnInsert`/`$set` blocks. Under the cap (or the day's first call) the filter matches/upserts and the `$inc` applies atomically; at/over the cap the existing same-day doc no longer matches, so the upsert attempts a second `date_utc==today` insert and the unique `date_unique` index raises `DuplicateKeyError`, caught and surfaced as `TavilyQuotaExceeded` (no credit consumed on refusal). Cap stays calls-only (`credits_today` tracked, not capped) — race fix, not a new ceiling (user-delegated). Added `from pymongo.errors import DuplicateKeyError`; removed the now-redundant pre-check in `search()`. Backend-only; callers (`news_fetcher.py`) untouched. Verified on EC2 at backend HEAD `4ac2c95`: `/health` ok/ok, `/suggestions/latest?direction=buy|sell` + `/cron/heartbeats` all 200 (import chain intact; the guard has no HTTP surface, so coverage is deploy + import-graph + boot regression).** | `app/services/tavily_client.py` ~110-145 | SHIPPED 2026-06-09 (Chat 5.14) — commit `4ac2c95` |
| 20 | P3-4 | Add 1-2 attempt retry with 30-60s backoff inside `notify.email()` on transient 5xx / 429. Don't retry 400s. **Chat 5.15 SHIPPED (TD34): added a 1-retry (2 attempts total) loop inside `email()` on transient HTTP 429/5xx with a blocking 30s backoff; 400s and any other client/no-status error return immediately (no retry). Added module-level `logging` + `time` imports + a module logger, two helpers — `_email_error_status()` (reads the Resend SDK exception's int status off `.code`/`.status_code`, falls back to `error_type=="rate_limit_exceeded"`→429) and `_is_transient_email_error()` (True only for 429 + 5xx) — plus three module constants (`_EMAIL_MAX_ATTEMPTS=2`, `_EMAIL_RETRY_BACKOFF_SECONDS=30`, `_EMAIL_TRANSIENT_STATUSES`). The `{ok, id, error}` return contract and the no-raise / swallow-exceptions guarantee are UNCHANGED, so the three callers that branch on `result["ok"]` (`digest_delivery`, `reconciliation._send_drift_alerts`, `cron_health_check` dual-transport) are untouched — verified by re-reading all three at HEAD before patching. Retry count / backoff seconds / blocking-`time.sleep` acceptability were user-delegated; chose 1 retry + 30s fixed blocking sleep (conservative end of the 30–60s window). Verified on EC2 at backend HEAD `7d77b9c` via a monkeypatched harness.** | `app/services/notify.py` | SHIPPED 2026-06-12 (Chat 5.15) — commit `7d77b9c` |
| 21 | P3-5 | Persist suggestion run BEFORE digest formatting; pass `inserted_id` explicitly to `send_combined_digest`. **Chat 5.16 SHIPPED (TD35): re-anchored at HEAD `67fd680` and found the run is ALREADY persisted before digest formatting (`_persist_run(run)` runs inside `_run_buy_pipeline`/`_run_sell_pipeline` and returns `result.inserted_id`); the real defect was that the id was discarded and then re-derived TWICE via `find_one(..., sort=[("run_date", -1)])`. Fix (model-carried id, NO signature change): `_persist_run` now sets `run.id = result.inserted_id` on the in-memory `SuggestionRun`, covering every persist path. `send_combined_digest` reads `buy_run_id = buy_run.id` and the internal `find_one` re-derivation is deleted (signature unchanged → sole caller `_do_both` untouched). `_do_both` attaches outcomes via `buy_run.id` / `sell_run.id`. Standalone `--direction=buy/sell` path passes `run_id=run.id` into the two `send_weekly_digest(run)` calls. Backend-only, ONE commit `f4168b3`. Verified on EC2 via grep + a monkeypatched harness with a `find_one` tripwire.** | `app/services/digest_delivery.py` ~470-490; `app/services/suggestion_engine.py`; `scripts/run_weekly_suggestions.py` | SHIPPED 2026-06-12 (Chat 5.16) — commit `f4168b3` |
| 22 | P3-1 | Reject NaN in `_to_decimal`: `if isinstance(v, float) and v != v: raise ValueError("NaN not allowed")`. **Chat 5.17 SHIPPED (TD37): nested the NaN check inside the existing `float` branch (`if v != v: raise ValueError("NaN not allowed")` before `return Decimal(str(v))`). `ValueError` (not `TypeError`) so the `Money = Annotated[Decimal, BeforeValidator(_to_decimal)]` validator surfaces a NaN as a Pydantic `ValidationError` → 422 at the API boundary; `TypeError` would have escaped as a 500. Scoped to the float ingress path only — `Decimal('NaN')` / Decimal128 NaN read paths are out of #22's scope. Verified on EC2 at backend HEAD `1d627d7` via landed-grep + an in-box harness.** | `app/models/_common.py` ~12 | SHIPPED 2026-06-12 (Chat 5.17) — commit `1d627d7` |
| 23 | P2-12 | Add fallback log file for `cron_run` heartbeat-insert failure (`/home/ubuntu/cron-heartbeat-fallback.log`); `cron_health_check` reads both sources. **Chat 5.18 SHIPPED (TD38): `_persist`'s `except Exception: pass` replaced with `except Exception: _append_fallback(doc)` — a new best-effort writer that appends the heartbeat as ONE JSON object per line to `_FALLBACK_LOG_PATH = "/home/ubuntu/cron-heartbeat-fallback.log"` and NEVER raises. New reader `count_today_heartbeats_from_fallback(...)` mirrors `count_today_heartbeats` exactly; `_parse_fallback_dt` normalizes tz-aware → naive UTC; a missing/unreadable file or malformed line contributes zero and never raises. `cron_health_check.main` merges its counts into the Mongo counts BEFORE the MISSING/FAILED evaluation, recording `metadata.fallback_heartbeats_merged`. Because a run's heartbeat lands in at most one source, the merge can never double-count. Verified on EC2 at backend HEAD `0515fef` via a temp-file + forced-Mongo-failure-tripwire harness + the live `cron_health_check.py` running end-to-end through the merge loop.** | `app/services/cron_heartbeat_service.py` + `scripts/cron_health_check.py` | SHIPPED 2026-06-12 (Chat 5.18) — commit `0515fef` |
| 24 | P3-9 | Wrap `cron_health_check.main` Mongo reads in try/except that fires an "anomaly: health-check itself failed" ntfy on errors channel even when Mongo is unreachable. **Chat 5.19 SHIPPED (TD39): wrapped `main`'s per-cron heartbeat-read loop — the ONLY place the script reads Mongo (the `count_today_heartbeats` calls) — in `try/except`. On a Mongo-read failure the `except` fires a dedicated "anomaly: health-check itself failed — could not read cron heartbeats from MongoDB" alert on BOTH transports: `push_public(channel="errors", priority="urgent", ...)` GUARDED in its own try/except + `notify.email(...)` (never raises). It then RE-RAISES so `cron_run` records the run as a `failure` (its heartbeat falls to the #23/TD38 disk fallback). The pure `ist_today_window_utc()`, disk-only `count_today_heartbeats_from_fallback()`, and in-memory `get_registry()` stay outside the wrap, and the #23/TD38 merge loop is preserved verbatim INSIDE the wrap. Backend-only, ONE commit `7fcda9e`. LAST Phase 6 item — Phase 6 COMPLETE.** | `scripts/cron_health_check.py` | SHIPPED 2026-06-14 (Chat 5.19) — commit `7fcda9e` |

## PHASE 7 — Reconciliation alerting & feedback correctness — SHIPPED Chat A (COMPLETE)

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 25 | P2-7 | Make `take_auto_snapshot` fire `push_public("price", ...)` when drift change exceeds threshold against last manual snapshot. ntfy only (email too noisy on daily cron). **Chat A SHIPPED: now fires `push_public("price", ...)` when invested drift vs the last manual snapshot exceeds the existing `DRIFT_ALERT_THRESHOLD_INVESTED` (₹1,000), via a new `_send_auto_drift_alert` that mirrors only the ntfy half of `_send_drift_alerts`. Rising-edge dedupe: fires only when this snapshot has drift AND the most recent prior auto snapshot did not. Current-value drift intentionally NOT alerted. `drift_invested`/`has_drift`/`alerts_sent` recorded on the auto snapshot. ntfy guarded. Verified on EC2 via a hermetic 4-case harness.** | `app/services/reconciliation.py` ~78-115 | SHIPPED 2026-06-14 (Chat A) — commit `1340396` |
| 26 | P2-6 / TD1 | Decide & implement: direction-aware feedback relabel. Add `"direction": payload.direction` to outcome filter in `submit_feedback`. Folds in TD1's lingering question; may overlap with an F-ticket — confirm via #3 (TD15) before patching. **Chat 5.9 note: TD15 reconciliation found no F-ticket already covering this; stays OPEN.** **Chat A SHIPPED: the literal "add `payload.direction`" would have crashed — `SuggestionFeedback` had only `action` + `note` with `extra="forbid"`. Added `direction: Literal["buy","sell"] = "buy"` (default keeps the current frontend working). The relabel query filters by direction, using the repo's existing back-compat guard `{"$or":[{"direction":"buy"},{"direction":{"$exists":False}}]}` for the buy branch and a bare `{"direction":"sell"}` for sell. Does NOT close TD1/#43 — cross-direction relabel policy stays DEFERRED. Verified on EC2 via a hermetic harness.** | `app/routers/suggestions.py` ~310-325 | SHIPPED 2026-06-14 (Chat A) — commit `6032b64` |

## PHASE 8 — New features per chat split plan — SHIPPED Chats 6–8 (COMPLETE)

Do AFTER Phases 2 + 6 so underlying surfaces are correct. Chats 6 and 7 are independent; Chat 8 (watchlist) came last among these because it multiplies data volume.

| # | Source | Item | Files / surface | Status |
|---|---|---|---|---|
| 27 | Chat 6 / F1 + F3 | Ad-hoc chat about suggestions (F1) + ad-hoc chat about a specific holding (F3). Share `conversations` collection scaffolding. New `POST /chat/suggestions` and `POST /chat/holdings/{isin}` endpoints. Frontend chat surface. **Chat 6 SHIPPED across 4 units + 1 route-shadow fix: Unit 1 `conversation.py` real model + `scope` field + `scope_created_desc` index + `instrument_service.lookup_by_isin`; Unit 2 `conversation_service.ensure_stock_context(isin)` on-demand enrichment (fundamentals/earnings/news via the existing cron-path services, freshness-gated, writes only Phase-2 reference collections); Unit 3 chat service (buy-research framing for not-yet-owned names, position/tax overlay for held names, latest buy+sell run summary for F1; single structured Sonnet `{answer,intent}` call mirroring `dossier_service._generate_one`; Sonnet $3/$15 MTok → Money cost_usd; persist) + `routers/conversations.py` (`POST /chat/suggestions`, `POST /chat/holdings/{isin}` ISIN-validated + 404 on unknown instrument, `GET /chat/history`) + `main.py` include; Unit 4 frontend `ChatPanel` + `StockResearchPanel` (symbol→ISIN via existing `/instruments/search`) + `lib/api.ts` + embeds on `/suggestions` and `/holdings/[isin]`, self-contained markdown renderer (no new npm dep). Path-2 enrichment makes F3 work for ANY known NSE instrument, held or not. Generalized the per-stock endpoint (kept the documented `/chat/holdings/{isin}` path). Surfaced + fixed a latent `/instruments/search` route-shadow (dynamic `/{exchange}/{symbol}` declared first → search unreachable; reordered). Filed #50 (news entity mis-tagging) + #51 (dividend_yield ×100 formatting). End-to-end verified on EC2.** | `routers/conversations.py` (NEW), `services/conversation_service.py` (NEW), `models/conversation.py`, `services/instrument_service.py`, `routers/instruments.py`, `app/main.py`, frontend `components/chat-panel.tsx` (NEW) + `components/stock-research-panel.tsx` (NEW) + `lib/api.ts` + `app/suggestions/page.tsx` + `app/holdings/[isin]/page.tsx` | SHIPPED 2026-06-14 (Chat 6) — backend `5e787c9`, frontend `6093f63` |
| 28 | Chat 7 / F12 + F15 | `/portfolio/risk-summary` (concentration & risk alerts) + `/portfolio/by-tag?tag=X` (tag-based portfolio views). **Chat 7 SHIPPED across 2 backend units + 2 frontend units, design-approved before code, both endpoints READ-ONLY over the existing Phase-1 holdings/price surfaces (no new collections/indexes/deps). Unit 1 (`97041621`): extracted `_annotate_holdings(holdings, latest_prices) -> (annotated, accum)` out of `compute_summary` (behaviour-preserving, gated by a `/portfolio/summary` curl-diff that printed `OK: /summary unchanged`), added `compute_risk_summary` on the SAME helper (no parallel aggregation) — `concentration_by_holding` (every priced holding, desc by %), `concentration_by_sector`, two-tier `alerts` from four module constants (SINGLE_HOLDING WARN 10/HIGH 20, SECTOR WARN 30/HIGH 50, the TOP_MOVERS_LIMIT pattern) + a low-severity `stale_price` data-quality note — and `GET /portfolio/risk-summary` mirroring the `/summary` skeleton. Verified: 2 warn alerts (GAEL 13.67%, LT 10.02%), 28 holdings, 7 sectors; risk top concentration == /summary concentration[0]. Unit 2 (`803e6610`): `GET /portfolio/by-tag?tag=X` — required `tag` (Query min_length=1; missing/empty → 422), `find({"deleted_at": None, "tags": tag})` (exact case-sensitive array membership), annotated via the SAME path as `holdings.list_holdings` so rows render in the existing HoldingsTable, plus tag-scoped totals; unknown tag → zeroed 200; reuses `_to_dec` (no parallel converter). Verified: retail → TATASTEEL count 1 matching cross-check, shape parity with /portfolio/holdings, missing+empty → 422. Unit 3 (frontend): RiskSummary/HoldingsByTag types + getRiskSummary/getHoldingsByTag in lib/api.ts (new types — ConcentrationItem/SectorBucket didn't match), new RiskSummaryCard mounted full-width on the dashboard via an independent useQuery, + Tags nav link. Unit 4 (frontend): dedicated `app/tags/page.tsx` — tag-pill selector from holdings.tags → getHoldingsByTag → tag-scoped totals + reused HoldingsTable. `npm run build`/lint clean via ~/deploy-ui.sh. No new TD/follow-ups.** | `routers/portfolio.py`, `services/portfolio_service.py`, frontend `lib/api.ts` + `components/risk-summary-card.tsx` (NEW) + `app/page.tsx` + `app/tags/page.tsx` (NEW) | SHIPPED 2026-06-15 (Chat 7) — backend Unit 1 `97041621`, Unit 2 `803e6610`; frontend `e14d6a75` |
| 29 | Chat 8 / F13 | Watchlist: `build_universe` becomes NIFTY 100 ∪ watchlist ∪ held − excluded. Extend `refresh_fundamentals.py` AND `fetch_news_for_universe.py` to cover watchlist ISINs. New `/watchlist` CRUD endpoints (reuse `monitored_stocks` with `status="watchlist"`). Frontend watchlist surface. **Chat 8 SHIPPED across 4 units + 1 CORS fix, design-approved before code; must come last among Phase-8 (multiplies data volume; gated after Phase 4 storage + Phase 6 Tavily race). Resolved set-math: `build_universe` = NIFTY 100 ∪ watchlist (deduped by ISIN, watchlist ISINs outside NIFTY 100 resolved from `instruments`); held stays filtered DOWNSTREAM by `filter_universe` exactly as before, so net buy candidates = (NIFTY 100 ∪ watchlist) − held − rejected − acted; the data-refresh universe additionally ∪ held. Unit 1 (`34ff906d`): `MonitoredStockWatchlistPatch` (mirrors `MonitoredStockFeedbackPatch`, extra=forbid, status pinned watchlist, exclude_none on dump) + `build_universe` evolved + `get_watchlist_isins()` single source of truth. NO change to `get_excluded_isins` (watchlist is structurally outside its rejected/tracking scan; flipping rejected→watchlist auto-un-excludes) and NO change to `indexes.py` (monitored_stocks is one-doc-per-ISIN via upsert-on-{isin}; the partial unique index stays scoped to status:tracking). Unit 2 (`a250d001`): new `routers/watchlist.py` — GET list (price-enriched) + GET one + PUT (idempotent create/update, un-excludes a previously rejected/acted ISIN, 404s unknown instrument, seeds identity from instrument, Money→Decimal128) + DELETE (hard, watchlist-only so feedback rows are never nuked); write-before-apply audit (F10) reusing the SAME audit collection via a WIDENED action Literal (`AuditAction` += watchlist_add/update/remove); `main.py` include. Unit 3 (`9857570b`): `refresh_fundamentals.py` + `fetch_news_for_universe.py` fold in watchlist ISINs (the data-volume multiplier — weekly fundamentals + earnings + daily news + 1 Tavily call/run each), reusing `get_watchlist_isins()`; Tavily blast-radius documented as a soft guardrail (no hard cap; degrades safely via the TD33 atomic quota guard). Unit 4 (frontend `58bf6369`): `lib/api.ts` watchlist types + bindings (+ widened MonitoredStocksAuditEntry action/new_status) + new `app/watchlist/page.tsx` (reuses the /tags shell + ui/table primitives; add via searchInstruments→PUT; per-row Remove→DELETE; refetchQueries) + Watchlist nav link on the dashboard. CORS fix (`67704025`): added `PUT` to `main.py` CORSMiddleware `allow_methods` — the browser preflight for the watchlist upsert was rejected (GET/DELETE worked, PUT didn't); curl-from-box doesn't preflight, so it only surfaced in the browser. End-to-end verified on EC2 + browser. No new TD/follow-ups.** | `services/suggestion_engine.py`, `models/monitored_stock.py`, `services/monitored_stocks_audit_service.py`, `app/main.py`, `routers/watchlist.py` (NEW), `scripts/refresh_fundamentals.py`, `scripts/fetch_news_for_universe.py`, frontend `lib/api.ts` + `app/watchlist/page.tsx` (NEW) + `app/page.tsx` | SHIPPED 2026-06-15 (Chat 8) — backend Unit 1 `34ff906d`, Unit 2 `a250d001`, Unit 3 `9857570b`, CORS `67704025`; frontend `58bf6369` |

## PHASE 9 — Cross-cutting cleanup before GO LIVE

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 30 | P2-1 | Replace 9 `datetime.utcnow()` call sites with project's `utcnow()` from `app.models._common`. | `routers/portfolio.py:43`, `services/scoring_service.py:116,813,890`, `services/dossier_service.py:166,192`, `services/fundamentals_service.py:370,485,505` | SHIPPED 2026-06-15 (Chat B) — 11 sites / 5 files (9 listed + 2 `conversation_service` stragglers); verified grep-clean + F3 runtime |
| 31 | P2-8 | Audit every `datetime.now(timezone.utc)` (tz-aware) Mongo write site; replace with `utcnow()`. Add CI lint rule (ruff custom or grep). | `routers/transactions.py:196,245,267`, `services/reconciliation.py:78,~138`, `services/tavily_client.py:50,~55`, `services/price_service.py:155` | SHIPPED 2026-06-15 (Chat B) — tree-wide: 15 write/query sites->`utcnow()` (incl. reconciliation:213 + price_service bulk_get_latest_intraday misses) + 19 `# tz-ok:` annotations + NEW `scripts/check_datetime_hygiene.py` tokenize-based guard; guard PASSED + negative controls fired |
| 32 | P3-2 | Pin `requires-python = ">=3.12,<3.14"` in `pyproject.toml`. | `pyproject.toml` | SHIPPED 2026-06-15 (Chat B) — pin + `uv lock` relock (no dep churn); `uv sync` clean, Python 3.12.3 in range |
| 33 | Review note | Stand up basic pytest harness. Minimum coverage: `_fifo_replay`, `preview_sell`, `validate_replay`, `recompute_holding` idempotency, `submit_feedback` write-before-apply ordering, `take_auto_snapshot` drift math. | `tests/*` (NEW) | SHIPPED 2026-06-26 (Chat B) — hermetic, zero-dependency harness; 28 tests across the 6 targets. NEW `tests/_fakes.py` (in-memory `FakeCollection`: find/find_one+sort, insert_one, update_one $set/$setOnInsert/upsert, delete_one/delete_many, count_documents; $ne/$in/$nin/$exists/$or/$and; `tx()`/`oid()` factories) + `tests/conftest.py` (`fake_db` fixture monkeypatching the `Collections.*` accessors). `recompute_holding` gets FULL DB idempotency (run twice -> stable aggregates + preserved created_at + exactly one active doc). DB-coupled targets monkeypatch `fetch_metadata`/`_get_our_numbers`/`_send_auto_drift_alert`/`log_change` (no Atlas/yfinance/ntfy). No new deps (pytest>=8.3 already in dev group; pythonpath=["."]). Verified on EC2: `uv run python -m pytest tests/ -v` => 28 passed; hygiene guard still PASSED. backend `04fd970` |
| 34 | Ops gap | Audit `/health` endpoint — does it actually ping Mongo or just return 200? Add Mongo + optionally yfinance heartbeat. **Chat A SHIPPED: `/health` already pinged Mongo but hardcoded `{"status":"ok"}` + HTTP 200 even when the ping failed. Now returns 503 + `{"status":"degraded","mongo":"fail"}` on ping failure and 200 + `{"status":"ok","mongo":"ok"}` on success. yfinance deliberately NOT probed on the hot path. Verified on EC2: live `/health` → 200 ok/ok; a forced-`ping()`-False TestClient probe → 503 + degraded.** | `app/main.py` | SHIPPED 2026-06-14 (Chat A) — commit `bd52c6b` |
| 35 | Ops gap | Add ntfy push on `insert_intraday_quotes` exception during market hours. **Chat A SHIPPED: `insert_intraday_quotes` wrapped in try/except — on failure it fires a GUARDED `push_public("errors", ...)` then re-raises so `cron_run` still records the failure heartbeat. Verified on EC2 via a hermetic harness.** | `scripts/refresh_prices_intraday.py` | SHIPPED 2026-06-14 (Chat A) — commit `bd52c6b` |
| 36 | Ops gap | Add `POST /admin/recompute/{isin}` (Tailscale-only). Replaces SSH-shell recovery for stuck holdings (#7 fallback). | `app/routers/admin.py` (NEW) | SHIPPED 2026-07-01 (Chat B) — NEW `app/routers/admin.py` delegates to `holdings_service.recompute_holding` (TD20 advisory lock; no parallel path), registered in `app/main.py`; ISIN pattern-validated (422); `{status: recomputed\|no_active_holding, isin, holding}`, 409 on lock contention; verified on EC2 (idempotent re-run, no-txn -> no_active_holding, OpenAPI surfaced, 28 pytest + hygiene guard green); backend `1ef0ead` |
| 37 | Ops gap | Rehearse Atlas backup → fresh-DB restore for `monitored_stocks` + `suggestion_outcomes` + `digest_deliveries`. Document exact `mongorestore` command in `Project_State.md`. | `docs/Project_State.md` | OPEN |
| 38 | Ops gap | Switch backend logging to JSON-structured (basicConfig → custom formatter or `python-json-logger`). | `app/main.py` (logging config) | OPEN |

## PHASE 10 — Chat 9 pre-launch cleanup

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 39 | Chat 9 / F11 | Capital gains pack. New `GET /tax/capital-gains?fy=YYYY-YY` returning STCG/LTCG breakdown per lot for the financial year. Frontend tax view page. | `routers/tax.py` (NEW), `services/tax_service.py` (NEW), frontend tax page | OPEN |
| 40 | Chat 9 | Realized P&L UI hide. Backend field stays (FIFO needs it); frontend stops rendering everywhere it currently shows. | Frontend: holdings-table, holding-stats, totals-row, holding-header | OPEN |
| 41 | Chat 9 / TD6 | Wire `holdings.stop_loss`. Reader + writer + alerts: when intraday price crosses below stop_loss, fire ntfy. Frontend stop_loss edit field on holding drill-down. Closes TD6. | `routers/holdings.py` (PATCH expansion), `services/price_service.py` (alert trigger), `scripts/refresh_prices_intraday.py`, frontend `holding-stats.tsx` | OPEN |

## PHASE 10.5 — USER ACCEPTANCE REVIEW (second-to-last; gates GO LIVE)

NEW (Chat 8, at the user's explicit request). This is the second-to-last stage. The tool is considered "production-ready" only AFTER this review is complete and all blocking findings are resolved or explicitly deferred. GO LIVE (Phase 11 / #42) MUST NOT start until then.

| # | Source | Item | Files / surface | Status |
|---|---|---|---|---|
| 52 | User request (Chat 8) | Complete end-to-end user acceptance review / UAT. The user (Sahil) personally walks every page, every flow, every cron output, and every endpoint and logs every bug, issue, or change request they want implemented. Each finding is filed as its OWN row in the NEW ITEMS phase (do not renumber existing rows) with a Source of `User review`, then triaged into a chat/bundle. **Gating:** Phase 11 GO LIVE (#42) must NOT start until this review is complete and all blocking findings are resolved or explicitly deferred. Run this in its own dedicated chat (the "User Review chat") AFTER Phase 9 (Chat B) + Phase 10 (Chats 9 + C) so the surface under review is feature-complete. Output: a triaged list of new rows + a go/no-go call on GO LIVE. | Whole product (frontend pages, backend endpoints, cron outputs, digests/alerts); findings land in `docs/master_todo.md` NEW ITEMS + `Project_State.md` | OPEN |

## PHASE 11 — GO LIVE

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 42 | Chat 10 / F7 | One-time real ICICI data import. Wipe + re-import via `scripts/refresh_from_icici.py` wrapper. Default wipe-and-replace scope: ONLY `transactions`, `transactions_staging`, `holdings`. Preserve `monitored_stocks`, `suggestion_outcomes`, `digest_deliveries`, all `instruments_*`, `prices_*`, `news_articles`. **GATED behind Phase 10.5 (#52): do not start until the user acceptance review is complete and blocking findings are cleared or deferred.** | `scripts/refresh_from_icici.py` (NEW or wrapping existing import_orderbooks) | OPEN |

## PHASE 12 — Post-launch deferred work

| # | Source | Item | Files | Status |
|---|---|---|---|---|
| 43 | TD1 | Make `monitored_stocks` direction-aware: add `direction` field, dual rows per ISIN. Reconcile with #26 — if that already solved the practical pain, this becomes optional internal cleanup. **Chat A note: #26 added direction-aware feedback RELABEL (a `direction` field on the payload + a direction filter on the outcome query) but did NOT make `monitored_stocks` itself direction-aware (still one row per ISIN, direction-agnostic). This row stays DEFERRED; revisit whether the practical pain is gone now that relabel is direction-correct.** **Chat 8 note: F13 watchlist reuses the same single-doc-per-ISIN model (status="watchlist"); if TD1 is ever taken, the watchlist status interplay with a direction field must be reconciled at that point.** | `models/monitored_stock.py`, `routers/suggestions.py`, `services/suggestion_engine.get_excluded_isins` | DEFERRED |
| 44 | TD3 | Split `dossier_service.valuation_verdict` from single string into `{verdict, rationale}` for cleaner UI. | `services/dossier_service.py`, frontend `suggestion-card.tsx` | DEFERRED |
| 45 | TD7 | Refactor `CandidateScore` so sell-side groups live as first-class fields instead of flowing through `group_meta`. Removes the buy/sell asymmetry in the data model. | `models/suggestion.py`, `services/scoring_service.py`, `services/explainability.py`, frontend | DEFERRED |

## NEW ITEMS — filed Chat 5.9 / 5.16 / 5.19 / Chat 6 (do not renumber existing rows)

| # | Source | Item | Files / surface | Status |
|---|---|---|---|---|
| 46 | TD21 | Registry-generated crontab migration (the deferred scheduler-architecture work). `CRON_REGISTRY` gains a parseable cron expression per `CronSpec` → new `scripts/render_crontab.py` renders a committed `ops/crontab` → `deploy.sh` installs it + a drift-validation step (`crontab -l` diff vs rendered). Version-controls the schedule and makes TD14-class drift structurally impossible, while keeping process isolation + deploy-safety (explicitly chosen OVER in-process APScheduler, which on the 1 GB t3.micro would let the ~5-min Sunday dossier run compete with the live API and die on every `systemctl restart`). Update the F4 "no silent failures" triad in Project_State §9 when it lands. Its own dedicated chat. | `app/services/cron_heartbeat_service.py`, `scripts/render_crontab.py` (NEW), `ops/crontab` (NEW), `deploy.sh` | OPEN |
| 47 | TD22 | `track_suggestion_outcomes` cron FAILS every weekday (19:45 IST; the 21:00 IST health email shows `track_suggestion_outcomes: 0 success / 1 failure` daily). Distinct from TD14 — surfaced during the Chat 5.9 TD14 investigation. Root-cause the daily failure (read `cron-outcomes.log` at HEAD + the script body) and fix. **Chat A SHIPPED: root cause was a stats-dict key drift. `snapshot_open_outcomes()` returns its count under `active_outcomes` (renamed in Commit A.5 when selection broadened from "open" to all non-expired), but the cron's `main()` still read `stats["open_outcomes"]` → `KeyError` on every run, raised inside the `cron_run` context (at line 27) before any result line flushed. Fix: read `stats["active_outcomes"]` and rename the heartbeat metadata key + print label to match. Safe rename. Reproduced live at `6032b64`, then verified the fix at `4b638e6`.** | `scripts/track_suggestion_outcomes.py`, `app/services/outcome_tracker.py` | SHIPPED 2026-06-14 (Chat A) — commit `4b638e6` |
| 48 | TD36 | Stale-doc cleanup (NEW Chat 5.16, NO CODE): README.md (backend) + `docs/data_flow.md` still describe the Tavily quota as "monthly", but the code is DAILY — `TAVILY_DAILY_CALL_LIMIT`, one `tavily_quota` doc per `date_utc`, resets 00:00 UTC. Correct the "monthly" wording in both files to "daily (resets 00:00 UTC)". Doc-only. **Chat A SHIPPED: corrected all "monthly" → "daily (resets 00:00 UTC)" in both files. A code check also surfaced and fixed in the same commit: data_flow's "resets at 00:00 IST on the 1st" → "00:00 UTC each day", and a README env var that does not exist (`TAVILY_MONTHLY_QUOTA` → the real `TAVILY_DAILY_CALL_LIMIT`, enforced in `tavily_client`). Verified via grep.** | `README.md`, `docs/data_flow.md` | SHIPPED 2026-06-14 (Chat A) — commit `fae6edf` |
| 49 | TD40 | (NEW Chat 5.19, surfaced during #24 verification) The idle `weekly_suggestions_sell` CronSpec carries `expected_weekdays={6}` (Sunday), so `cron_health_check` flags it `MISSING: weekly_suggestions_sell` and sends a REAL anomaly alert (ntfy + email) every Sunday — even though the umbrella `weekly_suggestions` cron (`--direction=both`) covers both buy and sell. False positive. **Chat A SHIPPED (option 1): set the idle spec's `expected_weekdays=set()` so `is_expected_today()` is always False; the spec stays in the registry/dashboard as `expected=False`. Re-read the #23/TD38-hardened file at HEAD and left `_append_fallback` / `count_today_heartbeats_from_fallback` / `_persist` untouched. Verified on EC2.** | `app/services/cron_heartbeat_service.py` (CRON_REGISTRY `weekly_suggestions_sell` spec) | SHIPPED 2026-06-14 (Chat A) — commit `6032b64` |
| 50 | Chat 6 | News entity mis-tagging in `news_articles.entities_isins`: the #27 F3 chat for HDFCBANK (INE040A01034) returned classified articles about TCS and Kenya's Family Bank — i.e. articles tagged with HDFC Bank's ISIN that are not about HDFC Bank. The chat query correctly filters `entities_isins: <isin>`; the defect is upstream, in how `news_fetcher` / `news_classifier` attach `entities_isins` during fetch/classify (likely over-broad Tavily matching or classifier entity attribution). Directly degrades chat (#27) and likely the news_score signal + dossier news block. Investigation-first: sample mis-tagged docs at HEAD, find where the wrong ISIN is attached, then tighten. **Chat 8 note: now also affects watchlist names (F13) — every watchlisted ISIN pulls daily news, so the mis-tagging blast-radius grows with watchlist size.** | `app/services/news_fetcher.py`, `app/services/news_classifier.py`, `news_articles` | OPEN |
| 51 | Chat 6 | `dividend_yield` ×100 formatting inconsistency: the #27 chat showed Reliance "Dividend Yield: 46%" (should be ~0.46%). Verified the chat formatter (`conversation_service._fmt_pct`) is byte-consistent with `dossier_service._fmt_pct` (`f"{v*100:.2f}%"`), so this is a PRE-EXISTING app-wide issue, not new to #27 — some yfinance `dividend_yield` values are stored already-as-percent (0.46) while `_fmt_pct` assumes a fraction and multiplies by 100. Decide the canonical unit (store as fraction OR percent) and fix at the source (`fundamentals_service` ingest) and/or both `_fmt_pct` formatters consistently — do NOT diverge the chat formatter from the dossier. Affects the dossier news/fundamentals block and the chat answer. | `app/services/fundamentals_service.py`, `app/services/dossier_service.py`, `app/services/conversation_service.py` | OPEN |

> User-review findings (Phase 10.5 / #52) will be appended here as additional rows once that chat runs — Source `User review`, do not renumber existing rows.

---

## Summary by phase

| Phase | Items | Theme | Gating |
|---|---|---|---|
| 1 | 1-3 | Unblock ops + reconcile docs | No code — SHIPPED Chat 5.9 |
| 2 | 4-8 | Transactions/holdings/audit invariants | Foundation for Chat 9 + 10 — SHIPPED Chat 5.10 |
| 3 | 9-11 | Intraday & price correctness | Foundation for Chat 8 sell-side — SHIPPED Chat 5.11 |
| 4 | 12-13 | Storage hygiene (TTL + purge) | Foundation for Chat 10 real data — SHIPPED Chat 5.12 |
| 5 | 14-18 | Frontend correctness + quick wins | Independent — SHIPPED Chat 5.13 |
| 6 | 19-24 | External-service hardening | Foundation for Chat 8 (parallelism) — COMPLETE: #19 SHIPPED Chat 5.14, #20 SHIPPED Chat 5.15, #21 SHIPPED Chat 5.16, #22 SHIPPED Chat 5.17, #23 SHIPPED Chat 5.18, #24 SHIPPED Chat 5.19 |
| 7 | 25-26 | Reconciliation alerting + feedback direction | COMPLETE — #25 + #26 SHIPPED Chat A |
| 8 | 27-29 | New features (Chats 6, 7, 8) | COMPLETE — #27 SHIPPED Chat 6; #28 SHIPPED Chat 7; #29 SHIPPED Chat 8 |
| 9 | 30-38 | Pre-launch sweep + ops gaps | Last clean state before GO LIVE — #34 + #35 SHIPPED (Chat A); #30 + #31 + #32 SHIPPED 2026-06-15 (Chat B); #33 SHIPPED 2026-06-26 (Chat B); #36 SHIPPED 2026-07-01 (Chat B); #37-38 OPEN (Chat B continues) |
| 10 | 39-41 | Chat 9 pre-launch cleanup (F11 + realized P&L hide + stop_loss) | OPEN |
| 10.5 | 52 | USER ACCEPTANCE REVIEW (user walks the whole tool, files findings) | OPEN — second-to-last; gates GO LIVE |
| 11 | 42 | Chat 10 GO LIVE (F7 real data import) | OPEN — gated behind Phase 10.5 |
| 12 | 43-45 | Deferred TDs (TD1, TD3, TD7) | DEFERRED |
| NEW | 46-51 | TD21 scheduler migration (OPEN) + TD22 outcomes-cron failure (SHIPPED Chat A) + TD36 Tavily doc cleanup (SHIPPED Chat A) + TD40 weekly_suggestions_sell Sunday false MISSING (SHIPPED Chat A) + #50 news entity mis-tagging (OPEN, Chat 6) + #51 dividend_yield ×100 formatting (OPEN, Chat 6) | Filed Chat 5.9 (46-47) / Chat 5.16 (48) / Chat 5.19 (49) / Chat 6 (50-51); schedule independently |

---

## Cross-references

- `docs/Project_State.md` Section 0 — bootstrap (this file is item 5 on the read list)
- `docs/Project_State.md` Section 13 — historical "shipped" log and chat split plan
- `docs/Project_State.md` Section 18 — TD registry (each row cross-references its master_todo `#N`)
- `docs/data_flow.md` — data flow reference

## How to update this file

- **Every chat ends with two doc commits:** `Project_State.md` full-file replacement AND a `master_todo.md` update (status changes + current-position pointer advance).
- **Never delete shipped rows.** Change `OPEN` → `SHIPPED <YYYY-MM-DD> (Chat <N>)`. Shipped rows are the audit trail.
- **Adding new items mid-stream** (e.g., a fresh bug discovered): append the row at the END of the appropriate phase (or in the NEW ITEMS phase); do not renumber existing items (item numbers are stable references).
- **Re-ordering between phases:** allowed if the rationale block explains why; document the reason in the row's Notes column if needed.
- **If a row becomes obsolete** (e.g., TD15 reconciliation reveals an F-ticket already shipped a fix): change Status to `DROPPED <YYYY-MM-DD> (Chat <N>) — <one-line reason>`. Do not delete.
- **Current-position pointer** at the top must advance every time the lowest-numbered OPEN row changes.

End of master_todo.md.